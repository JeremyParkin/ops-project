# Project Context

This document is durable handoff context for a fresh Codex coding session. It describes the current implementation, not the full chat history.

## Product Purpose

This project is an early vertical slice of a flexible business operations platform. The eventual product should let users define their own operational systems: entity types such as Client, Employee, Project, Deliverable, Task; fields on those entities; relationships between records; records themselves; and deterministic automations around those records.

The product direction is deliberately metadata-driven. The UI should be generic enough that a newly configured entity immediately works with the same record form, table, relation controls, and workflow machinery without adding entity-specific React components.

Core product/design principles established so far:

- Make the system easy to configure, but reluctant to let users accidentally make data inconsistent.
- Prefer explicit, reversible lifecycle states before destructive operations.
- Do not silently rewrite, null, cascade, or delete dependent data.
- Preserve immutable internal identities even when user-facing names change.
- Favor deterministic software behavior. Longer term, AI should help users configure this deterministic system through natural language; AI should not become the routine executor of ordinary operations that the product itself can perform reliably.
- Keep the UI understandable and specific, while keeping the underlying implementation generic.

## Tech Stack

- TypeScript
- Next.js 16.3.1 with the App Router and React 19
- Supabase/Postgres for persistence
- `@supabase/supabase-js` from server-side code
- No ORM
- Node requirement: `>=24 <25`
- Styling is simple app CSS/Tailwind-compatible setup; no UI component framework
- Playwright Test for E2E tests

Normal server-side Supabase access is centralized in `lib/supabase/server.ts`. It reads:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

The separate server-only `lib/supabase/admin.ts` reads `SUPABASE_SECRET_KEY` and is reserved for E2E fixtures/bootstrap administration. Normal application runtime uses request-scoped authenticated Supabase clients with the publishable key; RLS is the tenant-security boundary.

Useful npm commands:

```bash
npm run dev
npm run lint
npx tsc --noEmit
npm run build -- --webpack
npm run test:e2e
npm run test:e2e:ui
```

Use the webpack build form. The standard verification sequence must be run sequentially:

```bash
npm run lint
npx tsc --noEmit
npm run build -- --webpack
npm run test:e2e
```

Do not run typecheck and build concurrently; they can race over `.next` generated types.

## Domain Model

The canonical TypeScript domain types are in `lib/domain/types.ts`.

`Workspace` is the top-level tenant boundary. Authenticated users receive access only through explicit `workspace_memberships`; repository calls keep `workspaceId` explicit. The validated `active_workspace_id` cookie is UI convenience state, never authorization.

Phase 7A roles and capabilities:

- Every membership has exactly one same-workspace `workspace_role`. Existing memberships received a broad built-in compatibility role during migration; custom roles use the same tables and capability vocabulary.
- Workspace membership continues to grant workspace-wide **read** visibility in 7A. Capability IDs gate interactive mutation/configuration; role names are descriptive only and never authority.
- Supported capability IDs are `schema.manage`, `automation.manage`, `records.operate`, `processes.operate`, `operations.view`, `workspace.manage_settings`, `workspace.manage_members`, `workspace.manage_roles`, and `workspace.manage_organization`.
- The authoritative checks are RLS and hardened authorized RPCs. `getWorkspacePermissionContext`/`requireWorkspaceCapability` provide server-action and UI convenience only.
- `schema.manage` covers entity/field/schema/display configuration; `automation.manage` covers workflow and Process Template configuration; `records.operate` covers record, relation, and saved-view writes; `processes.operate` covers interactive run starts, human completion, approval decisions, and action-step retry operations. Member/role administration and organization structure are separately capability-gated.
- Role assignment, role editing, and role deletion-with-reassignment are atomic, workspace-advisory-serialized RPCs. They validate the proposed final workspace state, reject self-escalation/cross-workspace IDs/unknown capabilities, and preserve at least one membership whose role has both member and role management capabilities. Ordinary members still cannot enumerate raw `workspace_memberships` rows; the management UI uses narrow authorized role/member listing RPCs.
- Trusted system-owned workflow/process execution remains separate from interactive checks: service-role execution can use the canonical automation paths, while interactive callers cannot invoke scheduler/dispatcher system paths arbitrarily.

Phase 7B teams and reporting:

- `workspace.manage_organization` gates all organizational mutations. Existing roles that already had both member- and role-management capabilities received it during migration; newly created custom roles may include it through the same canonical capability vocabulary.
- Teams are flat workspace-scoped metadata. A member may belong to multiple teams; a team may have multiple leads. Leads are organizational facts only and never confer permissions.
- Each member may have zero or one direct primary manager. Manager assignment is workspace-advisory-serialized and rejects self-reporting, foreign workspace IDs, and reporting cycles, including concurrent changes.
- Raw organization tables are unavailable to `authenticated`. Hardened authorized RPCs perform management writes and scoped read helpers reveal only management data to organization managers, or a member's own teams, manager, direct reports, and team peers where relevant. Raw `workspace_memberships` remains self-only.
- Archiving a team blocks new members/leads; hard deletion requires an empty team. Removing a workspace membership cascades only its current team, lead, and reporting relationships. Organization edits never change roles or capabilities, and do not affect My Work, record visibility, or system automation.

Phase 7C manager portfolio:

- Team Work (`/team-work`) is a separate operational portfolio; My Work remains the current user's personal assigned-work queue. It includes only assigned active `human_task` and `approval` StepRuns plus the same deterministic pending projection already used by My Work.
- Management scope is current direct reports plus members of current active teams the viewer leads, with no recursive reporting. The current user is excluded; overlapping people/work are deduplicated while retaining direct-report and team provenance. Archived teams do not contribute scope.
- `operations.view` gates the portfolio, and a user must have non-empty current management scope for its navigation/page to appear. Reporting and team-lead facts grant scope only, never capabilities. The read RPC is fixed-search-path `SECURITY DEFINER`, derives scope from `auth.uid()`, and rejects non-members/no-capability callers; it does not alter the deliberate workspace-wide process read baseline.
- Team Work has validated All, Direct reports, active led-team, and individual filters. Forged/out-of-scope parameters redirect to the canonical unfiltered page and can never expand scope. It renders no completion, approval, retry, or mutation controls; rows drill into existing Process Run and record-detail pages.

Phase 7D operational analytics:

- Operational Analytics (`/analytics`) measures work, not people: every metric is a raw, traceable count or median derived directly from `process_runs`/`process_step_runs` history, never a composite score, rank, or performance/efficiency label. Gated identically to Team Work (`operations.view` plus non-empty current management scope, same nav condition) — there is no workspace-wide analytics surface in v1.
- `private.managed_user_ids` is the one authoritative scope derivation Team Work and Analytics both use; `list_managed_people_context_authorized` was refactored to delegate to it with its external behavior unchanged (reverified against the full existing `team-work.spec.ts` suite). Portfolio-level totals (workload snapshot, throughput, timeliness) are always computed from this deduplicated set directly, never by summing per-team rows.
- Throughput counts only completed `human_task`/`approval` StepRuns as "completed work" — system routing (`parallel_split`/`parallel_join`), automated action nodes, and timer/condition waits never inflate it just because a template contains more of them. Completed ProcessRuns are reported as a separate figure. Bottleneck/dwell-time analysis separately includes `wait`/`condition_wait` alongside `human_task`/`approval` (their dwell time is genuinely useful process information) but excludes `parallel_split`/`parallel_join`/`action`.
- Overdue reuses the exact existing derived rule (`status = 'active' AND due_at IS NOT NULL AND due_at < now()`). The overdue rate's denominator is only active assigned `human_task`/`approval` StepRuns that actually carry a `due_at` — undated work is excluded from the denominator entirely, not merely from the numerator.
- Time periods are 7/30/90-day rolling UTC windows (no calendar-day alignment; there is still no persisted user/workspace timezone setting). "Timeliness trend" reports on-time vs. late completions among dated, already-completed work — a literal historical "overdue count over time" is not reconstructable from current schema without a parallel event history, so this was deliberately redefined rather than approximated.
- Historical attribution uses **current** management scope, not a point-in-time org snapshot: a person's historical work always appears under whoever currently manages them, with a visible on-page disclaimer stating this plainly. Team-level workload rows are limited to teams the caller currently leads, exclude the caller's own membership (consistent with every other scope query excluding self), and may legitimately overlap when one managed person belongs to multiple led teams — they are explicitly labeled as contextual and are never summed into a portfolio total.
- Aggregation happens entirely in Postgres via five new capability-gated RPCs (`get_operational_summary_authorized`, `get_throughput_trend_authorized`, `get_bottleneck_metrics_authorized`, `get_workload_by_person_authorized`, `get_workload_by_team_authorized`), one per concern rather than one combined query, each independently callable/testable. No StepRun history is pulled into Next.js for aggregation. Charts are minimal hand-rolled inline SVG (`analytics-trend-chart.tsx`); no charting dependency was added.
- Drill-downs reuse existing pages exclusively: bottleneck rows link to the process template's edit page (safe by construction — a template row can never be hard-deleted while any run references it); person/team workload rows link to Team Work's existing `?person=`/`?scope=team&id=` filters. No new query-builder, run-listing page, or reporting engine was built.

Phase 8A worker experience and information architecture (navigation/copy only — no new tables, RPCs, or authorization semantics):

- Navigation is now genuinely capability-gated, not just under-labeled: the "Configure" section (renamed from "Workspace setup") and each link inside it only render for a caller who actually holds the matching capability — this closed a real, pre-existing gap where Workflows/Processes/Create Entity were visible (though not usable) to every workspace member regardless of capability. The entity page's "Manage" action is now gated by `schema.manage` the same way. Team Work/Analytics keep their existing Phase 7C/7D `operations.view` + non-empty-scope gate, unchanged.
- Terminology: "Workflow" is renamed to "Automation" in every product-facing label (nav, page headers, buttons, the capability description text) — internal names (`WorkflowDefinition`, `workflow_execution_logs`, the `/workflows` route) are unchanged, since this is a label change, not a domain rename. "Process" is unchanged (kept deliberately distinct from "Automation": reactive/instant vs. multi-step/sequenced). "Entity" is avoided in ordinary product-facing copy where practical (the entity page's eyebrow, the "Data model"/"Create object" creation flow, the "Business objects" nav/Home section labels) — `EntityType`/`EntityRecord` and all schema/table names are unchanged.
- Home (`app/page.tsx`) leads with a personal "What needs you" attention strip (My Work data, capped at 3 items) and, for managers/leads only, a single team-attention line — never a dashboard, never any config/admin content, even for admins. Business-object cards are decluttered to Open + Add; per-card saved-view shortcuts were removed from Home (view switching already lived on the entity page itself and still does).
- My Work and Team Work rows now pair the origin record's label with its business-object type name (e.g. "Acme Corp · Clients"), reusing already-fetched entity-type data — no new query, no change to the underlying `MyWorkItem`/`ManagedWorkItem` shape.
- A new CSS token, `--color-grit-light` (`#a39e92`, ~6.5:1 contrast on the graphite/slab sidebar ground vs. the prior `--color-grit`'s ~4.8:1 there), replaces `text-grit` specifically for inactive sidebar nav text. `grit` itself is unchanged and still used everywhere else (including on the paper ground, where its contrast was already ample).
- Verified via a real manual walkthrough (not just automated assertions) as four personas — pure worker, manager/lead, builder/admin, and a brand-new empty workspace — using a throwaway fixture created and torn down for the session: confirmed correct nav/Home content and gating for each, the new contrast token's computed color, and no regressions in Team Work/Analytics/onboarding rendering.

Application Shell & Scalable Navigation (post-8A; a global-header + contextual-navigation architecture replacing the per-page sidebar, no authorization changes — purely a relocation/consolidation of existing capability gates):

- Every page previously mounted its own `<WorkspaceNavigation>` sidebar; navigation now lives once in `app/(app)/layout.tsx`, which renders the persistent `<AppHeader>` and moved every authenticated route into the `app/(app)/` route group to reach it — routes are unchanged (a route group folder is invisible to the URL). `app/components/entity-navigation.tsx` (the old sidebar) was deleted.
- Global tier ("where am I"): Home / Work / Business / Analytics / Configure across a persistent Graphite header, plus a compact records-search field and an Account menu. Contextual tier ("how do I move within this"): a left rail rendered only on a business object's own pages (list/detail/edit), carrying its saved views — Home/My Work/Team Work/Analytics/Configure intentionally have no contextual rail.
- Business objects no longer enumerate as a flat, unbounded sidebar list. The Business dropdown shows a deterministic alphabetical slice (first 6 active objects) plus a permanent "All objects" link to a new index page (`app/(app)/entities/page.tsx`) with a client-side name filter, description, `schema.manage`-gated "Create object", and `schema.manage`-gated archived-objects toggle. This was scoped deliberately narrow: no favorites/pins/recency tracking (nothing persists real view history yet, so the dropdown is explicitly a "quick-jump," not a "recently viewed" claim), no object grouping/categories, no configurable icons.
- Configure holds Automations (`automation.manage`), Processes (`automation.manage`), Data model (`schema.manage`), and Workspace settings (`workspace.manage_*`), each gated independently, with the Configure trigger itself gated on the union of all three capabilities. An earlier interim version of this pass dropped `schema.manage` from that union entirely (reasoning: schema management had moved to Business → All objects, so Configure had nothing schema-related to show) — that was corrected after review: Configure is meant to hold a first-class "Data model" destination for schema.manage users, not silently drop the concept because a browsing surface for it exists elsewhere. "Data model" reuses the exact same `/entities` route and `EntityTypeBrowser` component Business → All objects links to (no second object-management system) but at `?manage=true`, presenting it as a distinct, builder-oriented configuration surface rather than an identical page reached two ways — see the `?manage=true` presentation-mode split described under Application Structure. Because `canConfigure` exactly unions the same three capabilities gating the individual items, no combination of capabilities can ever produce an empty dropdown.
- Saved views moved from a horizontal pill-tab row atop the entity page into the contextual rail as `<ObjectContextNav>`, framed as ways of seeing the object (not configuration) — "All {Object}" plus each view, with `aria-current="page"` marking the active one. Creating/editing/deleting a view is unchanged and stays in-page under the existing "Manage views" disclosure; the rail's "+ New view" link forces `?view=all&newView=true#manage-views` so it lands in create mode (not "Edit {whatever the current view happens to be}") and auto-opens the disclosure.
- The header's dropdowns (`app/components/nav-menu.tsx`) are a WAI-ARIA disclosure pattern, not an application menu: `aria-expanded`/`aria-controls` on the trigger, a plain `role="group"` panel of ordinary links/buttons — deliberately not `role="menu"`/`"menuitem"`, since the component doesn't implement full arrow-key/type-ahead menu keyboard behavior and claiming that role would promise interaction it doesn't deliver. Click-to-open (not hover-intent); Escape closes and refocuses the trigger; an outside click closes; a route change closes it by remounting via `key={pathname}` rather than a setState-in-effect (avoids a React cascading-render lint violation while giving the same "always closed after navigating" behavior).
- Workspace switching and sign-out moved from the old sidebar's always-visible `<select>`+button and bare button into the Account menu (labeled by the current workspace name, with a stable `aria-label="Account menu"` for tests/assistive tech independent of that name) — one membership button per workspace instead of a `<select>`, since a dropdown of named buttons reads better than a select-then-submit pair once it's already inside a menu.
- Responsive collapse is deliberately minimal, not a mobile redesign: below the `lg` breakpoint the whole primary nav collapses into one "Menu" disclosure holding a flattened version of every destination; above it, the row is the full header. All dropdown panels (including the mobile one) cap at `max-h-[70vh]` with `overflow-y-auto` so a workspace with many business objects can't push a panel off-screen with no way to scroll to the rest.
- A pre-existing bug surfaced during this work and fixed in passing: `listCurrentUserMemberships` (`lib/auth/workspace.ts`) cast the embedded `workspaces` relation to an array (`workspaces[0]?.name`) when Supabase/PostgREST actually returns a single object for this to-one relationship — `[0]` on a non-array object is always `undefined`, so every membership's `workspaceName` silently resolved to `""`. This was latent in the old sidebar's workspace-switcher `<option>` labels too; it became immediately visible once the Account menu started using the workspace name as its own trigger label. Fixed to cast to the object directly.
- Manual dogfood covered a worker with a small object count, a worker with 16 objects (verifying the dropdown stays a fixed 6-item slice and the All Objects page's search/scroll handles the rest calmly), a manager, a schema.manage-only builder (the capability gap above), and a brand-new empty workspace (onboarding renders correctly under the new shell; the Business dropdown shows its empty state); all via throwaway fixtures created and torn down for the session. Verified at desktop, tablet, and mobile viewport widths, including the mobile "Menu" panel's overflow behavior (initially found and fixed hanging off the right edge of a narrow viewport — the trigger sits left-of-center in the header, so a left-aligned panel of fixed min-width overflowed; switched that one instance to right-aligned).

`EntityType` represents a user-defined kind of record. It has `id`, `workspaceId`, `name`, `slug`, optional `description`, optional `displayFieldDefinitionId`, optional `archivedAt`, and ISO 8601 UTC `createdAt`/`updatedAt` strings. Entity IDs are stable; names, slugs, and display-field configuration are user-facing metadata.

`FieldDefinition` represents a field on an entity. Supported field types are:

- `text`
- `number`
- `date`
- `boolean`
- `relation`

Each field has an immutable `key`, unique within the workspace. Record JSON values are keyed by this `key`, not by mutable display name or slug. This lets users rename fields without migrating stored primitive values. `FieldDefinition.id` is used for relational/config references such as workflows and relation rows. `relatedEntityTypeId` is present only for relation fields. Fields also have `required`, `position`, optional `archivedAt`, and ISO timestamps.

`EntityRecord` represents one record of an entity. Primitive field values are stored in `entity_records.values` JSONB under immutable field keys. Relation values are persisted in `entity_record_relation_values`, not only as JSON strings. Repository reads merge relation target record IDs back into `EntityRecord.values[field.key]` so generic forms/tables can treat records uniformly.

`FieldValue` is `string | number | boolean | null`. Dates are stored as `YYYY-MM-DD` strings. All timestamp fields are ISO 8601 UTC strings.

Record display labels are centralized in `lib/domain/record-repository.ts`: use a configured active text display field when present, otherwise the first active text field by position, otherwise a shortened record ID. If the configured display-field value is empty, labels fall back directly to the shortened record ID. Archived target records can still display with an `(Archived)` suffix where needed.

Workspace record search is centralized in the same repository. It searches active text fields on active records in active entities, groups results by entity, and uses the centralized label resolver for every result.

Workspace onboarding and Home:

- A workspace is new only when it has zero entity types, including archived entities. An archived-only workspace remains established and never re-enters onboarding.
- New workspaces receive a two-step Home setup flow. Users can select Clients, Projects, Tasks, and/or Sales / Opportunities, review the resulting metadata and inferred direct relations, then create everything atomically. “Start from scratch” opens the existing entity-definition form and creates no template metadata.
- Starter structures are ordinary editable metadata, not persistent product modes. Relations are added only when both selected endpoints exist: `Project.Client`, `Task.Project`, and `Opportunity.Client`.
- Established Home is operational: entity cards are primary and show their active record count, Open and Add shortcuts, and capped saved-view links. The Add shortcut targets the existing `#add-record` form.
- Workspace navigation prioritizes Home, Search, and active entities. Workflows, Create entity, and archived-entity management live under the secondary Workspace setup disclosure. Active entity pages are records/views/add-record first; schema and lifecycle controls are available through explicit `?manage=true` mode.
- Operational UX Polish keeps the established workspace table-first: compact record/view controls precede the records table, Add Record is an inline native disclosure rather than a persistent form, and empty entity/view states explain whether the entity has no records or the selected view has no matches. No new record route, modal, domain capability, or persistence model was added.
- Phase 8C.1 — Empty-workspace authority correction: `isNewWorkspace` Home rendering now branches on `schema.manage` (`app/(app)/page.tsx`). A caller with it sees the unchanged two-step onboarding wizard; a caller without it sees a read-only "Nothing has been set up yet. Ask a workspace admin to create your first business object." state — no form, no "Start from scratch" link. `/entities/new` (the wizard's "Start from scratch" target) gained the same `schema.manage` page-level redirect-to-Home gate. This closed a genuine authority gap, not just a UI one: `create_entity_types_with_fields_authorized` (0026) predated the roles/capabilities system (0045) and was only ever membership-checked — since 0045 made `workspace_memberships.role_id` mandatory for every membership, "a member with no assigned capabilities" is no longer a state any membership can be in, so the original justification no longer held. Verified before changing it that no workspace can exist today with a member but zero schema.manage-capable members as a result of the app's own bootstrap process, because there is no app-mediated "create a workspace" flow at all: a workspace's first role(s) and membership(s) are created entirely by manual SQL (per `supabase/README.md`) from whoever stands the workspace up, who necessarily assigns capabilities at that point — and since creating any further role requires `workspace.manage_roles`, a bootstrap that withheld `schema.manage` from every initial member would leave the workspace permanently unmanageable anyway, a strictly worse state than today's gap. `create_entity_types_with_fields_authorized` was updated (migration `0060_workspace_onboarding_schema_capability.sql`) to call `private.require_interactive_workspace_capability(p_workspace_id, 'schema.manage')`, the same helper every other interactive schema-write wrapper already uses — closing the boundary at the database layer, not merely hiding UI. `createWorkspaceStarterStructure` (`app/actions.ts`) also gained an explicit app-layer `schema.manage` check ahead of the RPC call — deliberately asymmetric with `createEntityDefinition`/`/entities/new`, which needed only a page-level gate for UX (its underlying `create_entity_type_with_fields` RPC is SECURITY INVOKER and was already correctly blocked by the pre-existing `entity_types_schema_write`/`field_definitions_schema_write` RLS policies; there was never an actual bypass there, just a confusing dead-end form). Migration `0060` has since been applied and confirmed live (a schema.manage-less caller's direct RPC call now fails with `Permission denied: schema.manage`, reconfirmed directly during Phase 8C.2 dogfood) — the earlier "not yet applied" note here is now stale and has been corrected.

`EntityView` represents a saved table view for one entity. Saved views live in `entity_views` with `name`, `position`, `isDefault`, JSONB filters/sorts/column field IDs, and ISO timestamps. They configure table presentation only; they do not copy schema or records.

## Data Integrity And Lifecycle Rules

Workspace boundaries are enforced structurally throughout the schema using workspace-scoped foreign keys and composite constraints. Relation rows carry source and target entity IDs explicitly so Postgres can prove:

- source record belongs to the source entity
- field definition belongs to that same source entity
- relation field declares the target entity
- target record belongs to the target entity
- all rows belong to the same workspace

Records:

- `entity_records.archived_at` implements archive/restore.
- Active record tables exclude archived records by default.
- Archived records do not appear as new relation dropdown options.
- Existing relationships to archived records remain valid and displayable.
- When editing a record that already references an archived target, that archived target remains selectable as the current value so unrelated edits are not destructive.
- Hard deletion is blocked if any incoming relation references the target record, or if any Process Run (any status) originated from it. If unreferenced, deletion removes the record; source-side relation rows cascade through existing constraints/RPC behavior.

Entities:

- `entity_types.archived_at` implements archive/restore.
- Archived entities are hidden from normal navigation/selectors by default.
- Direct entity URLs remain viewable, but archived entities are read-only except restore/safe delete actions.
- Entity rename updates user-facing name/slug while preserving entity ID.
- Hard deletion is allowed only when the entity has zero records, including archived records; no field in the workspace has `related_entity_type_id` equal to that entity ID (self-relations count and block deletion); no workflow's `create_record` action still targets that entity; and no Process Template (active or archived) applies to it. The workflow check has no FK backing it (the entity-target relationship lives inside `actions[]` JSONB, not a column) — it is an explicit application-level `EXISTS` scan inside `delete_entity_type_if_safe`, deliberately blocking rather than cascading, matching the field-deletion dependency pattern below. The process-template check *is* additionally backed by a structural FK (`process_templates.applies_to_entity_type_id ... on delete restrict`), since that relationship lives in a real column, not JSONB.

Fields:

- `field_definitions.archived_at` implements archive/restore.
- Normal metadata reads exclude archived fields by default. Use `includeArchivedFields: true` only for management, workflow repair/execution validation, dependency inspection, and safe preservation paths.
- Archived fields disappear from ordinary record forms, tables, and new workflow configuration.
- Existing workflows and saved views referencing archived fields remain intact but invalid or stale until corrected.
- Hard deletion is safe-only. It is blocked if primitive record JSON contains the field key anywhere, if relation rows exist for the field, if workflow JSON config references the field, if the field is configured as an entity display field, or if saved table-view config references the field.
- Primitive deletion dependency detection treats `values ? field_key` as a dependency even if the stored value is null/empty; no automatic cleanup is performed.

Required field safety:

- Adding a required field to an entity with existing records is rejected.
- Changing optional -> required is handled by RPC validation; every existing record must already have a valid non-null value. Relation fields require a relation row for every record. Violations reject the change with a count.
- Required -> optional is always allowed.

Saved views:

- Every entity has an implicit All Records view.
- Saved views support AND-only filters, typed operators, ordered visible columns, and deterministic sorting with record ID tie-breaks.
- Text contains/not-contains matching is case-insensitive.
- Date values must be valid `YYYY-MM-DD` strings.
- At most one saved view per entity can be the default view.
- Stale column/sort references are surfaced as warnings; stale filter references fail closed.
- Deleting a view never deletes records.
- Workspace Home uses entity base links for primary navigation, preserving configured default-view behavior. Its secondary shortcuts use `?view=all` and `?view=<view-id>` routes.
- Entity pages keep All Records and saved-view switching visible, while saved-view creation, editing, default selection, and deletion live under an explicit native Manage views disclosure. Stale-reference warnings still open and fail closed as before.

Workspace search:

- `GET /search?q=...&type=<entityTypeId>` performs a case-insensitive trimmed substring search across active text fields on active records in active entities, filtering, ranking, and capping entirely in Postgres (Phase 8D.4, `search_workspace_records_authorized`) rather than fetching every active record into Node — see Phase 8D.4 under Testing below.
- Results are grouped by entity, capped at 20 records per entity (enforced in SQL), and link to record detail pages. An optional object-type filter restricts matching to one entity type; the cap is unchanged either way.
- For each entity, display-field matches rank before other text-field matches; prefix matches rank before general substring matches; remaining ties use the identity field's raw value then record ID.
- A match outside the display field can show concise matching-field context. Search is deliberately limited to records: no fuzzy matching, cursor pagination, workflow/view/settings search, or a maintained search index/projection in v1 (plain `LIKE` was benchmarked to 10,000 records and found fast enough — see Phase 8D.4).

Record details and reverse relationships:

- Stable detail route: `/entities/[entityTypeId]/records/[recordId]`.
- Detail pages show active fields in schema order, human-readable primitive formatting, outgoing relation links, lifecycle actions, and derived incoming relationships.
- Detail pages are identity-led: Edit is the primary action, while archive/restore/delete remain available through a secondary native Record actions disclosure. Details and Related Records retain their generic metadata-driven behavior.
- Archived records remain directly viewable, show archived state, and can be restored or safely deleted.
- Incoming/reverse relationships are derived from existing relation rows and metadata. They are not stored separately.
- Reverse relationship groups exclude archived source records, archived relation fields, and archived source entities.
- Active incoming relation fields produce a reverse group even when it has no records, so users can create the first related record.
- A reverse group can open the source entity's normal create form with its specific relation field prefilled to the current active record. The prefilled relation remains editable; successful creation and Cancel return to the originating detail page through validated origin entity/record IDs, never an arbitrary return URL.

Faster Record Work — inline primitive-field editing:

- The entity records table (`entity-records-table.tsx`) supports inline, click-to-edit editing of `text`, `number`, `date`, and `boolean` fields directly from the table, via a new `updateRecordField` server action in `app/actions.ts` and a `EditableTableCell`/`EditableCellForm` client component pair in `app/components/editable-table-cell.tsx`.
- Relation fields and the identity/display field remain read-only inline in v1; they keep their existing badge/link rendering. Archived records and archived entities disable inline editing entirely (same `recordEditPathBase` gate the Edit link already uses).
- Interaction model is consistent across all four supported types: an explicit click/button opens edit mode; Enter or an explicit Save control commits; Escape cancels and returns focus to the triggering cell control; blur outside the cell's own form also cancels — it never silently saves. Boolean fields use the same explicit-commit model (toggle the checkbox, then Enter/Save), not autosave-on-click.
- After a successful save, the page calls `router.refresh()` to re-render from the server; there is no optimistic client-side record state in v1.
- Because `update_entity_record_with_relations` replaces the entire primitive `values` object rather than patching one key, `updateRecordField` always merges the edited value into the full current active-field value snapshot before validating and writing — it reuses `validateRecordFormData` and `updateEntityRecordInRepository` unchanged, so required-field validation is identical to the full edit form.
- A no-op inline edit (submitted value equal to the persisted value, via the same `valuesAreEqual` equality used for watched-field change detection) short-circuits before the repository write and before workflow execution — no DB write, no `record_updated` workflow event.
- A real inline edit follows the exact same previous-snapshot/write/reload-next-snapshot/`getChangedFieldDefinitionIds`/`executeRecordUpdatedWorkflows` sequence as `updateRecord`, so `changed`/`changed_from`/`changed_to`/`changed_from_to` and watched-field semantics behave identically whether the edit came from the full Edit form or an inline cell.
- If validation fails on the edited field, the cell stays in edit mode with an inline field-level error. If a save is blocked by a different (typically hidden, view-filtered-out) field on the record, the cell shows a concise fallback ("This record needs additional changes. Open full edit.") linking to the full Edit route, rather than reproducing full-record validation inside the table.
- No migration, repository, RLS, or domain-model change was required — the feature is additive at the server-action and UI layer only.

## Workflow System

Workflow domain types are in `lib/domain/workflow-types.ts`; validation is in `lib/domain/workflow-validation.ts`; execution is in `lib/domain/workflow-engine.ts`; persistence is in `lib/domain/workflow-repository.ts`.

Implemented trigger types:

- `record_created`
- `record_updated`

Implemented action types (per step; see "Multiple ordered actions" below):

- `create_record`
- `update_record`
- `update_related_record`
- `start_process`

Workflows are stored in `workflows` with `trigger_type`, `trigger_entity_type_id`, an ordered JSONB `actions` array (at least one element, enforced by a `jsonb_array_length >= 1` CHECK constraint), and a narrowed JSONB `action_config` holding only workflow-level `triggerConfig`/`conditions`. Each element of `actions` is one step: `{ actionType, actionTargetEntityTypeId?, relatedFieldDefinitionId?, processTemplateId?, fieldMappings }`. `create_record` requires `actionTargetEntityTypeId`; `update_record` targets the triggering record; `update_related_record` targets the current record reached through that step's own `relatedFieldDefinitionId`; `start_process` requires only an active compatible `processTemplateId` and always uses the original triggering record as its ProcessRun origin. The legacy top-level `action_type`/`action_target_entity_type_id` columns no longer exist (see Database And Migrations).

Conditions:

- Stored in `conditions`, at the workflow level — not per action.
- Empty conditions means always run.
- Conditions are AND-only.
- Supported operators include equality/inequality, numeric comparisons, date before/after, `is_set`/`is_not_set`, and (record_updated only) `changed`, `changed_from`, `changed_to`, `changed_from_to`.
- At execution time, all condition configuration is fully validated before evaluation. Broken config logs `failed`; non-matching valid conditions log `skipped` with “Workflow conditions did not match.”
- Conditions evaluate against the triggering record snapshot for that workflow event, exactly once, before any action runs. No action — including one that modifies the field a condition matched on — causes conditions to be re-evaluated.

Record-updated watched fields:

- Stored in `triggerConfig.watchedFieldDefinitionIds`, at the workflow level — not per action.
- A `record_updated` workflow must watch at least one field.
- ANY watched field changing qualifies.
- Invalid or archived watched fields log `failed`.
- If watched fields did not change, execution logs `skipped` with “Watched fields did not change.”

Transition-aware conditions (`changed`, `changed_from`, `changed_to`, `changed_from_to`):

- `record_updated` only; `record_created` workflows never expose or accept them (rejected server-side even if crafted directly, not just hidden in the editor).
- Compare the field's previous persisted value against its current persisted value from the same original user-edit event that `app/actions.ts`'s `updateRecord` already loads to compute watched-field changes. No new persistence: previous values are never stored in `action_config` or execution logs, only carried in memory for that one execution.
- `changed` needs no operand. `changed_to`/`changed_from` need one operand (`value`/`previousValue` respectively). `changed_from_to` needs both, and they must differ.
- Every transition operator requires the field to have genuinely transitioned (`previous !== current`); `changed_to`/`changed_from_to` do not fire merely because the field already held the target value.
- `0`, `false`, and `""` are real, distinct values, never treated as unset; only null/undefined collapse together (same equality rule already used for watched-field change detection, reused directly).
- Relation operands compare record IDs; the editor shows human-readable labels via the same active-record dropdown used for relation equality conditions.
- **Watched-field invariant:** a transition operator's `sourceFieldDefinitionId` must also be in `triggerConfig.watchedFieldDefinitionIds`, or the workflow cannot be saved (“`<field>` must be a watched field to use a changed condition on it.”). This is enforced in the one shared `validateWorkflowConditions` function used at both save time and execution time, so execution never relies on editor behavior alone. The editor auto-checks a field as watched when a transition operator is selected for it, but does not prevent unchecking a still-referenced watched field afterward — that combination fails save validation with a clear inline message instead.
- v1 limitation: from/to operands must be concrete typed values; there is no “unset” sentinel. A transition from/to an unset value cannot be expressed directly. Combining `changed` with `is_set`/`is_not_set` approximates it but is not equivalent (it does not guarantee the *previous* value was specifically unset).

Field mappings (per action step):

- `unset` is available for optional target fields in `create_record`.
- `leave_unchanged` and `clear` are available for `update_record` and `update_related_record`.
- `constant` stores a type-appropriate literal.
- `source_field` copies a direct field from the trigger record.
- `template` is available for text targets only.

Compatibility is strict: text -> text, number -> number, date -> date, boolean -> boolean, and relation -> relation only when both fields target the same related entity. No implicit coercion, no relation traversal, no dynamic lookups.

Templates:

- Persist canonical placeholders as `{{field:<field-definition-id>}}`.
- The UI shows human-readable placeholders using disambiguated entity-qualified labels so duplicate field names can round-trip without exposing UUIDs.
- Execution formatting is deterministic: text plain string, number `String(value)`, date stored `YYYY-MM-DD`, boolean `Yes`/`No`, relation display label, missing optional value empty string.

Relation handling in workflows:

- Constant relation mappings can select active records only.
- Source relation values may come from existing archived relationships, but creating a new relation to an archived target is rejected.
- Direct relation placeholders render the related record display label.

Execution semantics:

- Enabled workflows execute in deterministic order: `created_at ASC, id ASC`.
- Each matching workflow executes independently. Failure of one workflow does not prevent later workflows from running, and the original triggering record remains created/updated.
- Workflow-generated record creation or updates do not recursively trigger workflows.
- Eligibility/watch-field detection/conditions are based on the original persisted triggering event, and are evaluated exactly once, before the first action runs — never per action, never re-checked after an action executes.
- `update_record` and `create_record` actions both reload the latest authoritative triggering record before resolving mappings/templates, so a later action sees an earlier action's committed effects rather than the original triggering snapshot. (`create_record`'s reload was added in this milestone — with only ever one action per workflow previously, the original triggering snapshot and "latest" were always identical, so the gap was invisible until multiple actions could run in sequence.)
- `update_record` modifies only explicitly configured fields, preserving unrelated active and archived primitive values and unrelated relation rows.
- A valid no-op `update_record` logs `succeeded` with result message “No changes required.” and does not perform an unnecessary DB update or bump `entity_records.updated_at`.
- `update_related_record` follows exactly one active direct relation field on the latest triggering record, then reloads and updates that one related record using the same mapping, compatibility, validation, atomic RPC, no-op, and no-recursion rules as `update_record`.
- `update_related_record` configurations require at least one mapping other than `leave_unchanged`. Missing relations, archived selected relation fields, archived target entities/records, and archived mapped fields fail execution. If a related target is resolved before a later failure, its entity/record IDs are retained in the execution log.
- `start_process` is available on both record-created and record-updated workflows. It has no mappings, target selector, or relation traversal: it calls the existing `startProcessRun` repository operation with the active workspace, configured template, triggering entity, and original triggering record. The canonical membership-checked RPC therefore remains the sole owner of template/origin validation, graph traversal, snapshots, assignee/due-rule handling, advisory locking, and the one-active-run rule. An active duplicate run fails the action rather than silently no-oping.

Multiple ordered actions:

- A workflow's `actions` array is executed strictly in configured order, sequentially (`for...of`, awaited) — no parallelism, no branching, no delays/scheduling.
- The first action to fail stops the remaining actions in that workflow run. Actions that already completed keep their committed writes — there is no cross-action rollback of the workflow run, and the original triggering event is never rolled back.
- A later workflow (in the deterministic `created_at ASC, id ASC` order) still runs even if an earlier workflow's action sequence failed partway through.
- Workflow-driven writes still bypass the normal user-edit action layer entirely (they call the record repository directly), so they still cannot recursively trigger workflows — true for every action in the sequence, not just the first.
- The editor (`app/components/workflow-create-form.tsx`) supports Add Action, Remove Action (minimum one action enforced both client-side and server-side), and Move Up/Move Down for reordering; there is no drag-and-drop. Per-action form field names are namespaced by a client-generated action id (`mappingType:<actionId>:<targetFieldId>`, etc.) so two actions can target the same field without colliding.
- Submitted action ids must be unique; a duplicate is rejected server-side with a clear `_form` validation error before any per-action parsing that keys off action id, so a duplicate can never silently collapse two submitted actions into one persisted action.

Execution logs:

- Stored in `workflow_execution_logs`.
- Status is `succeeded`, `failed`, or `skipped` — this is the whole-workflow-run status, not a per-action status.
- Logs include trigger entity/record, timestamps, error/result messages, legacy `created_record_id`/`action_entity_type_id`/`action_record_id`, and a structured `action_results` JSONB array.
- `action_results` holds one ordered entry per action that actually began execution (never an entry for an action skipped because an earlier one failed first), each with `index`, `actionType`, `status` (`succeeded`/`failed`), and — where applicable — `actionEntityTypeId`, `actionRecordId`, `createdRecordId`, `processTemplateId`, `processRunId`, `originEntityTypeId`, `originRecordId`, `resultMessage`, `errorMessage`. Process-specific fields preserve traceability without overloading the legacy record-action fields.
- Legacy singular fields (`createdRecordId`/`actionEntityTypeId`/`actionRecordId`) keep their original single-action meaning for a workflow with exactly one action. For a workflow with more than one action they are `null` on a fully successful run (no single action to point at without being misleading) and describe the *failed* action on a failed run (a genuinely resolved, non-arbitrary target) — `action_results` is authoritative in both cases, especially for multi-action runs.
- The top-level `errorMessage` for a multi-action failure is prefixed with which action failed, e.g. `"Action 2 (update_related_record) failed: ..."`; a single-action workflow's error message is unprefixed, exactly as before this milestone.
- `resultMessage` for a multi-action success summarizes each action's outcome (`"Action 1: ... Action 2: ..."`); a single-action workflow's result message is unprefixed, exactly as before (e.g. still exactly `"No changes required."` for a single-action no-op).
- The workflow execution log UI (`app/workflows/page.tsx`) renders the `action_results` breakdown beneath the existing summary line only when there is more than one entry, so single-action and legacy logs render exactly as they did before this milestone.
- Action entity/record log fields intentionally do not have FKs so audit/history semantics are not undermined by future hard deletes.
- Deleting a workflow currently cascades its execution logs. This is acceptable for the prototype; audit preservation may change later.

## Process System

The first foundation for a reusable human-process orchestration engine, deliberately distinct from the other three subsystems: Entities are business data, Views are ways of seeing records, Workflows are event-driven automation ("when X happens, do Y"), Processes are repeatable sequences of human/operational work ("to accomplish X, these steps need to happen"). Domain types are in `lib/domain/process-types.ts`; persistence is in `lib/domain/process-repository.ts`; form-shape validation is in `lib/domain/process-validation.ts`; server actions are in `app/process-actions.ts` (a separate file from `app/actions.ts`, kept out of that already-large file).

Persistence model — edge-driven and graph-capable, supporting linear paths, approvals, timer waits, condition/event waits, conditional branching, and structured parallel regions:

- `ProcessTemplate` (`process_templates`): `name`, optional `description`, immutable `appliesToEntityTypeId` (set once at creation, never editable), `archivedAt`-only lifecycle (matching `EntityType`/`FieldDefinition`, not the `workflows.enabled` boolean pattern — a template creates durable historical dependents the way records do, unlike workflows).
- `ProcessNode` (`process_nodes`): one step definition, `nodeType` (`'human_task'`, `'approval'`, `'wait'`, `'condition_wait'`, `'parallel_split'`, or `'parallel_join'`), `name`, and a JSONB configuration source. Human tasks and approvals may carry assignment/due configuration; a timer wait has a snapshotted `waitRule`; a condition wait has a snapshotted `conditionWaitRule`; neither has an assignee or due rule. Parallel system nodes carry a `parallelGroupId`, no assignee, and no due rule. Node IDs are stable across ordinary template edits.
- `ProcessEdge` (`process_edges`): a real ordered adjacency table (`sourceNodeId` -> `targetNodeId`), not a position column on nodes. It records default/conditional routing, parallel branches, or a stable approval outcome UUID plus label. Approval outcome labels are nonblank and unique per approval after trim/case-folding; distinct outcomes may intentionally target the same step. Canonical validation enforces the supported graph shape rather than attempting to repair malformed direct data.
- `ProcessRun` (`process_runs`): one started instance, `status` (`active`/`completed`), `originEntityTypeId`/`originRecordId`, and a **snapshot** of `processTemplateName`/`processTemplateDescription` taken at start time — a later template rename never rewrites a run's historical display. `processTemplateId` is retained for traceability/dependency checks only.
- `ProcessStepRun` (`process_step_runs`): one instantiated step, fully snapshotting `nodeType`/`name`/`config` from its node at start time, plus persisted optional `dueAt` and `resumeAt`, `status` (`pending`/`active`/`completed`/`skipped`), `parallelGroupId`, optional branch token, and a stable `stepIndex`. A completed approval additionally snapshots its selected outcome ID/label and decision time/actor ID/actor email separately from its routing history. `sourceNodeId` is a soft reference (no FK), matching the existing `workflow_execution_logs` precedent of not FK-constraining audit fields — a step run's correctness never depends on its originating node still existing.

Template editing preserves node identity: `save_process_template_authorized` updates existing `process_nodes` rows in place for steps resubmitted by ID (rename/reorder/config edits never change the ID), inserts new rows for null-ID steps, deletes rows omitted from the submission, and always fully rebuilds `process_edges` from the final submitted order (edges have no independent identity or history value to preserve). Submitted existing node IDs are validated server-side to actually belong to the same workspace/template.

Process-run lifecycle:

- The canonical start operation (`start_process_run_authorized`) is shared by manual record-detail starts and workflow `start_process` actions. It validates the template/origin, serializes on a per-origin-record advisory lock, rejects a second concurrent active run, creates all StepRuns, and snapshots every template edge into immutable `process_step_run_routes` rows between those StepRuns. It deterministically selects the sole validated start node; `0032` corrects a PostgreSQL `min(uuid)` defect in the already-applied `0031` function, while `0031` itself is corrected for clean bootstrap.
- A template + origin record combination may have unlimited *historical* runs, but at most one *active* run at a time — once a run completes, starting another is allowed and record detail offers "Start another run."
- Generic completion (`complete_process_step_run_authorized`) accepts active `human_task` StepRuns only. An approval is decided only by `decide_process_approval_authorized`: an assigned approval requires its snapshot assignee; an unassigned approval is decidable by any same-workspace member. A timer `wait` is never manually completable: it becomes active with one persisted UTC `resumeAt`, then the trusted scheduler resumes it through the narrow service-only batch RPC. A `condition_wait` is also never manually completable: it snapshots its condition configuration at run start, evaluates live current origin or direct-related record values, and advances only when the condition becomes true through the service-only wakeup dispatcher. An approval selects exactly one edge from the run-scoped outcome snapshot, records a separate decision snapshot plus an `approval_outcome` routing result, and leaves the approval active on failure. Ordinary/conditional human-task and wait sources select one route: ordered AND-only conditions evaluate against current origin-record values, then the first match or exactly one default wins. A parallel split instead creates one immutable join obligation per branch token and activates every selected branch. A join auto-completes only after every expected token arrives, then activates its downstream route exactly once. Conditional, approval, and wait routing inside one branch are token-local, so skipped alternatives never block sibling work or the matching join. Routing updates are serialized per process run with an advisory lock; evaluation errors are atomic, leaving the current step active with no partial routing write.
- Process runs never write to `entity_records`; workflow-triggered starts call the process RPC directly and do not recursively trigger workflows.

UI: `/processes` (list), `/processes/new`, `/processes/[processTemplateId]/edit` (the step editor — Add approval/Move Up/Move Down/Remove, no drag-and-drop, no canvas) live under the "Workspace setup" navigation disclosure next to Workflows. `/process-runs/[processRunId]` is a flat top-level route (not nested under the origin entity) showing the ordered step list: active human tasks expose Complete, while active approvals expose their named decision actions and completed approvals show their recorded decision. Record detail shows a concise "Processes" section per applicable template: not-started shows Start process; an active run shows progress/current step and Open process (no duplicate Start); a completed-with-no-active-run state shows the last completion and Start another run. **Starting a process redirects immediately to its `/process-runs/[id]` page** rather than staying on record detail — worth knowing when writing E2E flows that need to return to record detail afterward.

Security model: all nine process tables, including immutable `process_step_run_routes`, `process_parallel_join_obligations`, `process_condition_wait_dependencies`, and `process_condition_wait_wakeups`, are RLS-enabled and grant **select-only** to `authenticated` — there is no raw authenticated insert/update/delete path on any of them, unlike the older entity/workflow tables' incrementally-tightened grants. Every ordinary write goes through a membership-checked, fixed-search-path `SECURITY DEFINER` `_authorized` RPC from the start (`save_process_template_authorized`, `archive_/restore_process_template_authorized`, `delete_process_template_if_safe_authorized`, `start_process_run_authorized`, `complete_process_step_run_authorized`, `decide_process_approval_authorized`), and `workspace_id` is immutable on all nine tables via the same `private.reject_workspace_id_change()` trigger every other workspace-scoped table uses. `resume_due_process_waits_system` and `dispatch_process_condition_wait_wakeups_system` are the only trusted scheduler/dispatcher exceptions: both are fixed-search-path, service-role-only, and have PUBLIC, anon, and authenticated EXECUTE revoked. The protected internal `POST /api/internal/process-waits` route checks `PROCESS_WAIT_SCHEDULER_SECRET` and invokes only these narrow batch RPCs; it accepts no workspace/run authority from callers. Approval decision execution is granted only to `authenticated`/`service_role`; both PUBLIC and explicit `anon` execution are revoked.

Safe-delete integration: `delete_entity_type_if_safe`/`delete_entity_record_if_unreferenced` (and their `_authorized` wrappers) were extended with a `process_template_count`/`process_run_count` column respectively (required a drop+recreate, matching how migration 0020 handled the same return-shape change) — an entity type cannot be hard-deleted while any process template (active or archived) applies to it, and a record cannot be hard-deleted while any process run (any status) originates from it. `delete_process_template_if_safe_authorized` also blocks hard deletion while either ProcessRun history or any workflow `start_process` action references the template, returning both dependency counts for friendly messaging. Archiving remains allowed and does not rewrite workflow config; an archived referenced template simply fails execution until restored. Process templates/runs/step-runs are never cascade-deleted when their business object is deleted — deletion is blocked, not cascaded, same philosophy as the relation-field and workflow-target checks.

Fixed step assignment + My Work (migration `0028_process_step_assignment.sql`):

- Each `human_task` or `approval` `ProcessNode` may have `assigneeUserId` — none, or exactly one current workspace member. `process_nodes` carries a composite FK `(workspace_id, assignee_user_id) -> workspace_memberships(workspace_id, user_id)`, no cascade. This one constraint structurally guarantees an assignee is always a member of the *same* workspace, and blocks removing that membership (or deleting the underlying `auth.users` row, which cascades into `workspace_memberships`) while any template node — including nodes on an archived template — still assigns them. There is still no membership-removal product feature; this hardens whatever removal path eventually ships.
- `process_step_runs` carries a parallel `assigneeUserId` (soft, no FK — matching the existing `sourceNodeId` precedent) plus a snapshotted `assigneeLabel` (the assignee's email at the moment the run started). Historical step runs render entirely from this snapshot; they never require the membership row to still exist, and they never block membership removal on their own — only a live `process_nodes` reference does.
- Human-readable identity is deliberately minimal: a single new RPC, `list_workspace_member_identities_authorized(workspace_id)`, membership-checked, `security definer`, `set search_path = ''`, with every reference to `auth.users`/`public.workspace_memberships` fully schema-qualified. It returns only `{user_id, email}` for the requesting member's own workspace — no other `auth.users` metadata, no profile table, no denormalized column on `workspace_memberships`. Assignment always resolves the *current* email live (template editor, run detail for an in-progress step); `process_step_runs.assigneeLabel` is the one place email is ever persisted, and only as a point-in-time snapshot.
- `save_process_template_authorized` validates a submitted `assignee_user_id` is a current workspace member before writing (friendly error; the composite FK is the structural backstop) and updates it in place for resubmitted steps — same identity-preserving semantics as name.
- `start_process_run_authorized` copies each node's `assignee_user_id` and resolves its current email into `assignee_label` at that exact moment — a later template reassignment never rewrites an already-started run's step.
- `complete_process_step_run_authorized` is now assignee-aware: an assigned active step may only be completed by `auth.uid() = assignee_user_id`; an unassigned active step remains open to any authenticated same-workspace member. This is the sole enforcement point — `ProcessRunDetailView` additionally hides the Complete action for non-assignees, but only as a UX nicety on top of the RPC check, never instead of it.
- "My Work" (`/my-work`, primary nav, not under Workspace setup) is a read-only convenience filter, not a new visibility boundary — any workspace member could already see any process run via `/process-runs/[id]` regardless of assignment. `listMyWorkItems` in `process-repository.ts` derives the current user exclusively from `getCurrentUser()` server-side (never a client-submitted ID), and splits into "Ready now" (`status = 'active'`) and "Upcoming" (`status = 'pending'`) sections, both scoped to `assignee_user_id = <current user>` and joined to only `process_runs.status = 'active'` — a completed run's steps disappear from My Work entirely. The origin record's label is resolved live (not snapshotted — unlike the run/template name, the origin record is a live business object, not history).

Process Due Dates + Attention (migration `0029_process_step_due_dates.sql`):

- A human-task or approval node may configure no rule or `{ amount: 1..8760, unit: 'hours' | 'days' }` in `config.due_rule`; one day always means exactly 24 elapsed hours. The template editor exposes this as optional `Due [amount] [Hours|Days] after activation`. TypeScript and the canonical `save_process_template_authorized` RPC reject zero, negatives, fractions, unsupported units, malformed config, and values above 8760; failed saves are transactional.
- Every StepRun snapshots this configuration at run start. Only active steps receive a persisted `due_at`: start calculates the first deadline from one activation timestamp, and completion atomically calculates the next step's deadline from that pending StepRun's snapshot. Pending steps intentionally retain `due_at = null`; live `process_nodes` timing configuration is never read during an already-started run.
- `overdue` is strictly derived, never stored: an assigned StepRun is operationally overdue only while `status = 'active'`, `due_at is not null`, and `due_at < now()`. Completion keeps `started_at`, `due_at`, and `completed_at` history but removes the step from operational overdue work.
- My Work now groups the current user's active-run assignments as Overdue, Ready now, and Upcoming. Active due-dated work sorts by `due_at` ascending, then undated active work by stable run/step order; pending work has no fabricated deadline and retains deterministic run/step ordering.
- Process Run and record-detail process summaries show compact due context only where present. Timestamps are stored as UTC `timestamptz` and rendered as browser-local absolute date/times after hydration; there is deliberately no user/workspace timezone setting or relative-calendar wording in v1.
- No scheduler, queue, email, or notification subsystem exists. This milestone intentionally sends no reminders; a later delivery increment needs scheduled execution, a delivery channel, idempotency/delivery history, retry policy, and timezone policy.

Process Graph 5C.2 — Wait States (migration `0037_process_wait_states.sql`):

- A `wait` is an active system StepRun with no assignee or due rule. It is never shown in My Work and generic completion rejects it. Pending and skipped waits keep `resume_at = null`; activation calculates and persists it once; completed waits retain `started_at`, `resume_at`, and `completed_at` history.
- Supported snapshotted rules are elapsed hours, calendar days in an explicit IANA timezone, Monday-Friday weekday durations (activation date does not count), and calendar targets: first through twentieth weekday of next month, first selected day of week next month, or one specific zoned date/time. Hours are pure UTC elapsed time; calendar rules preserve local wall-clock time and persist the resulting instant as UTC. Holidays/business calendars are deliberately deferred.
- The scheduler is provider-neutral. A deployment cron invokes the secret-protected internal route, which uses the admin client only to call service-role-only `resume_due_process_waits_system`. The batch scans active due waits in deterministic bounded order, uses `FOR UPDATE SKIP LOCKED` plus the existing per-ProcessRun advisory lock, rechecks state under lock, and treats already-resolved waits as harmless no-ops. A failing wait rolls back its own advancement and remains durable/retryable without corrupting other due waits.
- Wait completion uses immutable run-scoped routes and current authoritative origin values for any outgoing conditions. It preserves a parallel branch token, records exactly one join arrival, and activates downstream human/approval work with their existing assignment/due semantics. Manual and workflow-triggered starts snapshot waits identically; live template edits do not change an activated wait's `resume_at` or an existing run's route/config snapshot.

Process Graph 6A — Visual Process Builder Foundation (no migration; UI/state-layer only):

- `app/components/process-template-form.tsx` now offers a List/Graph toggle inside the existing template editor route (`/processes/new`, `/processes/[processTemplateId]/edit`) rather than a separate page. Both views render from one shared client-side `steps` state; there is no second data model, no separate save path, and no persisted layout — switching views never loses in-progress edits.
- The per-node-type editing fields (wait configuration, condition-wait configuration, assignee/due, approval outcomes, conditional routing) were extracted from the List view into shared components in `app/components/process-node-editor.tsx` (`ProcessNodeEditor` and its sub-fields), so List rows and the Graph side panel render and mutate state through identical controls — there is exactly one implementation of each editing control, not two that could drift.
- Graph view (`app/components/process-graph-view.tsx`) renders every node (`human_task`, `approval`, `wait`, `condition_wait`, `parallel_split`, `parallel_join`) as a distinctly labeled card and every route as an SVG edge, labeled with its condition summary, approval outcome, or "Otherwise" for the default route. Conditional branching is edge/route behavior, not a node type — there is no separate "branch" node. Selecting a node (click, or Enter/Space when focused, each card is a real `<button>`) opens a side panel with that node's full `ProcessNodeEditor`; Escape deselects. Reordering remains List-only in 6A — the Graph panel does not offer Move Up/Down.
- Layout (`app/components/process-graph-layout.ts`) is computed fresh on every render, never persisted: each step's rank is its position in the already-topologically-ordered `steps` array (routes only ever target a later step), and lanes are single-column except inside a parallel split/join region, where each branch gets its own lane via a bounded walk from the split to its matching join. This was sufficient for both reconvergent conditional branches (which need no special handling — multiple edges into one node render correctly with plain rank ordering) and parallel fan-out/rejoin; a graph layout library was deliberately not added.
- Known cosmetic limitation, deferred as future polish: an edge whose target is not the immediately-next step (for example one approval outcome among several skipping over another outcome's target) can render its line/label passing behind an intervening node card. This does not affect data correctness or the panel's editing behavior.
- Fixed alongside 6A verification (pre-existing, unrelated to the graph work itself): `app/processes/[processTemplateId]/edit/page.tsx` never reconstructed a `condition_wait` node's saved `conditionWaitRule` (target kind, relation, entity type, conditions) into the edit form's initial state, so reopening an existing condition-wait step for editing silently showed it with zero conditions. Both List and Graph read from the same corrected initial state, so both now load a saved condition wait's target, conditions, operators, and values correctly.
- Deferred beyond 6A: freeform node positioning/drag-to-move, drag-to-connect/rewire edges, layout persistence, process-*run* (instance) graph visualization, and any visual polish beyond this usable foundation.

Process Graph 6B — Direct Graph Editing (no migration; UI/state-layer only):

- The Graph view (6A) gained direct-manipulation editing while keeping the same governing rule: every gesture is sugar over the existing `steps`/`routes` array model, never a second state shape, and array order remains the sole topological-ordering authority.
- **Insert-on-edge**: a "+" control on every rendered edge opens a type picker (human task / approval / wait / condition wait — not split/join, which still only comes from "+ Add parallel paths" since a region has structural invariants a single splice can't express) and splices a new node into that edge: the clicked route's target is repointed to the new node, and the new node gets a fresh route to the edge's old target. This is uniform across plain, conditional, approval-outcome, and parallel-branch edges — only the clicked route's target pointer changes.
- **Delete**: a delete affix on each node card (and the panel's Remove button) triggers an explicit choice, not a silent rewire, whenever the node is unambiguous (exactly one inbound route from anywhere, and its own routes are exactly one plain default route): "Delete and reconnect '{from}' to '{to}'" (primary) versus "Delete without reconnecting." Ambiguous nodes (approvals, split/join, anything with zero or multiple in/out routes) delete immediately with no invented rewire, identical to the List view's existing Remove behavior.
- **Reorder**: Up/Down buttons on each node card call the same adjacent-swap `moveStep` the List view already used, now guarded on both views by `canSwapAdjacent`: a swap is disabled whenever the step at the lower position has a route directly targeting the step at the higher position (proven correct by construction — no other route can be broken by an adjacent swap), with the reason surfaced via `title`. The RPC remains the authoritative backstop; this is UX safety layered on top, not a new validation source. True pixel/pointer drag was deliberately not built — button-based reorder gets most of the "feels visual" value with far less engineering/testing surface, and it doubles as the mandatory keyboard-accessible path a future drag implementation would need anyway.
- **Edge-routing readability**: fixed the known non-adjacent occlusion issue with a bounded, deterministic technique — an edge whose target isn't the very next rank in the same lane now jogs through a fixed side gutter column (clear of every node card by construction) instead of drawing straight through occupied space. This changes only edge *path* computation in `process-graph-layout.ts`; rank/lane assignment is unchanged. Concurrent gutter edges sharing the same column can still overlap each other — a known, deliberately deferred cosmetic gap, not attempted further to avoid the layout work expanding into general obstacle-avoidance infrastructure.
- **Route navigation**: clicking an edge's label selects its source node and highlights/scrolls to that specific route inside the shared panel (`RoutesEditor`/`ApprovalOutcomesEditor` via a new `highlightRouteId` prop), so "edit this route" is one click instead of hunting through a node's routes.
- Explicitly deferred, per the approved plan: freeform x/y drag positioning, drag-to-wire edge creation, moving a node across branches or into/out of a parallel region (still only via delete+recreate), general crossing-minimization layout, and undo/redo.
- Fixed alongside verification (pre-existing, unrelated to the direct-editing work itself): `lib/domain/process-validation.ts`'s wait-amount check used `/^[1-9]\\d*$/` — a regex *literal* containing an escaped literal backslash+"d", not a digit class — so it rejected every wait amount unconditionally regardless of value. Corrected to `/^[1-9]\d*$/`. Two further pre-existing, unrelated bugs were found and deliberately left unfixed (flagged instead, matching this project's established practice for incidental discoveries): (1) typing/clearing/any-interaction with the rendered Wait Amount input crashes the page client-side (`Cannot read properties of null (reading 'value')`), reproduced identically against the pre-6A component, so it predates both 6A and 6B; (2) a "duration" wait snapshotted with unit `hours` always carries a timezone in its saved config (the form parser unconditionally re-defaults an empty `waitTimeZone` back to `"America/Toronto"`), which the RPC then correctly rejects with "Elapsed-hour waits cannot specify a timezone" — meaning saving *any* hours-duration wait has never actually worked via the UI, independent of the amount-regex bug. `calendar_days`-unit waits are unaffected (they legitimately carry a timezone) and were used to route around this in the new regression test.

Wait UI Bug Fixes (post-6B; a small bug-fix pass, not a milestone; no migration):

- Fixed the client-side crash: every field in `WaitConfigFields` read `event.currentTarget.value` *inside* the deferred `updateStep` state-updater callback, not synchronously in the `onChange` handler. React Strict Mode double-invokes that updater, and by the second invocation the SyntheticEvent's `currentTarget` has already been nulled out (a documented React constraint — `currentTarget` is only valid for the synchronous duration of the handler). Fixed by capturing each field's value into a local `const` synchronously in `onChange` and referencing that captured value inside the updater instead of the event. Root-caused with a direct diagnostic (not by trial and error): logging inside the actual handler showed the updater running twice with `event.currentTarget === null` both times.
- Fixed the timezone leak: `app/process-actions.ts`'s `getWaitRule` included `timeZone` in a "duration" wait's payload whenever the local form state's `waitTimeZone` was non-empty — but `waitTimeZone` defaults to `"America/Toronto"` and is never cleared when switching to "hours" (the IANA-timezone field is simply hidden for that mode, not reset), so every hours-duration wait silently carried a stale timezone the RPC then rejected ("Elapsed-hour waits cannot specify a timezone"). Fixed by gating inclusion on `unit === "calendar_days"` directly, not on truthiness. `calendar_days`/`weekdays`/`calendar_target` behavior is unchanged.
- Both fixes were verified by reverting each independently (via `git stash`) and confirming the new regression test genuinely fails against the pre-fix code, then passes again restored — not just written and assumed correct.
- The regression test uses real UI interaction throughout (typing into the Amount field, clearing it, selecting the Unit dropdown) rather than writing into the hidden `processSteps` payload, which is how this was worked around before the crash itself was fixed. One deliberate detail: the invalid-amount case clears the field to empty rather than typing "0" — the Amount input's own `min="1" step="1"` HTML5 constraints block a literal "0" or "1.5" at the browser level before the form ever reaches the server action, so those values would test native browser behavior rather than the regex fix; an empty value has no `required` attribute to trip and reaches the server, correctly exercising the actual fix.

Process Graph 6C — Visual Process Run / Execution View (no migration; UI/state-layer only):

- The Process Run detail page (`app/process-runs/[processRunId]/page.tsx` → `ProcessRunDetailView`) gained the same List/Graph toggle pattern as the template editor (6A), **List remains the default view**. Graph is purely additional — a second lens over the exact same already-fetched `ProcessRunWithSteps` data (`steps`, `routes`, `joinObligations`), never a second query and never new runtime semantics: it renders what the run's authoritative snapshot already says happened, nothing more.
- **Reuse, not a parallel implementation**: `computeProcessGraphLayout` (6A/6B) was type-narrowed to a minimal structural interface (`{key, nodeType, parallelGroupId, routes}`) it actually needs — a purely mechanical change verified against `process-graph-builder.spec.ts` before anything else was built, so the template editor's `LocalStep[]` still satisfies it with zero changes. `app/components/process-run-graph-adapter.ts` maps a run's snapshotted steps (sorted by `stepIndex`, which already gives the same "array order = topological rank" property `LocalStep[]` relies on) and routes (already keyed by step-run id, already fixed at run-start time) into that same shape — this is *why* a template edit after a run starts can never affect the run's visualization: the run's own data was already immutable before 6C existed, 6C just had to not accidentally join against the live template anywhere.
- `app/components/process-run-graph-summaries.ts` extracts `statusBadgeClass`, `waitRuleLabel`, `stepSummaryLine`, `routingResultLabel`, and a new `isActionableForViewer` out of the List view's previously-inline logic, so List and Graph share one implementation of every status/summary/decision-history string — verified to change no List-rendered text via the full run/approval/parallel/wait/condition-wait/conditional-branching regression suite.
- `app/components/process-run-graph-view.tsx` is the read-only run graph (no editing, no route changes, no manual state manipulation — explicitly out of scope and not built). Node cards show status as both border color and visible text (Sage/completed, Brass-deep/active, neutral/pending, and a distinct reduced-opacity Chalk treatment with explicit "Skipped" text for skipped) — never color-only. Edges are three-way and deliberately not dash-dependent for the taken/untaken distinction (dashing already carries different meaning in the template graph): a route is undetermined (dashed, neutral) until its source step resolves, then taken routes render solid graphite at full opacity/2.5px and untaken siblings render the same solid stroke at 0.35 opacity — color+opacity+width carries the signal, confirmed by asserting the actual opacity values in the regression test, not just their presence. Approval/conditional decision history and parallel-join progress ("Waiting for N of M branches." → "N of M branches joined.") reuse the identical prose the List view already had, now via the shared summaries module. Waits reuse `ProcessDueAt` verbatim for both `dueAt` and `resumeAt`.
- Per your refinement: `CompleteStepButton`/`ApprovalDecisionButtons` — the existing, sanctioned action components and server actions, not new ones — surface in the Graph side panel via `isActionableForViewer`, checked *per selected step-run*: in a parallel run with multiple active steps, only the one currently selected and satisfying the existing active/human-actionable/authorized rule shows controls, exactly matching what List already allowed.
- A small additive-only extension: `computeProcessGraphLayout` gained optional `nodeWidth`/`nodeHeight`/`rowHeight` (defaulting to the template editor's existing constants, so every existing caller is unaffected) — run cards carry more context (status, assignee, due/wait state) than a template step ever needs, so 6C passes taller dimensions rather than cramming.
- Manually verified end-to-end: built a template (human task → approval with a rejected outcome → skip → parallel split/join) and drove the entire run to completion through the Graph panel alone, confirming every transition — active→completed, the taken/untaken edge contrast, the skip treatment, the join's obligation-count text at both intermediate and final states, and the run's own status flipping to complete — before writing the automated test.

Automated Action Nodes (migrations `0041_process_action_nodes.sql` through `0044_process_action_fk_set_null_fix.sql`):

- A new `action` node type performs one deterministic system action automatically when activated: create record, update the origin record, update a related record, or start a process. It has no assignee, no due rule, never appears in My Work, and never requires manual completion — reusing the *exact* `WorkflowAction`/`WorkflowFieldMapping` shape and `executeSingleAction` executor workflows already use (`lib/domain/workflow-engine.ts`), not a second automation system. Editor config UI (`app/components/workflow-action-config-fields.tsx`) is a new shared component the process template editor consumes; the existing Workflow editor was deliberately left untouched (its action-config JSX remains its own, separately-tested implementation) rather than retrofitted under time pressure — a follow-up worth doing, not done here.
- Runtime shape: an activated `action` step stays `active` and uncascaded in SQL (`private.activate_process_step_run` now excludes it from the parallel/system auto-cascade, exactly like `human_task`/`approval`/`wait` already are) rather than resolving inline. The app layer then executes it via one canonical primitive, `executeAndCompleteActionStep` (`lib/domain/process-repository.ts`), and calls back `complete_process_action_step_authorized` (success, advances through the existing `private.complete_process_step_and_advance` continuation helper with a new `action_succeeded` routing outcome) or `fail_process_action_step_authorized` (failure). Both RPCs take only run/step identity and a result payload — never action config — so a caller can only ever say "execute this active ProcessStepRun," never specify an arbitrary mutation; the config actually executed always comes from the step's own immutable `config.actionConfig` snapshot.
- No new `ProcessStepRunStatus`: a failed execution leaves the step `active` with `action_result.status = 'failed'`, visible in both List and Graph with a distinct "Failed" treatment and a Retry control open to any workspace member (matching action nodes' no-assignee shape); a successful execution's `action_result.status = 'succeeded'` accompanies the ordinary `completed` transition. Retry re-runs the identical canonical executor on that one step — not a separate code path.
- `executeActiveProcessActionSteps` is a bounded (25-pass) per-run activation loop called after every mutation that can activate a step — `startProcessRun`, `completeProcessStepRun`, `decideProcessApproval` — so manual and workflow-triggered runs (both already funnel through these same repository functions) behave identically, and a chain of action nodes or several parallel branches each landing on their own action step all settle before the caller's request returns.
- Full composability: a wait or condition-wait resuming into an action node is supported. `getEntityContext`/`getEntityRecord`/`entityRecordExists`/`createEntityRecord`/`updateEntityRecord`/`startProcessRun`/`getProcessRunWithSteps` all gained an optional injectable `supabase` client (`lib/supabase/server.ts`'s `SupabaseServerClient` type), defaulting to the existing per-request cookie client everywhere it's omitted (zero behavior change for every existing caller). The wait/condition-wait scheduler route (`app/api/internal/process-waits/route.ts`) uses this to drain any action node its own RPCs just activated, via a narrow discovery query (`listActiveProcessActionStepRuns`, identity only) feeding the *same* canonical executor under its admin client — never a separate scheduler-only execution path or a broad service-role mutation surface.
- Idempotency: `entity_records.originating_process_step_run_id` and `process_runs.originating_process_step_run_id` (both nullable, partial-unique-indexed per workspace) are the durable identity a `create_record`/`start_process` action's own step run carries. `create_entity_record_with_relations` and `start_process_run_authorized` check for an existing row/run tagged with that identity before creating one, so a retry after a crash between a successful side effect and its completion record reuses the prior result instead of duplicating it; relation writes are never repeated on the reuse path. Updates need no equivalent — `executeRecordUpdate`'s existing diff-against-current-value check already no-ops a repeated identical update.
- Template-save validation (`private.validate_process_action_node_config`) is deliberately lighter than execution-time validation (which reloads live entity context exactly as workflow actions already do): it only rejects structurally dangling references (missing/archived target entity, relation field, or process template; a `start_process` action cannot target its own template) at save time.
- Safe-delete/lifecycle dependency scanning was extended everywhere the equivalent `workflows.actions[]` scan already existed: field deletion (`delete_field_definition_if_safe`), entity-type deletion (`create_record` targets), and process-template deletion (`start_process` targets) all now also scan `process_nodes.config.action_config`/`process_step_runs.config.action_config` the same way.
- Three real bugs were found and fixed via focused E2E runs against the applied migration, each isolated to its own follow-up migration rather than editing the original: (1) `CREATE OR REPLACE FUNCTION` with an appended parameter does not replace an existing function — Postgres matches by full parameter-type list, so it silently created a second overload, leaving every existing 4-arg caller ambiguous (`0042`); (2) `save_process_template_authorized` only stripped the new `action_config` key from steps it disguised as `human_task`, not from every other step, so an ordinary step's residual `action_config: null` key tripped a pre-existing structural allowlist guard inside the inner save function (`0043`); (3) a composite foreign key's plain `on delete set null` nulls every column in the FK including `workspace_id`, tripping the workspace-immutability trigger — fixed using PostgreSQL 15's column-scoped `on delete set null (...)` (`0044`). A fourth, non-migration regression was also found and fixed: the new `process_runs.originating_process_step_run_id` FK gave PostgREST two relationship paths between `process_runs` and `process_step_runs`, breaking My Work's embedded-select query; fixed by replacing that one embedded select with two plain queries rather than guessing the auto-generated constraint name.

Process Graph 5C.3 — Condition/Event Waits (migrations `0038_process_condition_event_waits.sql` and `0039_process_condition_wait_template_save_fix.sql`):

- A `condition_wait` is an active system StepRun with no assignee, due rule, timer rule, manual completion, or My Work entry. It snapshots `{ target, conditions }` at run start while evaluating the latest authoritative origin record or one direct related record at activation and on relevant future changes.
- `process_condition_wait_dependencies` records the active condition's field and relation-binding dependencies separately from `process_condition_wait_wakeups`, the durable outbox. Narrow triggers on primitive records, relation rows, and field lifecycle enqueue only change metadata. The service-only dispatcher does evaluation and advances through the existing run-scoped graph continuation path; it uses per-ProcessRun advisory serialization and authoritative row locks, closing activation-versus-update lost wakeups without broad workspace locks.
- An already-true condition advances immediately and retains no dependency rows. A false condition remains active with only its current dependencies. A related wait rebinds atomically from A to B when the origin relation changes: dependencies on A are removed, B is registered and evaluated, and later A changes cannot advance the wait. Dependencies are removed whenever the wait resolves or is skipped.
- Direct authenticated record edits and workflow-originated canonical record writes both enqueue wakeups. Repeated wakeups and concurrent dispatcher calls remain exactly-once at the StepRun level; one failing wakeup is isolated and retryable without corrupting unrelated work. Existing run snapshots never read live template configuration, but condition values remain intentionally live.
- Condition waits preserve conditional routing, approval/timer sequencing, branch tokens, one join obligation per branch, and manual/workflow-triggered run-start parity. Unresolved condition-field and relation references participate in the same archive/safe-delete protection pattern as Process Graph 5A; once a wait resolves, its active dependency rows no longer block that dependency path.

## Database And Migrations

Migrations live in `supabase/migrations/` and are currently applied manually through the Supabase SQL Editor. The latest migration is:

- `0066_workspace_search_rpc.sql` (**applied**) — see Phase 8D.4 under Testing below. Migrations `0060` through `0065` are also applied; the "not yet applied" notes attached to each below are stale as of this writing and have been corrected in place where they occur.

`0019_workflow_multiple_actions.sql` added the ordered `actions` JSONB column (backfilling every pre-existing single-action workflow into a one-element array before enforcing `NOT NULL`/non-empty-array constraints), narrowed `action_config` to `{ triggerConfig, conditions }`, dropped the legacy `action_type`/`action_target_entity_type_id` columns and their constraints/FK, added `action_results` JSONB to `workflow_execution_logs`, and rewrote `delete_field_definition_if_safe` to scan all of `actions[]`. `0020_entity_delete_blocks_create_record_targets.sql` followed up: dropping `action_target_entity_type_id` also removed the composite FK that used to structurally block deleting an entity still targeted by a `create_record` action, so `delete_entity_type_if_safe` was rewritten (drop + recreate, since its `TABLE` return shape gained a column) to explicitly block that case instead.

`0021_auth_workspace_rls.sql` introduced Supabase Auth memberships and RLS for every workspace-scoped table. `0022_workspace_ownership_and_mutation_grants.sql` makes `workspace_id` immutable on every persisted workspace-scoped domain row. `0023_record_mutation_rpc_wrappers.sql` adds membership-checking, fixed-search-path SECURITY DEFINER wrappers for canonical record create/update/delete, allowing raw record/relation writes to be revoked. `0024_entity_create_display_field_grant.sql` restores the narrowly required display-field update permission for the still-SECURITY-INVOKER entity-creation RPC. `0025_authorized_safe_delete_wrappers.sql` revokes PUBLIC execution from privileged wrappers and adds equivalent authorized wrappers for safe entity/field deletion, allowing raw authenticated DELETE to be revoked for both tables. `0026_workspace_onboarding_setup.sql` adds a membership-checked, fixed-search-path SECURITY DEFINER bulk metadata wrapper for atomic first-run setup. It serializes setup attempts with a workspace advisory lock, rejects any workspace that already has an entity (including archived entities), revokes PUBLIC execution, and grants only authenticated/service-role execution.

`0027_process_templates_and_runs.sql` adds the whole Process System (see above): five new tables (`process_templates`, `process_nodes`, `process_edges`, `process_runs`, `process_step_runs`), all RLS-enabled and select-only for `authenticated`; six new membership-checked SECURITY DEFINER RPCs for every write; the `private.reject_workspace_id_change()` immutability trigger applied to all five; and a drop+recreate of `delete_entity_type_if_safe`/`delete_entity_record_if_unreferenced` (plus their `_authorized` wrappers) to add the new `process_template_count`/`process_run_count` dependency columns. This migration exposed a gap in the shared E2E cleanup helper (`tests/e2e/helpers/supabase-test-data.ts`'s `cleanupEntitiesById`): its raw `entity_records`/`entity_types` deletes now need `process_runs`/`process_templates` cleared first, since both carry an `on delete restrict` FK back to entities/records — fixed alongside this migration so any future spec that starts a process against a test entity still cleans up correctly.

`0028_process_step_assignment.sql` adds fixed process-step assignment (see Process System above): `assignee_user_id` on `process_nodes` (composite FK to `workspace_memberships`, no cascade) and `assignee_user_id`/`assignee_label` on `process_step_runs` (soft snapshot, no FK); the new `list_workspace_member_identities_authorized` RPC; and in-place `CREATE OR REPLACE` updates (unchanged signatures, no new grants needed) to `save_process_template_authorized`, `start_process_run_authorized`, and `complete_process_step_run_authorized` for assignment validation, snapshotting, and assignee-only completion authorization.

`0029_process_step_due_dates.sql` adds nullable `process_step_runs.due_at` and an active-assignee due index, then updates the same canonical save/start/complete RPCs. It creates `private.process_due_at_from_config(jsonb, timestamptz)` to centrally validate snapshotted due configuration and calculate elapsed-hour deadlines. Process tables remain RLS-enabled/select-only to authenticated users; all affected functions retain membership checks, fixed search paths, revoked PUBLIC execution, and authenticated/service-role grants.

`0030_workflow_start_process_actions.sql` recreates only `delete_process_template_if_safe_authorized` because its table return shape gains `workflow_count`. It scans every `workflows.actions[]` JSON element for `start_process` references in the same workspace, blocks deletion when either workflow references or run history exist, and reapplies the membership check, fixed search path, revoked PUBLIC execution, and authenticated/service-role grants. Workflow action and execution-log additions are JSONB/TypeScript changes; no new workflow/process table or public RPC was introduced.

`0031_process_conditional_branching.sql` adds stable node positions, ordered/default/conditional template edges, `skipped` StepRuns, `routing_result`, and immutable per-run route snapshots. It validates forward-only connected DAGs and typed process-only branch conditions (including case-insensitive text `contains` / `not_contains`), evaluates against live origin values only at completion, and extends safe field deletion to live and unresolved snapshotted branch references. `0032_process_branching_start_function_fix.sql` replaces the already-applied start RPC to select its sole start node without PostgreSQL's unsupported `min(uuid)` aggregate.

`0033_process_parallel_paths_and_joins.sql` is the canonical clean-bootstrap Process Graph 5B migration. It adds `parallel_split`/`parallel_join` nodes, parallel group and branch-token snapshots, parallel route markers, and select-only `process_parallel_join_obligations`. Canonical template validation permits only explicit, non-nested/non-overlapping split/join regions with at least two branches, and canonical start/complete functions snapshot then execute them with per-run advisory serialization. `0034_process_parallel_system_node_null_config_fix.sql` is corrective for environments that applied the original `0033`: it changes the system-node validation so JSON `null` means no due rule, while a genuinely configured due rule or any assignee remains invalid. It preserves the existing SECURITY DEFINER, fixed-search-path, grants, and RLS posture; the corrected `0033` already includes the same rule for fresh databases.

`0035_process_approval_nodes.sql` is the canonical clean-bootstrap Process Graph 5C.1 migration. It adds approval nodes, stable UUID-backed named outcome edges (including multiple outcomes sharing a target), snapshotted outcome routes, separate approval decision history on `process_step_runs`, and `decide_process_approval_authorized`. It restricts generic completion to human tasks, extends assignment/due/My Work semantics to approvals, and preserves run-scoped graph snapshots through conditional and parallel routing. `0036_process_approval_anon_execute_fix.sql` is corrective for already-applied `0035` environments where Supabase retained an explicit `anon` EXECUTE grant: it explicitly revokes both PUBLIC and `anon`, then grants only authenticated/service-role execution. The corrected `0035` includes the same explicit anon revoke for fresh databases.

`0037_process_wait_states.sql` is the canonical Process Graph 5C.2 migration. It adds `wait` nodes and `process_step_runs.resume_at`, validates structured timer rules, snapshots wait config at run start, calculates a UTC resume timestamp only when a wait activates, and adds the service-role-only `resume_due_process_waits_system` batch RPC. The corresponding protected internal route is scheduler-provider-neutral: an external cron may call `POST /api/internal/process-waits` with `PROCESS_WAIT_SCHEDULER_SECRET`; normal users cannot call the RPC or provide workspace/run authority.

`0038_process_condition_event_waits.sql` is the canonical clean-bootstrap Process Graph 5C.3 migration. It adds `condition_wait` nodes, active dependency and durable wakeup/outbox tables, snapshot/live evaluation, minimal enqueue-only record/relation/field-lifecycle triggers, and the service-role-only `dispatch_process_condition_wait_wakeups_system` RPC. It also routes successful condition waits through the existing graph continuation mechanics and extends unresolved-field safe-delete inspection. `0039_process_condition_wait_template_save_fix.sql` is corrective for already-applied `0038` environments: ordinary template submits serialize absent `condition_wait_rule` as JSON null, so the wrapper removes that additive key before forwarding non-condition nodes to the legacy saver. It also restores the timer scheduler's established `{ result: { resumed, skipped, failed, conditions } }` response contract. `0040_process_condition_wait_workspace_cascade_fix.sql` is corrective for already-applied environments: relation-change triggers retain normal clear/replacement wakeups but skip outbox inserts after a cascading workspace deletion has removed the parent workspace. The corrected `0038` includes all three compatibility behaviors for fresh databases.

`0045_workspace_roles_and_capabilities.sql` adds `workspace_roles`, `workspace_role_capabilities`, and mandatory same-workspace `workspace_memberships.role_id`, backfilling a full compatibility role for existing memberships. `0046_workspace_capability_enforcement.sql` adds the interactive capability helper plus record wrappers and initial role-management RPCs; `0047_process_capability_wrappers.sql` wraps interactive process operations; `0048_metadata_workflow_capability_policies.sql` separates membership reads from capability-gated metadata, view, workflow, and safe-delete writes. `0049_transactional_role_management.sql` replaces row-level lockout guards with workspace-serialized final-state role edit/delete RPCs; `0050_transactional_member_role_assignment_fix.sql` gives assignment the same final-state/self-escalation protections. `0051_workspace_role_management_reads.sql` adds narrow capability-gated member/role UI read models without opening raw membership enumeration. `0052_record_create_wrapper_overload_fix.sql` removes an accidental four-argument record-create wrapper overload, retaining the canonical five-argument defaulted signature used by ordinary callers and process action nodes. `0053_workspace_role_read_model_email_type_fix.sql` corrects the authorized members read-model's `auth.users.email` result cast. The corrected `0046` and `0051` include their fresh-bootstrap fixes; `0052`/`0053` are corrective migrations for already-applied environments.

`0054_workspace_teams_and_reporting.sql` adds the flat organization layer: teams, multiple team memberships, multiple team leads, and one primary reporting relationship per member. It adds `workspace.manage_organization`, backfills it to every existing role with both member/role management capabilities, and includes it in fresh-bootstrap role capability validation. All organization tables are RLS-enabled with no authenticated raw access; hardened, fixed-search-path authorized RPCs cover management and narrow scoped reads. Workspace-level advisory serialization prevents concurrent reporting cycles, membership FK cascades clean current organization relationships, and the existing workspace-ID immutability trigger protects all new workspace-scoped rows.

`0055_manager_portfolio_reads.sql` adds `list_managed_people_context_authorized`: a narrow, fixed-search-path `SECURITY DEFINER` RPC for Phase 7C. It requires membership plus `operations.view`, derives and deduplicates the caller's direct-report/active-led-team scope with provenance, excludes the caller, and returns only identity/provenance fields required by the Team Work repository. `PUBLIC`/`anon` execution is revoked; `authenticated` and `service_role` are explicitly granted. It does not add tables, persisted aggregates, or change raw workspace process-read RLS.

`0056_operational_analytics_reads.sql` is the canonical Phase 7D migration. It extracts `private.managed_user_ids` — the exact direct-report/active-led-team CTE `list_managed_people_context_authorized` already had, now a shared `security definer`/`language sql` helper with all execution revoked from every role including `service_role` (callable only from other `security definer` functions, matching every other `private.*` helper in this codebase) — and refactors `list_managed_people_context_authorized` to delegate to it with an unchanged external contract. It adds five new capability-gated read RPCs for Phase 7D (see Domain Model above) and three new indexes verified against the actual schema first: `process_step_runs (workspace_id, status, completed_at)` and `process_runs (workspace_id, status, completed_at)` (neither table had any index supporting a period-scoped `completed_at` range scan — `process_runs` had none at all beyond its origin/template/active-uniqueness indexes), and `process_step_runs (workspace_id, assignee_user_id, status, node_type)` (the existing due-date index only covers the active+dated subset already used by My Work/Team Work). No new tables.

`0057_workload_by_team_self_exclusion_fix.sql` is corrective for an already-applied `0056`: `get_workload_by_team_authorized` joined every current member of a led team, including the caller themselves when they are also a member (not just lead) of their own team — inconsistent with every other 7C/7D scope query, which excludes the caller. Corrected `0056` already excludes self for fresh bootstrap.

`0058_analytics_ambiguous_column_fix.sql` is corrective for an already-applied `0056`/`0057`: `get_workload_by_person_authorized` (`user_id`) and `get_bottleneck_metrics_authorized` (`process_template_id`, `source_node_id`, `node_type`) each read a bare column name inside a CTE that collided with one of the function's own `returns table (...)` output columns — in PL/pgSQL, a `RETURNS TABLE` clause implicitly declares a variable per output column for the whole function body, so an unqualified reference of the same name is ambiguous between that variable and the actual column (Postgres error 42702). Found via careful review before running any tests that would have hit it, and fixed by qualifying every such reference with an explicit source alias. Corrected `0056` already qualifies these for fresh bootstrap.

`0059_field_position_swap_atomicity.sql` closes an atomicity gap found during a post-8B-closing-pass review of the field-reorder (Move Up/Move Down) feature: `swapFieldPositions` (`lib/domain/metadata-repository.ts`) originally issued its three-step sentinel position swap (needed because of `field_definitions`' `unique(entity_type_id, position)` constraint) as three separate app-issued `UPDATE`s, each its own transaction — a request interrupted between them (a crashed process, a dropped connection) could permanently strand a field at the sentinel position `2147483647`. The new `swap_field_definition_positions` RPC performs the identical three-step swap inside one `plpgsql` function, so all three updates commit or roll back together as a single transaction; `swapFieldPositions` is now a thin RPC call. No `security definer` — like `update_field_definition` beside it, this remains subject to the same `field_definitions_schema_write` RLS policy (`schema.manage`) as every other field mutation, so authorization is unchanged. **Applied** (confirmed live: `field-reorder.spec.ts` passes end to end).

`0060_workspace_onboarding_schema_capability.sql` closes the empty-workspace onboarding authority gap described under Workspace onboarding and Home above — `create_entity_types_with_fields_authorized` now requires `schema.manage` (via `private.require_interactive_workspace_capability`) instead of only workspace membership. Same signature, `CREATE OR REPLACE`, no new grants needed. Applied and confirmed live (a schema.manage-less caller's direct RPC call fails with `Permission denied: schema.manage`).

`0061_bulk_record_import.sql` (Phase 8C.2) adds the CSV-import bulk-create RPC and its idempotency ledger, `record_import_batches`; `0062_fix_import_batch_ambiguous_column.sql` is corrective for already-applied `0061`, qualifying an ambiguous column reference in the retry/collision branch (`imported_row_count` collided with the function's own `RETURNS TABLE` output column). See Phase 8C.2 under Testing above for full detail. Both applied.

`0063_process_recurrence.sql` (Phase 8D.1) adds origin-record-anchored process recurrence: `workspaces.timezone`, `process_recurrence_rules`, `process_recurrence_occurrences`, `process_runs.originating_recurrence_occurrence_id`, the pure `compute_recurrence_occurrence_date` math function, interactive rule-management RPCs (`create_/update_/set_process_recurrence_rule_active_authorized`, `automation.manage`-gated), and two service_role-only system RPCs (`start_process_run_system`, `discover_and_start_recurrence_occurrences_system`). See Phase 8D.1 under Testing below for full detail. Applied.

`0065_process_activity_events.sql` (Phase 8D.3) adds three event types to the existing `workspace_events` vocabulary (`process_started`, `process_completed`, `approval_decided`) and a new `(workspace_id, entity_record_id, created_at desc)` index for the record-scoped Activity query. The canonical `start_process_run_authorized_member` gained a required `p_actor_user_id` parameter with **no default** (drop + recreate, since the new parameter sits before the existing trailing-default one) so no caller can silently inherit `auth.uid()` by omission; the existing public `start_process_run_authorized` wrapper's own signature is unchanged and now explicitly passes `auth.uid()`, a new `start_process_run_via_workflow_authorized` wrapper (same `processes.operate` check, same shape) hard-codes `null` for workflow-triggered starts, and `start_process_run_system` passes `null` explicitly instead of relying on a default. `private.try_complete_process_run` (0033, still the sole place a run completes) and `decide_process_approval_authorized_member` (0035) both gained an event insert, guarded the same claim-before-side-effect way as every other event/notification insert in this codebase. New `list_record_activity_authorized` RPC is the only read path into `workspace_events` (the table itself stays fully closed, no RLS/grant change) — same workspace-membership bar as an ordinary record read, joins `process_runs`/`process_step_runs` for their already-durable snapshot columns rather than duplicating labels into event metadata. See Phase 8D.3 under Testing below for full detail. Applied.

`0066_workspace_search_rpc.sql` (Phase 8D.4) adds `search_workspace_records_authorized`: one query-time SQL pushdown for global Search's filtering, ranking, and per-entity-type cap (previously done in Node against every active workspace record). No new table, column, or extension — plain case-insensitive `LIKE` substring matching against `type = 'text'` fields only, with the query's own `%`/`_`/`\` characters explicitly escaped so they can never be reinterpreted as wildcards. `SECURITY DEFINER`, fixed `search_path`, explicit `private.is_workspace_member` check — the established authorized-RPC pattern, not the `SECURITY INVOKER`-relying-on-RLS shortcut that would have technically also worked here. No new capability. See Phase 8D.4 under Testing below for full detail, including the scale benchmark this migration's architecture (plain `LIKE`, no `pg_trgm`) was decided from. Applied.

Authenticated mutation grants are intentionally split, with Phase 7A capability policies layered over the previous RLS tenant boundary:

- Records and relation rows: canonical authorized wrappers for create/update/delete; records retain only direct archive/restore timestamp updates.
- Entity types and field definitions: raw create/update writes remain for the SECURITY INVOKER canonical RPCs, but RLS requires `schema.manage`; safe deletion is wrapper-only and also requires that capability. A technical same-workspace schema manager can still bypass some app-layer validation through permitted raw writes; wrapping those create/update RPCs remains future hardening.
- Entity views: direct writes remain for the existing repository but require `records.operate`. Workflows: direct writes remain for the existing repository but require `automation.manage`. Workflow execution logs allow direct insert only.
- `workspace_memberships` remains self-only select for authenticated users and has no raw authenticated mutation grant. Authorized list/assignment RPCs are the only normal management path.
- Organization tables (`workspace_teams`, team memberships/leads, and reporting relationships) have no raw authenticated access. Authorized management/read RPCs require `workspace.manage_organization` or intentionally scoped membership context.
- Process tables: select-only to `authenticated` from day one, with zero direct write grants ever — every mutation is a membership-checked authorized RPC. This is a stricter posture than every other table above, adopted deliberately for this new subsystem rather than repeating the incremental grant-then-wrap pattern documented for entity types/fields.

Major tables:

- `workspaces`
- `entity_types`
- `field_definitions`
- `entity_records`
- `entity_record_relation_values`
- `entity_views`
- `workflows`
- `workflow_execution_logs`
- `process_templates`
- `process_nodes`
- `process_edges`
- `process_runs`
- `process_step_runs`
- `record_import_batches`
- `process_recurrence_rules`
- `process_recurrence_occurrences`
- `workspace_events`
- `notifications`

Important RPCs/functions:

- `create_entity_type_with_fields` creates an entity and its initial fields atomically.
- `add_field_definition` adds a field, assigns next position with an advisory lock, and rejects required fields when records already exist.
- `create_entity_record_with_relations` atomically creates primitive JSONB values and relation rows.
- `update_entity_record_with_relations` atomically updates primitive values and covered relation fields; omitted relation fields are untouched, covered optional relations can be deliberately cleared.
- `update_field_definition` renames fields and changes required status with transactional required-field validation.
- `set_entity_display_field` sets or clears the configured display field, enforcing active same-entity text fields.
- `set_entity_default_view` sets or clears the default saved table view without a circular entity/view FK.
- `delete_entity_record_if_unreferenced` blocks hard deletion when incoming relation references exist or any process run originates from the record.
- `delete_entity_type_if_safe` blocks hard deletion for entities with records, entities with another field's relation pointing to them, entities still targeted by any workflow's `create_record` action (scans `actions[]` across all workflows in the workspace), or entities a process template (active or archived) applies to. Never modifies or deletes the dependent workflow/template — deletion is blocked, not cascaded.
- `delete_field_definition_if_safe` blocks hard deletion for primitive JSON values, relation rows, workflow JSON references (scanning workflow-level `triggerConfig`/`conditions` plus every action's `relatedFieldDefinitionId`/`fieldMappings` across `actions[]`), display-field configuration, or saved table-view configuration.
- `save_process_template_authorized` atomically creates or replaces a process template's name/description/steps, preserving node IDs for resubmitted steps and rebuilding `process_edges` from the submitted order.
- `start_process_run_authorized` validates an active compatible template/origin record, rejects a duplicate active run, walks the template's node/edge chain, and creates a snapshotted run with its first step active.
- `complete_process_step_run_authorized` atomically completes the active step and activates the next by `stepIndex`, or completes the run if none remains.
- `archive_process_template_authorized`/`restore_process_template_authorized` toggle a template's `archived_at`.
- `list_workspace_member_identities_authorized` returns only `{user_id, email}` for current members of the requesting member's own workspace — the sole source of human-readable assignee identity, used live for the assignee selector and never persisted except as `process_step_runs.assignee_label`.
- `delete_process_template_if_safe_authorized` blocks hard deletion while any process run (any status) references the template.
- `create_process_recurrence_rule_authorized`/`update_process_recurrence_rule_authorized`/`set_process_recurrence_rule_active_authorized` manage a recurrence rule's schedule; origin/template are validated at creation but never editable afterward.
- `start_process_run_system` and `discover_and_start_recurrence_occurrences_system` are `service_role`-only (PUBLIC/anon/authenticated all revoked) — the scheduler's own doors into process starts, structurally unreachable by any interactive caller regardless of capability. `discover_and_start_recurrence_occurrences_system` is the scheduler entry point: bounded batch, `FOR UPDATE SKIP LOCKED`, per-rule exception isolation, claims each due occurrence via `insert ... on conflict do nothing` before starting it.
- `compute_recurrence_occurrence_date` is pure math (no table access, no security definer) returning at most one row — the single latest due occurrence for a rule as of a given instant — so a missed-scheduler backlog is structurally impossible rather than policy-enforced.
- `mark_notification_read_authorized`/`mark_all_notifications_read_authorized` scope purely via `recipient_user_id = auth.uid()` in the `WHERE` clause; marking another member's notification (or one that doesn't exist) silently matches zero rows rather than raising.
- `generate_step_due_soon_notifications_system`/`generate_step_overdue_notifications_system` are `service_role`-only global scans (no `workspace_id` parameter), matching `resume_due_process_waits_system`'s exact posture. Every notification insert uses `on conflict (workspace_id, dedup_key) do nothing`, and the paired `workspace_events` row is created only when that insert actually claims a new row.
- `start_process_run_authorized_member` (canonical, 0065) takes an explicit, required `p_actor_user_id` with no default — every one of its three doors (`start_process_run_authorized`, `start_process_run_via_workflow_authorized`, `start_process_run_system`) must say what it means rather than a future caller silently inheriting `auth.uid()` by omission.
- `start_process_run_via_workflow_authorized` is a second interactive door into the same canonical start implementation as `start_process_run_authorized`, same `processes.operate` capability check, used only by workflow-triggered starts (`workflow-engine.ts`) — differs only in hard-coding a `null` actor, since a workflow-triggered start is automation even though it runs under the triggering editor's own session.
- `list_record_activity_authorized` is the sole read path into `workspace_events` for Activity: workspace-membership bar (same as an ordinary record read), scoped to one `entity_record_id`, newest-first, capped at 20, filtered to the Activity-visible event-type subset (`process_started`/`process_completed`/`step_assigned`/`approval_decided` — `step_due_soon`/`step_overdue`/`recurrence_started_process` stay in the table but are never projected). No service_role bypass — it's built only for an interactive session.
- `search_workspace_records_authorized` pushes global Search's filtering, ranking, and per-entity-type cap into one query: `type = 'text'`/non-archived fields on non-archived entity types/active records only, identity-field-then-prefix-then-position ranking, `row_number() OVER (PARTITION BY entity_type_id ...)` enforcing the 20-per-type cap in SQL. Same workspace-membership bar as an ordinary record read; no service_role bypass, matching `list_record_activity_authorized`'s posture.

Important structural constraints include workspace-scoped uniqueness/foreign keys, relation contract constraints, field position > 0, relation target metadata requirements, workflow trigger/action check constraints, and indexes for common workspace/entity/trigger lookups.

Workflow and saved-view references inside JSONB are not protected by relational FKs. Safe field deletion inspects current JSONB references transactionally, but concurrent reference creation during field deletion remains a future hardening concern for multi-user/authenticated operation.

## Application Structure

Important directories/files:

- `app/` contains Next.js routes, Server Components, and Server Actions. Every authenticated route lives inside the `app/(app)/` route group (URLs are unaffected — a route group folder is invisible to the path), so `app/(app)/layout.tsx` can mount the persistent global header once for all of them. `app/sign-in/page.tsx`, `app/no-workspace/page.tsx`, and `app/api/` stay outside the group, since they render before/without a workspace context.
- `app/(app)/layout.tsx` is the application shell: fetches the active workspace, capabilities, and a small alphabetical slice of active entity types once per navigation, then renders `<AppHeader>` followed by `{children}` (each page's own body, no longer responsible for mounting navigation itself).
- `app/components/app-header.tsx` is the persistent global header (application-shell redesign, post-8A): Home / Work / Business / Analytics / Configure across the top, a compact records-search field, and an Account menu — replacing the old per-page sidebar (`entity-navigation.tsx`, removed). Work collapses to a plain "My Work" link when the caller has no manager scope, else becomes a dropdown with My Work + Team Work. Business is always a dropdown: a deterministic alphabetical slice (first 6) of active business objects plus a permanent "All objects" link — deliberately not a "recently viewed"/favorites feature, since nothing persists real recency yet. Configure holds Automations/Processes (`automation.manage`), Data model (`schema.manage`), and Workspace settings (`workspace.manage_*`), gated on the union of all three so it never renders empty. "Data model" links to the same `/entities` route Business → All objects uses — reusing the page rather than building a second object-management surface; the page itself already shows Create/Archive controls only to a `schema.manage` caller, regardless of which nav path reached it. `app/components/nav-menu.tsx` is the shared click-to-open disclosure primitive behind every dropdown (Escape closes and refocuses the trigger, outside-click closes, `key={pathname}` remount closes on route change instead of a setState-in-effect) — deliberately not `role="menu"`/`"menuitem"`, since it doesn't implement full arrow-key/type-ahead menu keyboard behavior; the panel is a plain labeled `role="group"` of ordinary links/buttons instead.
- `app/(app)/entities/page.tsx` is the All Objects index, with two distinct presentation modes on one route/dataset/component rather than a second object-management system: plain (`/entities`, reached from Business → All objects) is a worker-oriented browsing surface — active objects only, `app/components/entity-type-browser.tsx`'s client-side name filter, an "Open" link per object, no Create/archive controls, regardless of the caller's own capabilities. `?manage=true` (reached from Configure → Data model, `schema.manage`-gated) is a builder-oriented configuration surface on the identical data/component — eyebrow "Configure", title "Data model", a "Create object" entry point, and a "Show archived objects" toggle (itself also `?manage=true`-scoped, so archived objects never surface in the plain mode even for a `schema.manage` caller). A caller without `schema.manage` requesting `?manage=true` is canonicalized to the plain URL, mirroring the entity page's own `?manage=true` hardening. Replaces the old per-object "Archived entities" sidebar link and the pre-redesign `/entities/new`-only "Data model" Configure link.
- `app/components/object-context-nav.tsx` is the contextual rail rendered only on a business-object's own pages (list, record detail, record edit) via `WorkspacePageLayout`'s `contextNav` slot: "All {Object}" plus each saved view as ordinary navigation (not configuration — editing/creating a view still happens in the existing in-page "Manage views" section, which the rail's "+ New view" link jumps to and auto-opens via a `newView=true` param). On the record detail/edit pages the rail renders with nothing highlighted (`highlightAll={false}`) since those aren't a view itself, just wayfinding back into the object. Saved-view tab-switching UI was removed from `app/components/entity-views-panel.tsx` (the create/edit/delete "Manage views" form stays there); `EntityViewsPanel` now also accepts `openManageByDefault`.
- `app/components/page-primitives.tsx`'s `WorkspacePageLayout` takes an optional `contextNav` (renamed from the old required `navigation`) — omitted entirely for Home/My Work/Team Work/Analytics/Configure pages, which are intentionally full-width with no contextual rail.
- `app/(app)/page.tsx` is the workspace home (Phase 8A): a "What needs you" attention strip (`app/components/home-attention-summary.tsx`, reusing `listMyWorkItems`, capped at 3 items), a one-line manager-only team-attention summary, then decluttered business-object cards (Open + Add only — saved-view shortcuts live on the entity page's contextual rail, not Home).
- `app/(app)/search/page.tsx` is the server-rendered workspace record search page.
- `app/actions.ts` is the main server-action layer for entity, field, record, lifecycle, and workflow mutations.
- `app/(app)/entities/[entityTypeId]/page.tsx` is the main entity page: metadata, saved views, records, record create form, table, field management, entity settings/lifecycle.
- `app/(app)/entities/[entityTypeId]/records/[recordId]/page.tsx` is the record detail page. `app/components/record-detail-view.tsx` (Phase 8B) renders it as three sections — Overview (primitive fields and forward relation fields together, position-ordered; primitives inline-editable, relations read-only), Related (reverse-derived relationship collections only), Processes (unchanged `ProcessSection`) — see Phase 8B under Testing below.
- `app/(app)/entities/[entityTypeId]/records/[recordId]/edit/page.tsx` handles metadata-driven record editing.
- `app/(app)/entities/new/page.tsx` handles entity creation.
- `app/(app)/settings/page.tsx` is the Members and Roles management surface. It fetches only the capability-gated role/member read models; `app/workspace-role-actions.ts` derives the active workspace server-side and calls the atomic management RPCs. `app/components/workspace-role-management.tsx` is the intentionally small management UI.
- `app/(app)/workflows/page.tsx`, `app/(app)/workflows/new/page.tsx`, and `app/(app)/workflows/[workflowId]/edit/page.tsx` handle workflow listing, creation, editing, toggling, deletion, and log display.
- `app/(app)/processes/page.tsx`, `app/(app)/processes/new/page.tsx`, and `app/(app)/processes/[processTemplateId]/edit/page.tsx` handle process template listing, creation, editing, archiving/restoring, and deletion. `app/(app)/process-runs/[processRunId]/page.tsx` is the flat process-run detail route. `app/(app)/my-work/page.tsx` is the primary-nav "My Work" page. `app/process-actions.ts` holds the process server-action layer, kept separate from `app/actions.ts`.
- `app/components/record-create-form.tsx`, `record-edit-form.tsx`, `record-detail-view.tsx`, and `entity-records-table.tsx` are generic metadata-driven record UI. `entity-records-table.tsx` and (since Phase 8B) `record-detail-view.tsx`'s Overview section both delegate eligible cells to `editable-table-cell.tsx` for inline text/number/date/boolean editing, via the same `updateRecordField.bind(null, actionContext)` wiring pattern.
- `app/components/entity-views-panel.tsx` manages saved table views (creation/editing, not navigation between them — see `object-context-nav.tsx` above).
- `app/components/workflow-create-form.tsx` is the reusable workflow definition form for create/edit.
- `app/components/process-template-form.tsx` is the process template editor shell (name/description/applies-to, Add/Move Up/Move Down/Remove, and the List/Graph view toggle). `app/components/process-node-editor.tsx` holds the shared per-node-type editing fields (`ProcessNodeEditor` and sub-fields) used by both the List row and the Graph side panel. `app/components/process-graph-view.tsx` renders the Graph view (nodes, edges, selection, side panel); `app/components/process-graph-layout.ts` computes its rank/lane layout; `app/components/process-graph-summaries.ts` formats node/edge summary text. `app/components/process-template-shared.ts` holds the shared local form-state types/helpers (`LocalStep`, `LocalRoute`, etc.) used across all of the above. `app/components/process-section.tsx` is the record-detail "Processes" summary. `app/components/process-run-detail-view.tsx` is the process-run detail page body. `app/components/process-row-actions.tsx`, `start-process-button.tsx`, and `complete-step-button.tsx` are the process lifecycle/action buttons.
- `app/components/field-*` and `entity-*` components handle metadata management.
- `lib/domain/types.ts`, `workflow-types.ts`, and `process-types.ts` define domain shapes.
- `lib/domain/metadata-repository.ts`, `record-repository.ts`, `view-repository.ts`, `workflow-repository.ts`, and `process-repository.ts` encapsulate Supabase access.
- `lib/domain/view-types.ts`, `view-engine.ts`, and `view-validation.ts` own saved-view behavior.
- `lib/domain/record-validation.ts`, `entity-definition-validation.ts`, `field-definition-validation.ts`, `field-edit-validation.ts`, `workflow-validation.ts`, and `process-validation.ts` own authoritative validation/parsing.
- `lib/domain/workflow-engine.ts`, `workflow-conditions.ts`, `workflow-change-detection.ts`, `workflow-template.ts`, and `workflow-field-labels.ts` own workflow behavior.
- `lib/supabase/server.ts` creates the server-only Supabase client.
- `supabase/seed.sql` seeds the demo workspace/client-style starting data.

## Testing

Playwright E2E tests live in `tests/e2e/`.

Current spec files:

- `entity-record.spec.ts`
- `relations.spec.ts`
- `workflows.spec.ts`
- `record-updated-workflows.spec.ts`
- `update-record-workflows.spec.ts`
- `required-field-rpc-safety.spec.ts`
- `required-field-update-rpc-safety.spec.ts`
- `display-field.spec.ts`
- `views.spec.ts`
- `record-detail.spec.ts`
- `update-related-record-workflows.spec.ts`
- `archived-relation-edit.spec.ts`
- `related-create-records.spec.ts`
- `workspace-navigation.spec.ts`
- `workspace-search.spec.ts`
- `dark-mode-contrast.spec.ts`
- `record-updated-transition-conditions.spec.ts`
- `workflow-multiple-actions.spec.ts`
- `workspace-onboarding.spec.ts`
- `operational-ux.spec.ts`
- `inline-record-edit.spec.ts`
- `process-templates.spec.ts`
- `process-runs.spec.ts`
- `process-conditional-branching.spec.ts`
- `process-parallel-paths.spec.ts`
- `process-approvals.spec.ts`
- `process-waits.spec.ts`
- `process-condition-waits.spec.ts`
- `process-graph-builder.spec.ts`
- `workspace-roles.spec.ts`
- `record-import.spec.ts`
- `process-recurrence.spec.ts`
- `notifications.spec.ts`
- `process-activity.spec.ts`

Vitest unit tests live alongside the domain code they cover, as `lib/domain/*.test.ts` (`npm run test:unit`, config in `vitest.config.mts`) — first introduced in Phase 8C.2 for CSV-import parsing/mapping logic that doesn't need a browser. Pure tests (`csv-parsing.test.ts`, `record-import.test.ts`, `recurrence-validation.test.ts`, `activity-copy.test.ts`, `activity-repository.test.ts`) run with no external dependency; DB-dependent ones (`record-import.relations.test.ts`, `record-import.commit.test.ts`, `recurrence-occurrence.test.ts`, `recurrence-commit.test.ts`, `notification-commit.test.ts`, `activity-commit.test.ts`) reuse the same `tests/e2e/helpers/supabase-test-data.ts` fixture helpers as the Playwright suite, with each domain call given an explicit `supabase` client (a function that defaults to `createServerSupabaseClient()`, which calls Next.js `cookies()`, throws outside a request scope — every such call site in a unit test must pass its own injected admin client instead of relying on the default). `recurrence-occurrence.test.ts` is a further variant worth noting: it calls `compute_recurrence_occurrence_date` with literal scalar arguments only, so despite the network round trip it needs no fixtures, no cleanup, and no injected client at all — closer in spirit to a pure test than the other DB-dependent files. `notification-commit.test.ts` has no equivalent pure-math counterpart: 8D.2's due-soon/overdue windowing and dedup-key logic live only in SQL (matching 8D.1's own precedent for date-sensitive logic), with no real TypeScript caller to extract a parallel pure implementation from — extracting one anyway purely to satisfy a "pure unit test" category would have been dead code with no production purpose, so that layer's coverage lives entirely in the DB/RPC tests instead. `activity-repository.test.ts` is new for 8D.3 and covers something the other files don't: `compareNewestFirstWithStableTies`, the client-side secondary sort that breaks same-`created_at` ties (see Phase 8D.3 below) — a real TypeScript caller that pure-tests cleanly, unlike the due-soon/overdue case.

`list_record_activity_authorized` has no service_role bypass (unlike `start_process_run_authorized_member`'s `auth.role() = 'service_role'` escape hatch) — it's built only for an interactive session, so `activity-commit.test.ts` reads it with an authenticated member client rather than the service-role admin client every other DB test file defaults to for convenience; calling it as `service_role` fails with the same "Workspace access denied" an outside caller gets, which is correct, not a bug.

Shared helpers live in `tests/e2e/helpers/`, especially `supabase-test-data.ts`. E2E data ownership is centralized there. Each run gets a unique `E2E <suffix>` prefix/marker applied to test-created entity names, workflow names, and test record names where naming exists. Cleanup deletes prefixed workflows/entities and dependent records/relations from the current development Supabase project; `cleanupEntitiesById` clears `process_runs`/`process_templates` scoped to those entity types *before* deleting `entity_records`/`entity_types`, since both carry an `on delete restrict` FK back to entities/records (added alongside migration 0027).

`process-runs.spec.ts` introduces one new pattern: a handful of its tests (rejecting a non-active-step completion, a duplicate active run, or a cross-workspace/foreign ID) call the process RPCs directly the way `required-field-*-rpc-safety.spec.ts` does for records — but since every process RPC is a membership-checked SECURITY DEFINER function with no raw unauthenticated twin (by design, see Process System above), those direct calls need a genuinely authenticated Supabase client, not the service-role admin client `createSupabaseTestClient()` provides. The spec signs in as the same disposable `e2e-runner@ops-project.test` user the browser session already uses (see `global-setup.ts`) via `supabase.auth.signInWithPassword`, scoped to that one spec file; it does not persist a session (`persistSession: false`) and needs no separate cleanup since the user itself is created/destroyed per-run by `global-setup.ts`, not by this pattern.

This first E2E setup intentionally uses the development Supabase project with namespaced disposable data. Before CI or broader team use, prefer a separate Supabase test project or local Supabase.

`playwright.config.ts` runs one Chromium worker, starts a production-style server with:

```bash
npm run build -- --webpack && npm run start:e2e
```

The default test URL is `http://localhost:3100`, overridable with `E2E_BASE_URL`. Global setup creates an ordinary authenticated E2E browser session and stores it at the ignored stable path `tests/e2e/.auth/e2e-auth.json`, outside Playwright-managed output directories. The auth/RLS security spec deliberately uses empty storage state. Traces, screenshots, and videos are retained on failure. Tests should prefer accessible selectors and stable user-facing semantics. Avoid brittle CSS selectors and add `data-testid` only when accessible selection is genuinely insufficient.

Current full-suite baseline after Process Graph 5C.2 Wait States (`0037_process_wait_states.sql` applied): **163 tests passing, 0 failures, 0 skipped, 0 retries** in 13.0 minutes. The focused `workflow-start-process.spec.ts` suite (6 tests) covers compatible/archived template editor behavior, strict crafted-action validation, created/updated-trigger starts, canonical run/step/due snapshots, workflow-triggered approval outcome snapshots, ordered-action duplicate failure, structured process logging, archive/restore recovery, and workflow-reference safe deletion. The Auth/RLS security suite (`auth-workspace-security.spec.ts`) has 3 tests covering sign-in/no-access, active-workspace cookie validation, two-user/two-workspace RLS, immutable workspace ownership, raw grant boundaries, authorized wrappers, and cleanup. The onboarding suite (`workspace-onboarding.spec.ts`) has 5 tests covering empty versus archived-only workspace behavior, approved starter schemas/relations, display fields, custom setup, atomic rollback and concurrent setup, authenticated/anonymous/non-member wrapper access, Home shortcuts, secondary setup navigation, and explicit entity management. The operational UX suite (`operational-ux.spec.ts`) has 2 tests covering the table-first entity workspace, intentional record creation, empty states, secondary saved-view configuration, and secondary lifecycle actions. The inline-record-edit suite (`inline-record-edit.spec.ts`) has 9 tests covering text/number/date/boolean inline edit and persistence, invalid-input inline error without persistence, Escape cancellation, identity/relation cells staying read-only inline, archived-record/archived-entity inline-edit disablement, no-op edits not triggering `record_updated` workflows, and a real inline edit firing transition-condition workflow semantics identically to the full edit form.

The `process-templates.spec.ts` suite (5 tests) additionally covers optional hours/days due-rule persistence through the editor. The `process-runs.spec.ts` suite (22 tests) additionally covers elapsed-hours and 24-hour-day calculation, pending `due_at = null`, StepRun rule snapshot stability across a live template edit, completed deadline history, malformed/invalid due-rule rejection with atomic template preservation, browser-local due-date hydration without hydration errors, and My Work Overdue/Ready-now/Upcoming grouping plus dated-before-undated ordering. It retains all prior start/completion, assignment, lifecycle, and direct-RPC security coverage. `process-runs.spec.ts` requires a genuinely authenticated Supabase client (not the service-role admin client) for its direct-RPC rejection tests, and the assignment tests additionally use a second disposable authenticated workspace member created/torn down entirely within this one spec file (see the pattern note above) — see the note above.

`process-conditional-branching.spec.ts` (5 targeted tests) covers edge-driven default and conditional routing, current-origin evaluation, route snapshot isolation from later template edits, atomic archived-field failure/retry, reconvergence, `skipped` state, routing history, unresolved-route field safe-delete protection, My Work split suppression, and backward-route edit rejection. Targeted Process Graph 5A verification also reran `process-runs.spec.ts` (22), `process-templates.spec.ts` (5), and `workflow-start-process.spec.ts` (5); the full-suite baseline remains unchanged until the final gate.

`process-parallel-paths.spec.ts` (7 tests) covers split activation, concurrent branch completion, exactly-once joins, conditional routing inside one parallel branch, branch-local assignee/due snapshots and My Work, select-only join-obligation runtime state, JSON-null system-node configuration, and atomic malformed-template rejection. Process Graph 5B targeted verification also reran `process-templates.spec.ts` (5), `process-conditional-branching.spec.ts` (5), `process-runs.spec.ts` (22), and `workflow-start-process.spec.ts` (5); the subsequent full gate passed at the baseline above.

`process-approvals.spec.ts` (4 targeted tests) covers stable outcome IDs/labels, shared targets, atomic malformed config rejection, run-scoped snapshot isolation from live label/target edits, decision history/routing history, UI decision actions, assignee/unassigned authorization, My Work due visibility for assigned approvals, anon execution denial, and approval routing inside a parallel branch with exactly one arrived join obligation. Targeted Process Graph 5C.1 verification also reran `process-templates.spec.ts` (5), `process-runs.spec.ts` (22), `process-conditional-branching.spec.ts` (5), `process-parallel-paths.spec.ts` (7), and `workflow-start-process.spec.ts` (6); the final full gate passed at the baseline above.

`process-waits.spec.ts` (3 focused tests) covers duration/calendar snapshots and pending `resume_at = null`, protected scheduler-route secret checks, service-only scheduler execution, due-wait one-time advancement, and branch-token/join behavior. Focused Process Graph 5C.2 verification passed `3/3` wait tests plus `49/49` narrow process regressions (`process-templates`, `process-runs`, conditional branching, parallel paths, approvals, and workflow-triggered starts). The full-suite baseline remains unchanged until the 5C.2 final gate.

`process-condition-waits.spec.ts` (4 focused tests) covers already-true immediate advancement with no dependencies, false conditions, irrelevant versus qualifying direct record updates, duplicate dispatcher execution, related-record A-to-B rebinding, a workflow-originated canonical update wakeup, manual-completion rejection, active dependency removal, and service-only dispatcher execution. Focused Process Graph 5C.3 verification reran it `4/4` plus the narrow existing process regressions: `process-runs` (22), `process-templates` (5), conditional branching (5), parallel paths (7), approvals (4), timer waits (3), and workflow-triggered starts (6), all green (`52/52`). The Process Graph 5C.3 full gate then passed: 167 tests passing, 0 failures, 0 skipped, 0 retries.

`process-graph-builder.spec.ts` (3 tests, new for Process Graph 6A) covers the Graph view rendering every node type distinctly while sharing state with the List view, selecting a node opening a pre-filled side panel whose edit persists through the same save action/RPC as the List view, and keyboard selection (Enter to select, Escape to deselect). `process-templates.spec.ts` gained one additional targeted regression test, "reopening a saved condition wait for edit restores its target, field, operator, and value," covering the condition-wait edit-load fix described under Process Graph 6A above; it was verified to genuinely fail against the pre-fix code before being confirmed green against the fix. Targeted Process Graph 6A verification reran `process-templates.spec.ts` (6, including the new regression test) and `process-graph-builder.spec.ts` (3) together, both green, before the final gate. The Process Graph 6A full gate then passed: 171 tests passing, 0 failures, 0 skipped, 0 retries in 13.6 minutes.

`process-graph-builder.spec.ts` gained 3 more tests for Process Graph 6B: insert-on-edge splicing a node between two existing steps and persisting on save; deleting an unambiguous node offering the reconnect-vs-not choice (and confirming ambiguous nodes still delete without a prompt); and the reorder guard blocking an unsafe adjacent swap in both List and Graph, with the concise reason visible, while a genuinely safe swap between never-connected steps stays enabled. `process-templates.spec.ts` gained one more targeted regression test at the time (initially working around the then-unfixed input crash and timezone bug by writing into the hidden `processSteps` payload). Targeted Process Graph 6B verification reran `process-waits.spec.ts` (3), `process-graph-builder.spec.ts` (6, including the three new tests), and `process-templates.spec.ts` (7, including the new regression test) together, all green, before the final gate. The Process Graph 6B full gate then passed: 175 tests passing, 0 failures, 0 skipped, 0 retries in 15.0 minutes. (An initial full-gate run flagged one failure in the unrelated `record-updated-transition-conditions.spec.ts` — a navigation-visibility timeout under transient system load; all 9 of that file's tests, including the one that failed, passed cleanly in isolation, and the full suite was rerun clean to confirm before recording this baseline.)

The Wait UI Bug Fixes pass rewrote that same `process-templates.spec.ts` test, now titled "typing into the Wait Amount field does not crash the page, an invalid amount still rejects, and a valid hours wait now saves with no timezone," to use real UI interaction throughout instead of the hidden-payload workaround: it types into the Amount field, clears it (verifying no crash and a `pageerror` listener stays empty for the whole test), submits with an empty amount and confirms the same rejection message as before, then saves a valid amount on the default "hours" unit and confirms via direct query that the persisted config carries no `time_zone` key, and separately switches the Unit select to `calendar_days` and confirms that config still legitimately carries one — covering both fixes and proving `calendar_days` behavior is unchanged. Test count is unchanged (one test rewritten, not added). Targeted verification reran `process-waits.spec.ts` (3), `process-graph-builder.spec.ts` (6), and `process-templates.spec.ts` (7) together, all green. The Wait UI Bug Fixes full gate then passed: **175 tests passing, 0 failures, 0 skipped, 0 retries** in 18.6 minutes. (Two transient, unrelated flakes surfaced across the first two full-gate attempts — a `global-setup.ts` sign-in timeout, and a stale-data locator collision in `process-runs.spec.ts` — each confirmed as a flake by an isolated rerun passing cleanly, before a fully clean run recorded this baseline.)

`process-run-graph.spec.ts` (1 comprehensive test, new for Process Graph 6C) drives an entire run — human task, an approval with a rejected outcome (proving the skipped branch and the taken/untaken route contrast), and a parallel split/join — to completion using only the Graph panel's Complete/Reject controls, asserting node `aria-label` status text, edge `data-taken` state and relative `stroke-opacity`, join obligation-count text at both an intermediate and the final state, the skipped node exposing no completion control, and that switching back to List shows the identical terminal counts. The mechanical `computeProcessGraphLayout` type-narrowing was verified in isolation first: rerunning `process-graph-builder.spec.ts` (6) alone before any runtime code was written, confirming zero call-site impact. Targeted Process Graph 6C verification then reran that same file plus `process-run-graph.spec.ts`, `process-runs.spec.ts`, `process-templates.spec.ts`, and the approval/parallel/wait/condition-wait/conditional-branching regressions together (59 tests, 58 passed and 1 isolated approval-spec timeout confirmed as a transient flake by a clean rerun). The Process Graph 6C full gate then passed cleanly on the first attempt: **176 tests passing, 0 failures, 0 skipped, 0 retries** in 17.4 minutes, with no flakes on this run.

`process-action-nodes.spec.ts` (2 tests, new for Automated Action Nodes) covers: a `create_record` action executing automatically with no manual click once its preceding human task completes, correctly mapping a field via `source_field` from the origin record, and creating exactly one record tagged with its originating step run's id; and a failed `update_related_record` action staying visible as "Failed" with its error message and a Retry control, then genuinely succeeding on retry after the underlying cause (a missing relation value) is fixed out of band — proving execution always reads the current record, never a stale snapshot. Targeted verification reran the new spec together with `process-graph-builder.spec.ts`, `process-templates.spec.ts`, `process-runs.spec.ts` (including both My Work tests), `process-run-graph.spec.ts`, `process-waits.spec.ts`, and `process-condition-waits.spec.ts` (45 tests) before the final gate, surfacing and fixing the three migration bugs and the My Work PostgREST-embedding regression described above. The Automated Action Nodes full gate then passed cleanly: **178 tests passing, 0 failures, 0 skipped, 0 retries** in 18.7 minutes, with no flakes.

Focused Phase 7A verification after migrations `0045`–`0053` is green: **56 tests passed** across `workspace-roles` (2), `auth-workspace-security` (3), `entity-record` (1), `views` (9), `workflows` (1), `process-templates` (7), `process-action-nodes` (2), `process-runs` (22), `process-approvals` (4), and `workspace-onboarding` (5). `workspace-roles.spec.ts` covers baseline membership reads, raw membership self-only visibility, each interactive capability boundary, authorized role/member reads, custom-role validation, cross-workspace rejection, self-escalation/self-role protections, safe reassignment deletion, concurrent final-state lockout prevention, and the Members/Roles route. It also verifies the record-create wrapper has one PostgREST-resolvable five-argument signature while four-argument callers remain supported by its default origin parameter. E2E fixture helpers now explicitly assign roles to disposable memberships.

The Phase 7A final gate passed cleanly: **180 tests passing, 0 failures, 0 skipped, 0 retries** in 18.9 minutes. The first full-run attempt surfaced two test-only 5-second UI waits while long serial fixture scenarios were still completing a record save or workflow toggle; both actions pass in isolation and their containing specs pass with explicit 15-second mutation waits. No product, RLS, or authorization defect was found. The final clean run exercised all Phase 7A guarantees: membership-based workspace-wide reads; capability-gated record, schema, automation, and human-process mutation; capability-gated member/role administration; self-escalation and last-administrator protections; cross-workspace denial; self-only raw membership visibility; and trusted system automation separate from interactive-user authority.

Focused Phase 7B verification after `0054` is green: **12 tests passed** across `workspace-organization` (3), `workspace-roles` (2), `auth-workspace-security` (3), `process-action-nodes` (2), and the two My Work cases in `process-runs`. It covers capability-gated organization management, PUBLIC/anon denial, multiple memberships/leads without authorization escalation, manager self/cycle/concurrent-cycle rejection, archived/non-empty team lifecycle rules, membership-removal cleanup, scoped versus raw organization reads, foreign IDs, the Workspace Settings Teams/Reporting UI, existing-role capability backfill, role creation with the new capability, baseline RLS, unchanged My Work, and trusted automated actions. An unrelated PostgREST embed ambiguity in active-workspace resolution was found when the new relationship graph was applied; the membership query now names its original workspace FK explicitly.

The Phase 7B final gate passed cleanly: **183 tests passing, 0 failures, 0 skipped, 0 retries** in 18.1 minutes. Three earlier full-load-only failures were test timing issues: valid record save/redirect completion exceeded Playwright's default five-second assertion window in three existing serial workflow helpers. Each passed in isolation and its containing spec; the specific post-submit assertions now use a 15-second timeout. No product, authorization, RLS, or organization defect was found.

Focused Phase 7C verification after `0055` is green: **37 tests passed** across `team-work` (3), `auth-workspace-security` (3), `process-action-nodes` (2), `process-runs` (22), `workspace-navigation` (2), `workspace-organization` (3), and `workspace-roles` (2). The new Team Work fixture covers capability/Public denial, no-scope behavior, direct-report and active-led-team unioning, self/unrelated/archived-team exclusion, multi-team/direct-report overlap provenance and deduplication, cross-workspace rejection, validated filters, next-request organization/role changes, no action controls, and existing drill-ins. The regressions confirm My Work, raw organization/process permissions, navigation, cross-workspace RLS, and trusted system automation remain unchanged.

The Phase 7C final gate passed cleanly on its second full-suite attempt: **186 tests passing, 0 failures, 0 skipped, 0 retries** in 17.4 minutes. `listMyWorkItems` was refactored into a shared `listAssignedWorkItems({ workspaceId, assigneeUserIds })` primitive (My Work now calls it with `[currentUser.id]`) so Team Work's projection is genuinely the same query shape and Overdue/Ready now/Upcoming logic as My Work, not a parallel reimplementation. The first full-suite attempt surfaced 7 failures scattered across unrelated files (`entity-record`, `inline-record-edit`, `process-parallel-paths`, `process-run-graph`, `process-runs`, `record-detail`, `record-updated-workflows`), all default-5-second-timeout or cleanup-network-blip signatures; a second attempt (after all 7 passed cleanly in isolation) surfaced 2 different failures with the identical timeout signature in yet other files, which also passed cleanly in isolation. No product, regression, or test-only defect was found in any of the 9 total flakes — this matches the same long-serial-single-worker timing pattern already documented for the Phase 7A and 7B final gates, just a higher count on this run. All Phase 7C guarantees held on the clean run: `operations.view` plus non-empty scope gating, deduplicated direct-report/active-led-team scope with the current user excluded and archived teams contributing nothing, scope-not-authority for managers/leads, an unchanged My Work, shared projection logic, no mutation controls, cross-workspace isolation, unchanged baseline process read semantics, and unaffected system-owned automation.

`analytics.spec.ts` (8 tests, new for Phase 7D) covers: `operations.view`/non-empty-scope/cross-workspace enforcement across all five new RPCs, analytics scope matching Team Work's scope exactly; throughput counting only completed `human_task`/`approval` (with completed runs, and per-node-type exclusion of `action`/system routing, verified separately); bottleneck dwell time including `wait`/`condition_wait` and excluding `action`; the overdue-rate denominator excluding undated work; the 7/30/90-day rolling window (including an exact-median recomputation across the period boundary); "timeliness trend" as on-time-vs-late totals rather than a reconstructed overdue history; team workload rows overlapping a person across multiple led teams without being summed into the portfolio total; and the rendered page's existing-page drill-ins, visible disclaimer, and absence of ranking/performance language. Targeted verification reran `team-work.spec.ts` unchanged (confirming the `private.managed_user_ids` refactor preserves Team Work's scope exactly), `process-runs.spec.ts` (My Work regression), `process-action-nodes.spec.ts`/`process-waits.spec.ts`/`process-condition-waits.spec.ts` (system-automation regression), and `workspace-organization.spec.ts`/`workspace-roles.spec.ts` (org/role separation regression) — 47 tests total, all green (one `process-waits.spec.ts` failure was a `UND_ERR_CONNECT_TIMEOUT` network-level connection timeout to Supabase from the internal scheduler route, unrelated to any Phase 7D change, confirmed transient by a clean isolated rerun). Three real bugs were found and fixed before/during this pass, each addressed with its own corrective migration rather than editing an already-applied one: the two ambiguous-bare-column PL/pgSQL bugs (`0058`) and the team-self-exclusion inconsistency (`0057`), both described under Database And Migrations above; a fourth issue (reusing one origin record across multiple fixture-inserted active runs) was a test-fixture bug, not a product defect — `process_runs_one_active_per_origin_idx` correctly rejected it.

The Phase 7D final gate passed cleanly on the first full-suite attempt: **194 tests passing, 0 failures, 0 skipped, 0 retries** in 18.1 minutes — no flakes on this run. Migrations `0056`–`0058` are the complete Phase 7D migration set. All Phase 7D guarantees held on the clean run: `operations.view` plus non-empty scope gating identical to Team Work; analytics scope matching Team Work's scope exactly (both derived from the one shared `private.managed_user_ids`); direct-report/team overlap deduplicated in every portfolio-level total; team workload rows overlapping without being summed into those totals; throughput counting only completed `human_task`/`approval` steps with completed ProcessRuns reported separately; bottleneck dwell time including `wait`/`condition_wait` and excluding system/action nodes; overdue and its rate's denominator matching the exact existing derived definitions; completed-in-period respecting the 7/30/90-day rolling window; median step/approval/cycle-time and "timeliness trend" (on-time vs. late, not a reconstructed overdue history) all correct; current-org-structure historical attribution with the visible disclaimer intact; archived teams contributing no scope; org/role changes taking effect on the next request; no ranking/performance language anywhere on the page; safe drill-downs into Team Work's existing filters and the process template editor; cross-workspace rejection; and unaffected My Work, Team Work, and system-owned automation.

During targeted verification for Fixed Process-Step Assignment + My Work, one test-only issue was found and fixed: the "assignee dropdown lists only current members" assertion originally asserted an exact closed option set, which broke because the shared dev workspace already has a real membership (the developer's own account) beyond the two test-controlled members — fixed to assert inclusion of the expected members rather than an exact set, since the dropdown correctly returns *every* current member, not just test-created ones. No product bug. Process Due Dates + Attention had one separate test-only fixture-name collision (two templates in one scenario shared a name), fixed by making the fixture names distinct; no product defect. Its full gate was clean on the first post-targeted run: 138/138 passing, 0 failures, 0 skipped, 0 retries.

Phase 8A focused verification is green: the new `workspace-navigation.spec.ts` capability-gated-nav test (a pure worker sees no Configure section at all; a `schema.manage`-only user sees exactly Data model/Archived entities and nothing else; an `automation.manage`-only user sees exactly Automations/Processes and nothing else) plus the existing two navigation tests, updated for the new labels/decluttered-Home behavior — 3/3. Renaming "Workflow" → "Automation" in test-facing labels rippled into 9 other spec files (`getByLabel("Workflow Name")` → `"Automation Name"`, `"Create Workflow"`/`"Save Workflow"` buttons → `"Create Automation"`/`"Save Automation"`) plus `workspace-onboarding.spec.ts` (`"Workspace setup"` → `"Configure"`, `"Workflows"` → `"Automations"`, `"Manage entity"` → `"Manage"`, `"Define Entity Type"` → `"Create object"`, `"No active entities yet."` → `"No active business objects yet."`) — all mechanical label updates, no logic changes, all reverified green. Broader regression (`entity-record`, `record-detail`, `display-field`, `related-create-records`, `views`, `operational-ux`, `process-runs`, `team-work`, `analytics` — 66 tests) confirmed no unintended fallout from the Home/entity-page/My-Work/Team-Work changes. No production-code defects were found during this pass — every fix was either an intentional relabeling the existing tests needed to catch up to, or a genuine (and correctly caught) test-expectation update for the decluttered Home behavior.

A follow-up hardening pass closed the `?manage=true` content-gating gap noted below (see Intentional Limitations history): `app/entities/[entityTypeId]/page.tsx` now computes `canManageSchema` from the existing `getWorkspacePermissionContext` result *before* branching, gates `isManaging` on it (`(manageParam === "true" || isArchivedEntity) && canManageSchema`), and calls `redirect(recordsHref)` to canonicalize away from an explicit `?manage=true` for any caller without `schema.manage` — rather than merely hiding the form controls, an unauthorized caller is redirected to the same plain-records URL an authorized "Return to records" click would use. `isArchivedEntity` continues to gate record-creation/views/table props independently, so a redirected or archived-but-unauthorized viewer still gets a correct, non-broken, read-only records table. A new `workspace-navigation.spec.ts` test ("`?manage=true` is canonicalized away for a caller without `schema.manage`, and unaffected for one with it") covers all four cells: worker + `?manage=true` (redirected, no admin headings), worker + plain URL (no Manage link), `schema.manage` user + `?manage=true` (kept, admin headings shown), `schema.manage` user + plain URL (Manage link shown, no admin headings) — 4/4, plus 14/14 and 24/24 on the two affected regression batches (`process-templates`/`workflow-multiple-actions`; `entity-record`/`display-field`/`required-field-rpc-safety`/`workspace-onboarding`). No production defects found; the fix and its tests passed on the first attempt.

The final Phase 8A gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean on the first attempt. The E2E suite required 8 full-suite attempts to characterize: every attempt surfaced 1–3 scattered failures (14 total instances across ~11 spec files, no file/test combination repeating), each individually classified from its exact error output as either a default 5-second Playwright assertion timeout on a post-submit confirmation toast under full single-worker serial load, or a raw network `fetch failed`/connection-timeout to Supabase's Auth admin API — and each confirmed non-reproducing via a targeted isolated rerun of its containing spec(s) before the next attempt, per this project's established flake-investigation protocol (same taxonomy as the Phase 7A/7B/7C/7D final gates, just a persistently elevated count this run). Zero deterministic defects were found in 8 attempts; nothing implicated any Phase 8A change specifically — failures landed in both touched files (mechanical label-rename only, e.g. `record-updated-workflows.spec.ts`) and entirely untouched ones (e.g. `process-condition-waits.spec.ts`) with the identical timeout signature. The most representative single run (attempt 8) posted **186 passed, 2 failed, 8 did not run (serial-mode siblings skipped after their spec's failure), 0 retries, in 17.4 minutes**; both of its failures were reconfirmed clean (12/12) in an immediate isolated rerun. Given the consistent, non-converging pattern across 8 consecutive full attempts with zero code-linked signal, the retry loop was stopped here rather than continued indefinitely — the codebase is verified sound (clean lint/typecheck/build, every individual test passing in isolation, zero deterministic failures) even though no single full-suite attempt happened to land 196/196 clean.

The Application Shell & Scalable Navigation redesign (global header + contextual rail replacing the per-page sidebar) required rewriting `workspace-navigation.spec.ts` against the new header/dropdown/rail markup, since virtually every assertion in it targeted the old sidebar directly. Because every route moved into the `app/(app)/` route group and the sidebar was replaced project-wide, the change's blast radius on the rest of the suite was checked by running **every one of the 36 spec files individually** (not as one combined full-suite invocation — that's the separate final milestone gate, run afterward), which surfaced and fixed five real, narrowly-scoped test-only breakages, all now green: (1) `auth-workspace-security.spec.ts` and `analytics.spec.ts` clicked "Sign out" directly — now inside the Account menu, so both first open it; (2) `workspace-onboarding.spec.ts`'s workspace switcher used a `<select>`+"Switch" button that no longer exists — rewritten to open the Account menu and click the target workspace's own button; (3) `views.spec.ts` and `operational-ux.spec.ts` referenced the literal link text "All Records", which is now "All {ObjectName}" in the contextual rail; (4) `entity-record.spec.ts` clicked a cross-entity link that only existed because the old sidebar listed every business object on every page — rewritten to navigate directly by URL, since that convenience affordance is exactly what the redesign intentionally removed; (5) `record-detail.spec.ts` had a locator that became ambiguous once the record page gained both its own PageHeader eyebrow and the rail's own object-name label, both rendering the same text — scoped more precisely to the page's own header. One structural fix landed mid-verification: `workspace-search.spec.ts` expected a "Search" button that the header's initial search form didn't have (input-only, Enter-to-submit) — restored an explicit submit button rather than changing the test, since a tappable/clickable affordance is better UX than relying on implicit form submission. One failure (`record-updated-workflows.spec.ts`, a `fetch failed` during fixture setup) reproduced clean on an isolated rerun, matching this project's established environmental-flake signature — not a regression. No other product defects were found; every other fix in this pass was either a deliberate relocation the tests needed to catch up to, or a genuine test-only fragility the redesign exposed.

A follow-up review pass corrected the Configure/`schema.manage` IA before the final gate: the interim design (documented above in Domain Model) had dropped `schema.manage` from Configure's gate entirely, so a schema.manage-only builder saw no Configure trigger at all. That was revised so Configure keeps a first-class "Data model" destination for `schema.manage`, reusing the same `/entities` route Business → All objects already links to rather than building a second surface. `workspace-navigation.spec.ts`'s capability-gated test was rewritten accordingly to cover all five requested cases in one pass: an ordinary worker (Business/All objects usable, no Configure, no Create/Archive controls exposed), a `schema.manage`-only builder (Configure shows exactly Data model, which reuses `/entities` and shows the authorized controls there), an `automation.manage`-only builder (Configure shows exactly Automations+Processes, no Data model), a full admin (every Configure destination present), and All Objects itself remaining an ordinary, schema-concept-free browsing surface for the worker — 4/4 passing, plus `workspace-onboarding.spec.ts`'s unrelated Configure assertions reverified green (5/5) since adding Data model back is additive to what it already checks.

The final application-shell milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean on the first attempt. The E2E suite's first full-suite attempt posted **191 passed, 2 failed, 3 did not run (serial-mode siblings skipped after their spec's failure), 0 retries, in 19.0 minutes**. Both failures were individually classified from their exact error output and matched this project's long-documented flake taxonomy precisely: `analytics.spec.ts`'s failure was a raw `fetch failed` during its `afterAll` cleanup's `deleteUser` call (a network blip to Supabase's Auth admin API, not even inside the test body); `workflow-start-process.spec.ts`'s was a default 5-second Playwright assertion timeout waiting for a heading to reappear after a "Save Changes" click (the standard post-submit-under-full-suite-load signature). Neither file was touched by this session's shell/navigation changes. Both were confirmed non-reproducing via a single targeted isolated rerun of their containing spec files (14/14 passing, a superset of the 3 skipped serial siblings) before any further action, per the explicit instruction not to loop the full suite indefinitely once scattered known flakes are conclusively demonstrated — so the gate was accepted on this basis rather than re-attempting the full run again. Zero deterministic defects; zero product-code changes were needed to reach this result.

### E2E Reliability Hardening

Test-infrastructure hardening addressing the two recurring full-suite failure classes documented above (post-submit-confirmation timeouts, transient Supabase network failures during fixture setup/cleanup), plus webServer-lifecycle and failure-capture guidance. Test-infrastructure changes only — no product behavior changed, no global Playwright config changed, no parallelization.

**Mutation-confirmation helper.** Every mutating form in the app (21 components) submits a Server Action via React 19's `useActionState`, then either renders an inline confirmation in place or (several actions, e.g. `updateRecord`) call Next's server-side `redirect()`, landing on a re-rendered destination page — both round-trips normally resolve in well under a second but occasionally exceed Playwright's default 5s `expect().toBeVisible()` under a long single-worker full-suite run, never in isolation. `tests/e2e/helpers/ui.ts` adds `expectAfterMutation(locator)`, a thin wrapper asserting visibility with a documented `MUTATION_CONFIRMATION_TIMEOUT_MS = 15_000` instead of the default — no options parameter exposing an arbitrary override, and no change to the global Playwright `expect` timeout. Migrated narrowly to the exact assertion shape with actual recorded flake history, not universally: the post-"Save Changes" heading-reappearance check in `workflow-start-process.spec.ts` and `process-condition-waits.spec.ts`, the post-create-record toast in `record-updated-workflows.spec.ts`, and (found during the final validation gate below) the post-"Create Automation" workflow-link-appearance check in `update-record-workflows.spec.ts`. Unrelated assertions were left untouched.

**Supabase fixture/network retry policy.** `tests/e2e/helpers/supabase-test-data.ts` adds `withInfraRetry` (and the exported `isRetryableInfraError` classifier, kept exported for direct validation of the policy): retries only network-level transport failures (`fetch failed`, `ECONNRESET`, `ECONNREFUSED`, `ETIMEDOUT`, `UND_ERR_CONNECT_TIMEOUT`/`UND_ERR_SOCKET`, `socket hang up`, "network error") and HTTP 5xx responses, matched against a small allowlist of known signatures — **never** a bare substring match on the word "timeout" (a genuine Postgres statement timeout is a backend/logical condition, not a transient network blip, and is deliberately not retried). 4xx responses, auth/permission errors, and Postgres logical/constraint errors are never retried and surface immediately. Maximum 3 attempts total, fixed 250ms/750ms backoff, one `console.warn("[e2e-retry] ...")` line per retry so flakiness stays visible even when an operation ultimately succeeds. Applied at two seams chosen because they're where every actually-observed flake occurred: `createSupabaseTestClient()` transparently wraps `.auth.admin.{createUser,deleteUser,listUsers}` (zero call-site changes across the 10 files that use them — the `{data, error}` contract callers already check is preserved exactly), and the shared `throwOnError` choke-point used throughout this file's own fixture create/cleanup helpers now retries before throwing. **Deliberately not retried:** `createEntityRecord`'s RPC call (`create_entity_record_with_relations`) generates its own record id server-side with no client-supplied dedup key — unlike the `entity_types`/`field_definitions` inserts elsewhere in this file, which use a client-generated UUID and would fail loudly on a duplicate-key retry instead of silently double-creating, a blind retry here risks a genuine duplicate record if a response was merely lost after the server had already succeeded. **Deliberately not extended to** the ~29 spec files' own ad-hoc, direct `.from(...)` fixture calls outside this shared file — no evidence any of those have actually flaked, and retrofitting them would cross into rewriting the suite rather than hardening its shared infrastructure.

**Cleanup aggregation.** `cleanupEntitiesById`/`cleanupE2eRun`/`cleanupStaleE2eData` previously aborted on the first failed step, leaving every later step's data uncleaned even when those steps were independently removable. They now attempt every step regardless of an earlier one's outcome (each step still gets its own up-to-3-attempt retry via `throwOnError` first), collect failures into an array, and throw one aggregated error listing all of them only after every step has been attempted — a persistent failure in one class of disposable fixture data no longer prevents cleanup of everything else that could still be removed, and nothing is silently dropped: `listUncleanedE2eData` remains the audit of record. The new exported `deleteE2eUsers(userIds, client?)` applies the same attempt-all-then-aggregate pattern to the "delete a list of test users" loop duplicated across several spec files (`analytics.spec.ts`, `team-work.spec.ts`, `workspace-navigation.spec.ts` ×2, `auth-workspace-security.spec.ts`) — the exact shape of the observed `analytics.spec.ts` cleanup flake, where one user's delete failing shouldn't strand the others undeleted.

**WebServer lifecycle.** `playwright.config.ts`'s `webServer.reuseExistingServer: false` means Playwright always tries to bind port 3100 itself; if a prior run's `next-server` process tree survives (most plausibly from a full-suite run being backgrounded/piped and not fully reaped, rather than a Playwright defect), the next invocation fails immediately with a port-in-use error before any test runs. No evidence of this was found in this repo's history and no orphan was present during this hardening pass, so — consistent with not adding automatic process-killing — this stays documentation/workflow-only: before `npm run test:e2e`, `lsof -ti:3100` identifies anything bound to the E2E-dedicated port (3100 is never used for anything else in this config; the dev server is 3000). Only kill a process found there if it's plausibly this project's own orphaned `next-server`/`next start` (check `ps -p <pid> -o command=` for `next start --hostname localhost --port 3100` or the `.next` build output path) — never kill on port match alone, since an unrelated process could coincidentally hold the port.

**Failure-output capture.** Never make `command | tail -N` the only place a gate run's output goes — the lesson from previously losing early failure output this way. Standard workflow: `npm run test:e2e > <logfile> 2>&1` (full capture; run in the background for the ~20 minute duration), then `grep`/`tail`/`Read` against the complete file afterward for inspection — `tail`/`grep` become viewing tools over an already-fully-captured file, never the capture mechanism itself. Durable regardless of how stdout was handled: the configured `[["list"], ["html", { open: "never" }]]` reporter writes a complete HTML report to `playwright-report/` every run, and `trace: "retain-on-failure"` / `screenshot: "only-on-failure"` / `video: "retain-on-failure"` persist per-failure artifacts in `test-results/`, recoverable via `npx playwright show-trace` even if a log capture were imperfect.

**Validation.** The classification policy was validated directly (not just by reasoning): a temporary script imported the real, now-exported `isRetryableInfraError` and checked it against 23 cases spanning every category above (network signatures, 5xx/4xx status boundaries, Postgres constraint-violation messages, the "statement timeout"/generic-"timeout"-substring non-goals) — 23/23 passed, then the script was deleted. The 36 tests across every touched spec file (the 3 originally using `expectAfterMutation`, the 5 using `deleteE2eUsers`) passed together in one targeted run; no `[e2e-retry]` log line fired during that run, since no transient failure actually occurred to trigger one — reported honestly rather than claimed as exercised. One full `npm run test:e2e` was then run exactly once per the explicit instruction not to loop chasing a manufactured clean result: **192 passed, 1 failed, 3 did not run (serial-mode siblings skipped), 0 retries, in 18.2 minutes** — markedly cleaner than every previous full-suite baseline recorded in this document (which ranged 2–14 scattered failures). The single failure (`update-record-workflows.spec.ts`, waiting for a just-created workflow's link to appear after "Create Automation") matched the exact same documented signature as the sites already hardened but at a call site not previously in the identified set; a targeted isolated rerun of its file confirmed 9/9 clean both before and after, ruling out a regression, and the assertion was then migrated to `expectAfterMutation` too (now four sites total) as a direct, evidence-based extension rather than a proactive sweep — verified again in isolation (9/9). Per the same explicit instruction, the full suite was not re-run a second time solely to confirm this one additional fix; targeted isolated verification (the established, sufficient standard throughout this project's history) stands as the validation for it. Zero genuine product defects surfaced across the entire hardening pass.

### All Objects / Data Model presentation-mode fix

A follow-up fix closed an IA inconsistency the shell milestone left behind: Business → All objects and Configure → Data model both resolved to `/entities` and rendered identically (same "All objects" title, same Create-object button and archived-objects toggle visible to any `schema.manage` caller regardless of which nav path was used), undermining the intended browsing-vs-configuration distinction. `app/(app)/entities/page.tsx` now branches on `?manage=true` (`schema.manage`-gated, canonicalized away for anyone else — the same redirect pattern already used by the entity page's own `?manage=true`) to present two genuinely distinct modes on the identical route/dataset/`EntityTypeBrowser` component rather than a second object-management surface: plain `/entities` (Business → All objects) is worker-oriented — eyebrow "Business", title "All objects", no Create/archive controls for anyone, capability notwithstanding; `/entities?manage=true` (Configure → Data model) is builder-oriented — eyebrow "Configure", title "Data model", "Create object", and a "Show archived objects" toggle that itself stays `?manage=true`-scoped so archived objects never leak into the plain mode even for a `schema.manage` caller. The header's Configure → "Data model" link was updated to point at `?manage=true` accordingly; the plain Business → "All objects" link is unchanged. `workspace-navigation.spec.ts`'s capability-gated test was extended to assert the split directly — a worker sees the plain mode only and is redirected away from a manually-typed `?manage=true`; a `schema.manage` user sees the full Data model surface via Configure but reverts to the plain worker-style view when reaching `/entities` directly through Business, proving the mode is entry-driven rather than capability-driven alone — and its separate archived-entities test was updated to find archived objects through Configure/Data model rather than the plain page, which no longer offers that toggle at all. No new routes, no route changes, no new data model, no configurable record layouts or CSV import (out of scope, unrequested). Lint/typecheck/build clean; `workspace-navigation.spec.ts` (4/4) and `workspace-onboarding.spec.ts` (5/5, unaffected but reverified) green.

### Workspace Settings disabled-control contrast fix

Manual dogfooding surfaced dark-on-dark text in `workspace-role-management.tsx` and `workspace-organization-management.tsx` (Workspace Settings → Members/Roles and the org/team panel): the role dropdown, role-name/description inputs, the "reassign members to"/manager selects, and the brass "Create role"/"Create team" buttons all used `disabled:bg-slab` (a *dark* charcoal surface token, `#2e2c27`) while keeping their base `text-graphite` (near-black, `#1a1b18` — correct for the light `bg-paper` these controls otherwise sit on) with no disabled-state text override — near-black text on dark charcoal, close to unreadable. This was not a missing token: every other disabled control across the app (12+ components — `entity-create-form.tsx`, `record-create-form.tsx`, `record-edit-form.tsx`, `workflow-create-form.tsx`, `process-template-form.tsx`, `field-create-form.tsx`, `entity-settings-form.tsx`, `entity-views-panel.tsx`, `workspace-onboarding.tsx`, and the process/step action buttons) already uses a single consistent, correct pairing for disabled state: `disabled:bg-chalk disabled:text-stone` (chalk `#ddd9cf`, a light muted surface; stone `#55534d`, computes to ~5.5:1 contrast on chalk — comfortably clears WCAG AA). These two files were the only outliers, using the wrong background token for their disabled state rather than lacking a token entirely. The fix was to conform all 9 occurrences to the app's own existing, already-shared convention rather than inventing a new one — swap `disabled:bg-slab` for `disabled:bg-chalk` and add the missing `disabled:text-stone` where absent, identical to every other form's pattern. No new CSS/tokens/components were needed since the correct primitive already existed; the two outlier files simply weren't using it. Verified by computed style in a live browser session (`getComputedStyle`) after signing in as a builder and rendering both a disabled role card (the caller's own role) and a force-disabled team/manager control: every affected input/select now reports `background-color: rgb(221, 217, 207)` / `color: rgb(85, 83, 77)` (chalk/stone), matching the rest of the app exactly. Entity/schema forms, automation/process forms, and process/step action buttons were confirmed via source inspection to already use the correct pairing and were left unmodified; the sign-in/no-workspace auth forms (`.auth-form` in `globals.css`) use their own fully explicit, hardcoded colors with no `disabled:` variant and don't share this primitive at all. Focus and read-only states were untouched (no change to `:focus-visible` or any non-disabled styling); button hover states (`hover:bg-slab hover:text-chalk`, used correctly elsewhere in nav) were not affected since none of the changed elements had a conflicting hover rule. No component behavior, authorization, or visual redesign changed — only the disabled-state color pairing on 9 form controls across 2 files. Lint/typecheck clean; no E2E color-value assertions were added, consistent with this project's existing pattern of verifying contrast by computed style during manual dogfooding rather than encoding exact color values into the E2E suite.

### Phase 8B — Business Object Experience (record detail restructure)

`app/components/record-detail-view.tsx` was restructured from one flat "Details" field list plus a separate "Related Records" section into three sections — Overview, Related, Processes — with no new tables, migrations, or authorization semantics; the change is presentation/composition only, reusing existing repository data (`relationLabelsByFieldKey`, `incomingRelationGroups`) and the existing `updateRecordField` action exactly as already wired for the records table.

**Overview** (final shape, after the closing-pass regrouping below superseded the initial split) holds every non-identity field — primitive *and* forward relation — in one canonically-position-ordered pool; the identity/display field is excluded since it's already the page title. The collapsed summary shows the first `OVERVIEW_FIELD_CAP` (6) fields *populated-first*: populated fields (of either kind) in configured-position order, backfilled with empty fields in configured-position order to reach the cap. A relation is never ranked ahead of a primitive (or vice versa) merely because of its type — position alone decides, so a populated relation sitting late in position order can still lose the cap to earlier empty primitives, exactly as it would if it were a primitive. A native `<details>` "All fields" disclosure (matching the existing "Add record"/"Manage views" idiom) reveals every Overview-eligible field in canonical position order below it, including ones already shown above. Primitive rows are inline-editable via the same `EditableTableCell`/`updateRecordField.bind(null, actionContext)` pattern `entity-records-table.tsx` already used, gated identically (disabled when the entity or record is archived); relation rows render read-only (the same chip/link treatment Related always used, including the "(Archived)" target suffix) — no new component, no new server action.

**Related** now holds only reverse-derived relationship collections (`IncomingRelationGroup[]`) — forward relation fields moved into Overview during the closing pass (see below). Reverse relation groups keep their exact existing quick-create prefill link, empty state, and archived-source/field/entity exclusion, capped at `RELATED_PREVIEW_CAP` (5) with a "N more" `<details>` expand revealing the remainder — the underlying `listIncomingRelationsForRecord` query now fetches only the specific related record IDs it already computed (see the closing-pass performance fix below), not the source entity type's entire table.

**Processes** is `ProcessSection`, included unchanged — it already covered not-started/active/completed states, so it keeps the label "Processes" rather than "Active processes."

The record header gained an honest `Updated {relative time} ago` line (computed from `record.updatedAt` at render time, no new query, no client-side live-updating clock) and the entity list page's eyebrow (`app/(app)/entities/[entityTypeId]/page.tsx`) was changed from the static string "Business object" to the entity type name, for continuity with the record detail page's own eyebrow.

Initial-pass focused verification: `record-detail.spec.ts` grew from 7 to 10 tests — 3 new tests cover the populated-first Overview cap with "All fields" expand and inline editing at the new location, a sparse record still rendering Overview structure, and Related unifying a forward relation with reverse relation groups (including the new capped-preview "N more" expand) — plus the existing "archive/restore" test was extended to confirm inline editing is disabled while archived and re-enabled on restore. `workspace-navigation.spec.ts`'s one assertion checking the old literal "Business object" eyebrow string was updated for the new entity-name eyebrow (and tightened to an exact-match regex after discovering the plain substring form collided with an unrelated empty-state sentence that happens to contain the entity name). `related-create-records.spec.ts`, `inline-record-edit.spec.ts`, and `entity-record.spec.ts` were reverified unchanged (the reverse-relation-group DOM nesting Related relies on was preserved exactly) — 27 tests total across the five files, all green. No production defects found; two test-authoring mistakes (a misplaced archive-state assertion, and two DOM-duplication strict-mode violations from "All fields" intentionally re-rendering already-visible fields) were caught and fixed during this same pass, not shipped. Lint and `tsc --noEmit` clean. Per the approved scope, the full milestone gate was deferred to a dogfood-informed closing pass (below).

#### Phase 8B closing pass (dogfood-informed)

A structured worker/builder dogfood pass against a disposable fixture in the Demo Workspace surfaced several small, targeted findings, reviewed and triaged before any code changed (fix-now vs. roadmap vs. reject). Four items were approved for this closing pass; three others were confirmed as genuine future capability work and only added to `ROADMAP.md`.

**Terminology cleanup** (worker-facing copy only; Configure/Data Model builder copy was left as-is where already reasonable): "Record actions" → "More actions" (`record-detail-actions.tsx`, `entity-records-table.tsx`); the header and `/search` page's "Search records" language → plain "Search" throughout, including the empty/no-results copy; the relation-picker placeholder "Choose record" → the specific related object's name where already available (`record-create-form.tsx`) or "Choose an option" where it wasn't (`entity-views-panel.tsx`, no new prop threaded in for this); "Related Entity"/"Choose entity" → "Related object"/"Choose an object" plus one line of helper text in the field-create form, explaining what a relation field does. The misleading "Back to {entity}" link on the record detail/edit pages (it navigates to the collection, not back — a genuine correctness complaint, not a grammar one) became "← {entity}", accepted as the smallest truthful fix until plural entity labels exist; no runtime pluralization heuristic was added anywhere.

**Field reorder.** `FieldDefinition.position` was already the sole ordering signal for the records table, Overview, and "All fields" — but nothing let a builder change it after field creation. `lib/domain/metadata-repository.ts` gained `swapFieldPositions`, and `app/actions.ts` gained `moveFieldDefinition` (bound per-direction, mirroring the existing archive/restore action shape), giving `field-management-list.tsx`/`field-edit-form.tsx` Move Up/Move Down buttons per field (disabled at the first/last boundary), matching the process-template editor's existing reorder interaction rather than inventing a new one. No migration for this part: `position` already existed. The swap needed a small, non-obvious fix discovered during verification — `field_definitions` has both `unique (entity_type_id, position)` and `check (position > 0)`, so a naive two-step swap collides with the surviving row's still-current value; the fix routes the first field through position `2147483647` (a value no real field can ever reach, since positions only ever increase from 1) as an intermediate step. This was initially three separate app-issued `UPDATE`s (non-atomic — see the `0059_field_position_swap_atomicity.sql` migration under Database And Migrations above, added in a follow-up post-closing-pass review, which moved the same three-step swap into one RPC/transaction).

**Performance fix (relation label resolution).** Dogfooding raised, and inspection confirmed, that `getRelationLookups` and `listIncomingRelationsForRecord` each fetched an *entire* related entity type's table via `listEntityRecords` merely to resolve a handful of specific labels — `listEntityRecords` had no way to filter by ID. `listEntityRecords` gained an optional `ids` filter (short-circuiting to `[]` with no query when `ids` is an empty array). `listIncomingRelationsForRecord` now passes the exact `sourceRecordIds` it already derives from `entity_record_relation_values`, instead of discarding that list and fetching the source entity type's full table. `getRelationLookups` gained an opt-in `restrictToCurrentRecordValues` flag (default `false`, preserving every other call site's existing full-table behavior exactly — list pages and edit forms genuinely need the complete active-option set for their dropdowns); the record detail page is the one caller that sets it `true`, since it only ever needs labels for its own record's specific relation values. No pagination, no Home-count aggregate redesign, and no index changes were made — none were needed; the existing indexes already supported the narrower queries. `ROADMAP.md` now documents entity list page pagination and a real aggregate Home count as separate future work.

**Overview/Related regrouping.** Dogfooding judged the initial split "too structurally pure": a single-value forward relation like "Primary Analyst" reads as core record information, not as something separate merely because it's technically a relation. Confirmed via the relation storage constraint (`entity_record_relation_values` has `unique (workspace_id, source_record_id, field_definition_id)` — every forward relation is hard-constrained to at most one target) that this reduces to a clean, mechanical rule rather than a fuzzy one: *all* forward relation fields move into Overview; *only* reverse-derived collections stay in Related. The explicit risk accepted here — approved, not discovered as a surprise — is that this reverses the initial pass's "unify forward + reverse into Related" decision from a few paragraphs above; the initial-pass description above was updated in place rather than left stale, since a durable handoff document should describe what's actually true now. The ranking rule was decided explicitly rather than left implicit: a populated relation never outranks an earlier-position primitive (or vice versa) purely by type — see the Overview paragraph above.

Manual re-verification (worker and builder personas, against the disposable dogfood fixture) confirmed: the archived "Referred By" relation renders "(Archived)" and stays clickable from inside Overview; a complex record's Related section correctly showed three reverse groups including one exceeding the 5-record cap with a working "2 more" expand; a record with 10 populated fields across 22 total correctly showed only its first 6 by position in the collapsed Overview even though 4 more populated fields (further down in position, including two relations) existed; Move Up/Move Down correctly reordered fields and persisted after reload; "More actions" and "Search" render correctly in place of the old copy.

Focused re-verification after the regrouping: `record-detail.spec.ts`'s forward-relation-in-Related test was rewritten (it was testing a structure that no longer exists) to assert the forward relation now renders in Overview with no inline-edit control, while Related holds only the reverse group with its cap/expand intact — all 10 tests in the file pass. A new `field-reorder.spec.ts` (1 test) covers Move Up/Move Down boundary-disabling, persistence across reload, and that the records table's column order follows the same reordered `position`. Five other spec files referencing the changed copy strings (`workspace-navigation.spec.ts`, `operational-ux.spec.ts`, `process-runs.spec.ts`, `dark-mode-contrast.spec.ts`, `workspace-search.spec.ts`) were updated and reverified — one of those updates was corrective, not just mechanical: a `workspace-search.spec.ts` assertion collided ambiguously until scoped with `exact: true`. `related-create-records.spec.ts`, `inline-record-edit.spec.ts`, `display-field.spec.ts`, and `entity-record.spec.ts` were reverified unchanged. Every touched or reverified spec passed. No production defects were found beyond the position-swap constraint issue above, caught and fixed during this same pass.

The Phase 8B closing-pass full milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. The E2E suite posted **197 passed, 1 failed, 2 did not run (serial-mode siblings skipped), in 16.4 minutes** — the cleanest full-suite baseline recorded in this document (every prior final gate in this project's history posted at least one flake). The single failure (`workspace-navigation.spec.ts`, "archived entities stay out of normal home navigation...") was not a flake and was not re-run to chase a clean result: it was diagnosed precisely before the gate even ran. The manual dogfood fixture prepared for this milestone's review (deliberately left in place per explicit instruction, pending the requester's own sign-off) added 5 new active entity types to the Demo Workspace, bringing the total to 8 — one more than the header's `QUICK_JUMP_LIMIT` of 6. That flips `hasMoreEntityTypes` to `true`, which renders the Business dropdown's link as "All objects →" instead of the test's expected exact-match "All objects" — confirmed by direct reproduction and an entity-type-count query, unrelated to any code in this milestone. This is expected to clear on its own once the fixture is cleaned up; no code or test change was made for it, since doing so would paper over a real (if temporary and self-inflicted) navigation-copy side effect rather than fix anything. Zero genuine regressions; zero flakes.

### Phase 8C.1 — Empty-Workspace Authority Correction

Before changing anything, verified the workspace bootstrap path directly rather than assuming it was safe to tighten: there is no app-mediated "create a workspace" flow at all (confirmed via `supabase/README.md`, absence of any workspace-creation trigger, and `workspace_memberships.role_id` being a mandatory `NOT NULL` FK with no default since migration `0045`) — a workspace's first role(s) and membership(s) are created entirely by manual SQL from whoever stands the workspace up. Since creating any further role requires `workspace.manage_roles`, that person is already forced to hand-assign sufficient capabilities to the first member(s); a bootstrap that withheld `schema.manage` from all of them would leave the workspace permanently unmanageable regardless of this fix, not merely unable to onboard business objects. `create_entity_types_with_fields_authorized`'s membership-only check (`0026`) predated the roles/capabilities system (`0045`) entirely and was simply never updated once every other schema-write path was tightened to `schema.manage` (`0048`). Conclusion: safe to tighten, since no current or future membership can exist without a role a human deliberately assigned.

`app/(app)/page.tsx`'s empty-workspace branch now checks `schema.manage`: a capable caller sees the unchanged onboarding wizard, anyone else sees a new read-only "Nothing has been set up yet. Ask a workspace admin to create your first business object." section — no form, no "Start from scratch" link, nothing that promises functionality they can't use. `/entities/new` gained a matching page-level `schema.manage` gate (`redirect("/")` otherwise); its own RPC (`create_entity_type_with_fields`, SECURITY INVOKER) was already correctly blocked by the pre-existing `entity_types_schema_write`/`field_definitions_schema_write` RLS policies, so this closes a confusing dead-end form, not an actual bypass — deliberately different in kind from the onboarding RPC, which had a genuine bypass and needed the migration below. `createWorkspaceStarterStructure` (`app/actions.ts`) gained an explicit app-layer `schema.manage` check ahead of its RPC call, matching this specific action's existing explicit-early-return style (not `requireWorkspaceCapability`'s throw-based convention used by organization/role actions) — necessary as *immediate* enforcement precisely because the RPC-level fix depends on a migration this session cannot apply itself. Migration `0060_workspace_onboarding_schema_capability.sql` updates `create_entity_types_with_fields_authorized` to call `private.require_interactive_workspace_capability(p_workspace_id, 'schema.manage')`, the same helper every other interactive schema-write wrapper already uses, closing the boundary at the database layer rather than leaving it UI-only.

Focused verification: `tests/e2e/workspace-onboarding.spec.ts` gained a new `test.describe("empty workspace authority boundary")` block (1 test) proving a schema.manage-less worker sees the honest empty state and is redirected away from `/entities/new`; the existing RPC-authority test was renamed and extended with a direct-RPC sub-case (a worker's raw `create_entity_types_with_fields_authorized` call, bypassing the UI entirely) asserting rejection and zero entities created as a side effect. Both new/updated assertions were run and behave exactly as expected for the current (partially-applied) migration state: the new UI test passes already (no DB dependency), while the new RPC sub-case fails at exactly the expected line — the worker's call currently *succeeds* (`workerResult.error` is `null`), directly confirming the pre-migration bypass is real and precisely where migration `0060` closes it. `workspace-navigation.spec.ts` (4/4) and the rest of `workspace-onboarding.spec.ts` (5/5) were reverified green, confirming non-empty (Demo) workspace Home rendering and the runner's own (schema.manage-holding) onboarding path are unaffected. Lint and `tsc --noEmit` clean. Migrations `0059` and `0060` have both since been applied and confirmed live. Per the approved scope, 8C.2 (CSV import) was not started as part of 8C.1.

### Phase 8C.2 — CSV Import Pipeline

**Scope.** Import CSV rows into one existing business object, entered from the business-object collection experience (empty-object CTA, and a persistent "Import CSV" link once records exist) — never primarily through Configure → Data Model, and gated on `records.operate` alone (no new capability, no `schema.manage`). Server-side-authoritative CSV parsing (`csv-parse`), a mapping step (exact case-insensitive name-match suggestions, always editable, "Ignore" for unmatched columns), strict per-type parsing rules (dates require exactly `YYYY-MM-DD`; booleans accept a CSV-specific `true/false/yes/no/1/0` vocabulary, case-insensitive), and relation import by exact UUID or exact unique display-label match (no fuzzy matching, no auto-creation). Preflight validates every row (not just the ~50-row preview) into exactly two states, Ready or Error, with specific per-cell messages; commit requires zero errors, is one all-or-nothing transaction, and is blocked client-side until the preflight is clean. Deferred out of core 8C.2, per the original approved scope: schema-from-CSV (creating fields from unmapped columns), CSV export, and multi-object import in one file.

**New infrastructure.**
- `record_import_batches` (migration `0061`, id-as-import-UUID primary key, `workspace_id`/`entity_type_id` foreign-keyed together to `entity_types`) is the durable idempotency ledger — deliberately not just an `import_batch_id` column, so a claimed batch survives independently of whether any records exist yet. `bulk_create_entity_records_authorized` (also `0061`) does the actual bulk insert: one `records.operate`-checked, one-transaction PL/pgSQL function that re-applies the same required-field defense-in-depth as `create_entity_record_with_relations`, inserts every row plus its relation rows, and claims the batch id via `insert ... on conflict (id) do nothing`.
- `entity_records.import_batch_id` (nullable, `on delete set null`) is provenance only, not the idempotency mechanism.
- `lib/domain/csv-parsing.ts`, `lib/domain/record-import.ts` (mapping suggestions, per-cell parsing, batched relation resolution, `buildImportPreflight`), and `bulkCreateEntityRecords` in `lib/domain/record-repository.ts` are the domain layer; `app/import-actions.ts` are the three Server Actions (`parseImportFile`, `runImportPreflight`, `commitImport`); `app/components/record-import-flow.tsx` is the client wizard (upload → map → review → commit), invoked directly from event handlers rather than `useActionState`-bound forms, since the multi-step wizard needs structured data passed back and forth, not `FormData` — the first use of that pattern in this codebase. `next.config.ts` gained its first-ever custom config, `experimental.serverActions.bodySizeLimit: "8mb"`, to accommodate the 5&nbsp;MB app-level file-size cap enforced in `parseImportFile`.

**A genuine bug found and fixed before either migration was applied.** A pre-application review of `0061`'s idempotency logic (requested explicitly, before the migration was applied) traced the `insert ... on conflict (id) do nothing` → re-`select` → `raise` sequence against documented `FOUND` semantics and confirmed the *design* already prevented a colliding import id from ever returning another workspace's or entity type's cached result — the three-way `id`/`workspace_id`/`entity_type_id` match in the re-`select` correctly excludes a same-id row belonging elsewhere, causing an explicit `raise exception 'Import ID already used for a different object'` rather than a false-positive success. That review, being static, could not have caught what direct execution surfaced immediately after `0061` was applied: `returns table (imported_row_count integer)` implicitly declares a plpgsql variable of that name in the function's own scope, making `select imported_row_count into v_existing_count from record_import_batches where ...` ambiguous between the table column and that variable — Postgres rejected it outright on every retry or collision path. Migration `0062_fix_import_batch_ambiguous_column.sql` is a `create or replace function` with the reference qualified to `record_import_batches.imported_row_count`; nothing else in `0061` changed. Both migrations are applied and confirmed live.

**Focused verification.** `npm run test:unit`: 47/47 (41 pre-existing plus 6 new commit-level tests in `lib/domain/record-import.commit.test.ts` against the live RPC — clean import, full-transaction atomicity on an invalid row, relation import end to end via UUID and label resolution, and the three idempotency-boundary cases the pre-application review specified: same import id + same workspace/entity type is a safe no-op retry with no duplicate rows, a colliding id from a different workspace is rejected, a colliding id from a different entity type in the same workspace is rejected). New Playwright coverage in `tests/e2e/record-import.spec.ts` (4/4): the `records.operate` permission boundary, a clean import whose rows appear in the object's default table, a saved view, and global search, a mixed-validity CSV that blocks commit until remapped then imports cleanly, and a 500-row import (877ms end to end). A standalone, deliberately-outside-CI scale check (`scripts/manual-import-scale-check.ts`, `npx tsx scripts/manual-import-scale-check.ts`) exercised 5,000 rows directly against the domain layer: parse 28ms, preflight 21ms, commit 1,431ms, a same-id retry 510ms (no duplicate rows), for a 1,480ms end-to-end total — comfortably synchronous, no background-job infrastructure needed at this volume.

**Manual dogfood** (`dogfood.worker@ops-project.test` / `dogfood.builder@ops-project.test`, both preserved — only disposable fixture entity types/fields/records/relations/import batches were created and torn down) covered all nine approved scenarios end to end through the real browser UI: an empty object's CTA through 5 clean rows, a single mixed CSV combining valid rows, a bad date, an unmatched relation label, an ambiguous relation label, an unmapped extra column, and a missing-but-optional field (all judged correctly, with specific and actionable per-cell error messages), an archived relation target (blocked with a clear message), fixing the bad rows and re-importing successfully, and 500- and 5,000-row imports through the full browser → Server Action → RPC path (~6s for 5,000 rows — slower than the direct-domain scale check due to real network/auth overhead, still comfortably synchronous). Imported records were confirmed indistinguishable from manually created ones in the UI (same edit/more-actions affordances, same blank-value and relation-chip rendering).

Dogfood surfaced one genuine bug, fixed directly as small/routine per the approved plan: rapid repeated clicks on Import (reproduced with a scripted triple-click, representative of real double-click/impatient-retry behavior) each read the same pre-render `isImporting=false`/`importId=""` state before React had re-rendered, so each call generated an independent `crypto.randomUUID()` and committed as an unrelated import — three clicks produced three separate committed batches and 18 rows from a 6-row file. The backend idempotency boundary worked exactly as designed for what it was built to guard (repeated calls with the *same* id); the gap was client-side, generating a new id on every call instead of reusing one in flight. Fixed in `record-import-flow.tsx` with a synchronous `useRef` guard checked and set at the top of `handleCommit`, read/written immediately rather than batched like `isImporting` state — reverified with the same rapid-triple-click script: one batch, six rows.

The Phase 8C.2 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. The E2E suite posted **205 passed, 0 failed, in 16.0 minutes** — the cleanest full-suite baseline recorded in this document (every prior final gate posted at least one flake or skip). A single `[WebServer] ⨯ Error: The destination stream closed early` line appeared mid-run in the piped log output with no associated test failure, retry, or behavioral symptom; left unchased as a harmless artifact of the log-capture pipe rather than a product or infrastructure signal, consistent with this project's standing policy against re-running the full suite to chase an already-clean result.

### Phase 8D.1 — Recurrence

**Scope.** Origin-record-anchored process scheduling only: "run this process for THIS business object on a schedule" (e.g. Monthly Client Report for Meridian Health Partners). Deliberately does not support template-only recurrence (no origin record), "run for every active Client," or saved-view/filtered-object recurrence — `ProcessRun`'s existing origin-record requirement is unchanged, not weakened. A rule's `process_template_id`/`origin_entity_type_id`/`origin_record_id` are immutable after creation; only its schedule and `active` flag can change. Frequency is daily/weekly/monthly with an interval ("every N days/weeks/months"), an optional end date, and a local time of day interpreted in the workspace's timezone. A day-of-month that doesn't exist in a given month (31 in February) clamps to that month's last valid day rather than skipping the occurrence, anchored per interval-month "bucket" relative to the rule's own start date.

**Architecture.** Two already-established patterns were reused rather than invented: idempotent scheduled work (a durable claim row — `process_recurrence_occurrences`, keyed on `(recurrence_rule_id, occurrence_date)`, never wall-clock invocation time — inserted via `on conflict do nothing` before any side effect runs, the same shape as `record_import_batches` and `originating_process_step_run_id`), and system-vs-interactive authority (every process RPC already splits into a thin capability-checked wrapper plus an unwrapped canonical implementation; `start_process_run_system` is a new wrapper around the *unmodified* `start_process_run_authorized_member`, granted only to `service_role` — PUBLIC/anon/authenticated all revoked, so an interactive caller cannot reach it at all, not merely fail a check inside it). `discover_and_start_recurrence_occurrences_system` is the scheduler entry point, matching `resume_due_process_waits_system`'s exact shape: bounded batch, `FOR UPDATE SKIP LOCKED`, per-rule exception isolation. The occurrence-date math (`compute_recurrence_occurrence_date`) is pure — no table access, no `security definer` — and returns **at most one row**, the single latest due occurrence, so a missed-scheduler backlog is structurally impossible rather than policy-enforced: a 30-day scheduler outage produces exactly one run, not thirty, and older missed occurrences are never computed at all (nothing needs to mark them "skipped"). A due occurrence colliding with a still-active prior-period run (the existing one-active-run-per-origin rule) is recorded `status = 'failed'` and not retried until the next period, matching this codebase's established no-silent-auto-retry posture for action-node failures. `workspaces.timezone` (IANA string, default `UTC` for every existing and new workspace — never a browser/IP guess) is the first real consumer of `workspace.manage_settings`, present in the capability vocabulary since migration 0045 but never wired to anything until now.

**UI.** Recurrence configuration lives on the record detail page's existing Processes section (`process-recurrence-panel.tsx`), gated on `automation.manage` at both the data-fetch and render layer — an ordinary worker's page load never even queries recurrence rules, not just hides the control. Plain-language labels throughout ("Repeats: Monthly", "Every: 1 month(s)", "Day: 15", "Time: 09:00", a read-only "Timezone" field pointing at Workspace Settings) — no cron syntax, no RRULE. Workspace Settings (`workspace-timezone-settings.tsx`) gained a `workspace.manage_settings`-gated timezone field, a free-text input with a `<datalist>` of common zones for convenience; the RPC validates against `pg_timezone_names` server-side rather than trusting client input.

**Focused verification.** `npm run test:unit`: 84/84 (7 files) — `recurrence-validation.test.ts` (13, pure, frequency-specific field requirements/interval bounds/date-ordering), `recurrence-occurrence.test.ts` (15, DB-called-but-fixture-free — daily/weekly/monthly math, every-N interval, day-31 clamping including a leap year, start/end dates, latest-missed-occurrence, timezone + DST conversion), `recurrence-commit.test.ts` (9 — duplicate scheduler invocation produces exactly one run, latest-missed-occurrence at the full scheduler level (not just the pure math), inactive/archived-template/archived-origin rules all correctly excluded, a collision with a still-active prior run fails in isolation without blocking the batch, and an authenticated member holding `automation.manage` still cannot reach either system RPC directly). New Playwright coverage in `tests/e2e/process-recurrence.spec.ts` (3/3): a builder creating, editing, and disabling a schedule through the real UI; a worker seeing no recurrence UI at all; a scheduler-simulated run (this project has no committed cron frequency, so E2E drives `discover_and_start_recurrence_occurrences_system` directly rather than waiting on wall-clock time) appearing normally on record detail.

Two migration-adjacent bugs were found and fixed, both in test infrastructure rather than the product: `tests/e2e/helpers/supabase-test-data.ts`'s shared `cleanupEntitiesById` didn't know about the two new recurrence tables, so a fixture with a recurrence rule failed to clean up (`process_recurrence_rules` carries `on delete restrict` FKs to both `process_templates` and `entity_records`) — fixed by adding one cleanup step, positioned before the existing `process_runs` step since a rule's cascade-deleted occurrences hold the only reference that would otherwise dangle. Two test assertions compared a returned `timestamptz` against a JS `.toISOString()` string by exact format (`"...+00:00"` vs `"...Z"` — same instant, different representation) rather than by value — fixed to compare `getTime()`. Re-verified against the *pre-existing* `process-runs.spec.ts` suite (22/22, heaviest existing user of the modified cleanup helper) to confirm no regression.

**Manual dogfood** (`dogfood.worker@ops-project.test` / `dogfood.builder@ops-project.test`, both preserved, only disposable fixtures created/torn down) covered monthly and weekly rule creation through the real UI (rendered plain-language summaries: "Repeats monthly on day 15 at 09:00 (America/New_York)", "Repeats weekly on Monday at 08:30 (America/New_York)"), edit/disable/enable, a missed-scheduler-interval scenario (rule due for 45 days, scheduler run once, exactly one occurrence dated today — no backlog), a duplicate-scheduler-invocation scenario (two consecutive calls, exactly one run), an archived-template scenario (zero runs, correctly excluded), setting the workspace timezone from its `UTC` default, and confirming a worker sees zero recurrence UI and zero recurrence data fetched for a record that already has a builder-configured schedule. No defects found during this pass.

The Phase 8D.1 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. The E2E suite posted **199 passed, 2 failed, 7 did not run (serial-mode siblings skipped), in 19.5 minutes**. Both failures matched this project's long-documented under-full-serial-load signature exactly — a default-timeout assertion on a post-submit confirmation toast (`update-record-workflows.spec.ts`) and a 30s `afterAll` cleanup-hook timeout (`process-runs.spec.ts`) — and neither touches recurrence. Both were reconfirmed clean via one immediate isolated rerun of their two files together (31/31, including a fully clean `afterAll`), not by re-running the full suite again, per this project's standing policy against chasing an already-diagnosed flake. Zero deterministic defects.

### Phase 8D.2 — In-App Notifications

**Scope.** In-app only, three notification-generating event types: assignment (a human_task/approval step becomes active with an assignee), due-soon (active, assigned, due within 24 hours — a fixed v1 product default), and overdue (active, assigned, past due). My Work and Notifications stay strictly decoupled: My Work is derived operational truth (unchanged by this milestone); a notification's read state is purely a communication-history flag that never touches `process_step_runs` or any other operational row — verified directly by a dedicated test asserting a step-run row is byte-identical before and after marking its notification read.

**Shared event foundation.** `workspace_events` is a thin, bounded-vocabulary log (`step_assigned`, `step_due_soon`, `step_overdue`, `recurrence_started_process`), modeled deliberately after `workflow_execution_logs`/`process_step_runs.source_node_id` rather than the idempotency-ledger tables from 8C.2/8D.1: its reference columns carry **no foreign keys**, so a log row can never block deletion of the business object it describes. RLS-enabled with no select policy at all — closed for 8D.2, reserved for 8D.3's Activity UI to read from later.

**Assignment hook.** Inspected `private.activate_process_step_run` and confirmed it is the single function every step-activation path already funnels through — manual/workflow-triggered/recurrence-triggered start, ordinary continuation, conditional routing, parallel fan-out, approval nodes, and wait/condition-wait resumption. The notification+event insert was added directly there, guarded to `human_task`/`approval` nodes with a non-null assignee, and wrapped in its own exception-swallowing block: a notification-layer bug must never block the step from activating. `start_process_run_system` (8D.1) gained the same pattern to emit `recurrence_started_process` — event only, deliberately no notification (see below).

**Dedup model.** Every notification-generating path follows the claim-before-side-effect shape already established by `record_import_batches`/`process_recurrence_occurrences`: `insert into notifications ... on conflict (workspace_id, dedup_key) do nothing returning id`, and the paired `workspace_events` row is created only when that insert actually claims a row — so the event log never accumulates duplicates from a repeated scheduler pass without needing its own separate uniqueness constraint. Keys are `assignment:{step_run_id}`, `due_soon:{step_run_id}`, `overdue:{step_run_id}` — no date component, because `due_at` is set exactly once at activation and never mutated on an already-active step anywhere in this codebase (confirmed by inspection), and no reassignment path exists today (also confirmed), so there is no "which assignment episode" question to answer.

**Reminder scheduler.** Two new RPCs, `generate_step_due_soon_notifications_system`/`generate_step_overdue_notifications_system`, added to the existing internal scheduler route (`app/api/internal/process-waits/route.ts`) rather than a second endpoint — same shape as the other three system RPCs (global scan, service_role-only, bounded batch, `FOR UPDATE SKIP LOCKED`, per-row exception isolation). Documented invocation order: resume waits → dispatch condition wakeups → discover/start recurrence occurrences (8D.1) → generate due-soon notifications → generate overdue notifications → drain newly-activated action nodes (last, so a recurrence-started run's own first-step action node is guaranteed to exist by the time this runs).

**Recurrence-started notification — a conscious non-implementation.** Inspected who could plausibly receive it (rule creator, origin-record "owner" — no such concept exists, first assigned worker) and concluded the only genuinely useful recipient is whoever needs to act, which is already fully covered by the assignment notification the moment the new run's first step activates (recurrence-started runs go through the identical canonical activation path). Notifying the rule's creator on every cycle would be a routine, non-actionable ping — exactly the noise this milestone exists to avoid. The `recurrence_started_process` event is still recorded (free, useful future Activity history, inherits idempotency from `start_process_run_system`'s own occurrence-claim guarantee) — it simply drives no notification.

**Authorization.** `notifications`: RLS select-only-your-own (`recipient_user_id = auth.uid()`), zero raw write grants to any role — every row is created inside a `SECURITY DEFINER` function. Read-state changes go through two narrow RPCs, `mark_notification_read_authorized`/`mark_all_notifications_read_authorized`, both scoped purely via `recipient_user_id = auth.uid()` in the `WHERE` clause; a request for another member's notification id silently matches zero rows rather than raising, a deliberate choice to avoid confirming or denying whether a given id belongs to someone else.

**UI.** A header "Notifications" link (`app-header.tsx`) with an unread-count badge, fetched once per navigation alongside the rest of the shell's data (`getUnreadNotificationCount`, a `count: "exact", head: true` query — never a full row fetch). `/notifications` (`notification-list.tsx`) lists the 50 most recent, newest first, with per-item "Mark read" and a "Mark all read" bulk action. Notification `title`/`destination_href` are computed cheaply in SQL from already-snapshotted data (step name + event-type phrasing, `/process-runs/{id}`); the second display line (process name · origin record label) is deliberately **not** stored — it's resolved at render time (`resolveNotificationContext` in `notification-repository.ts`), batched by entity type to avoid N+1, reusing the existing `getRecordLabel`/`listEntityRecords` resolvers rather than reimplementing display-field resolution in SQL (which has no precedent anywhere in this codebase and would risk drifting from the one real implementation).

**Focused verification.** `npx vitest run lib/domain/notification-commit.test.ts`: 13/13 — assignment idempotency (exercised directly against the `dedup_key` unique constraint, since every plausible retry path already has its own upstream idempotency guard before ever reaching activation, making a genuine double-activation of the same step unreproducible through any real product path), no-notification-when-unassigned, completion preserving notification history while the item exits My Work, due-soon/overdue window boundaries and repeat-scheduler-pass idempotency, the recurrence-started event-without-notification behavior, cross-user isolation, mark-read/mark-all-read recipient scoping, and the My Work non-mutation guarantee. `npx playwright test tests/e2e/notifications.spec.ts`: 2/2 — header badge, notification list content, click-through to the process run, mark-read/mark-all-read, cross-user isolation, all through the real UI. Full `npm run test:unit` run: 96/97, with the one failure isolated to a pre-existing 8D.1 file (`recurrence-commit.test.ts`'s missed-occurrence test asserting against `new Date()` computed at assertion time) unrelated to this milestone's changes — see that test's own note below.

**Manual dogfood** (`dogfood.worker@ops-project.test`/`dogfood.builder@ops-project.test`, both preserved, only disposable fixtures created/torn down) covered an assigned human task and an assigned approval (each producing exactly one notification), a due-soon and an overdue step each surviving two scheduler passes with no duplicate, completing a task and confirming its notification stayed in history while the item left My Work, marking one notification read and confirming the header badge and unread count updated while My Work stayed byte-identical, marking all read, and cross-user isolation (the builder's own single notification, the worker's seven, neither visible to the other). Header badge and notification-list counts matched the backend exactly at every step (7 → 6 → 0 for the worker; 1 for the builder throughout). No defects found.

One incidental finding, not part of this milestone's own scope: re-running the full unit suite surfaced a pre-existing, deterministically-reproducing (not intermittent) date mismatch in 8D.1's `recurrence-commit.test.ts` ("only the latest due occurrence starts") — the scheduler's `clock_timestamp()` and the test assertion's separately-evaluated `new Date()` disagreed on the current UTC date by one day at the moment this was run, consistent with a clock/timezone discrepancy between the local test-runner host and the remote Supabase project rather than a flake or a logic defect. Left unfixed at the time per "do not reopen 8D.1"; fixed in the post-8D.2 cleanup pass below.

The Phase 8D.2 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. The E2E suite posted **205 passed, 2 failed, 3 did not run (serial-mode siblings skipped), in 22.2 minutes**. Both failures matched this project's established under-full-serial-load signature — a 30s `afterAll` cleanup-hook timeout (`process-runs.spec.ts`, the identical file/test/signature already seen in 8D.1's own gate run) and a default-timeout assertion on a post-submit confirmation toast (`update-related-record-workflows.spec.ts`) — neither touches notifications. Both were reconfirmed clean via one immediate isolated rerun of their two files together (26/26, including a fully clean `afterAll`), not by re-running the full suite again. Zero deterministic defects.

### Post-8D.2 cleanup pass — technical debt and reliability

A bounded pass between 8D.2 and 8D.3, addressing debt and flake sites surfaced by the two prior milestone gates. No product behavior changed; no migration required.

**Recurrence clock discrepancy, fixed.** Root cause: `recurrence-commit.test.ts`'s "only the latest due occurrence starts" test computed its expected date as `new Date().toISOString().slice(0, 10)` on the local Vitest-runner host, while the scheduler itself computes `occurrence_date` from `clock_timestamp()` on the remote Supabase project — two independent clocks the test implicitly assumed were identical. Fix: assert `occurrence_date` against `created_at` instead, a value the same insert stamped via `default now()` on the server itself, making both sides of the comparison server-authoritative rather than comparing a server value against an independently-computed local one. No tolerance was widened and no production code changed. Verified: `recurrence-commit.test.ts` 9/9, twice in a row. The same underlying skew surfaced independently and concretely during this pass as a transient `JWT issued at future` error from `createEntity` on one full-suite run (immediately clean on rerun with no code change) — corroborating the diagnosis; not itself fixed, see below.

**WebStorm/static-analysis warning audit.** No native WebStorm inspection run in this environment; used `eslint` (project's full `eslint-config-next` core-web-vitals + typescript rule set) and `tsc --noEmit` as the closest automated proxy, plus manual review of every previously-flagged site and everything touched this pass. Both tools are clean except the 3 already-accepted `<img>` LCP warnings (`app-header.tsx`, `no-workspace/page.tsx`, `sign-in/page.tsx` — intentional, no `next/image` equivalent need here). Re-audited the four previously-known sites, all reconfirmed unchanged: `entity-records-table.tsx`/`entity-views-panel.tsx`'s `href="#add-record"` anchors are still valid same-page links to `record-create-form.tsx`'s `id="add-record"` (confirmed both render together on `entities/[entityTypeId]/page.tsx`) — a real cross-component false positive, not a dead anchor. `record-repository.ts`'s `options` local (a named intermediate for a non-trivial ternary) and `process-repository.ts`'s locally-caught `throw` (an intentional single-catch funnel so any action-step failure, including the explicit one, records through `fail_process_action_step_authorized`) are both unchanged in surrounding logic since their prior audit — retained. "Import can be shortened" in `scripts/manual-import-scale-check.ts`/`record-import.relations.test.ts`/`record-import.commit.test.ts`: left as-is — every sibling `lib/domain/*.test.ts` file (`notification-commit.test.ts`, `recurrence-commit.test.ts`, `recurrence-occurrence.test.ts`) consistently uses the same relative-import style rather than the `@/*` alias; shortening only these three would make them inconsistent with the established convention, not more readable.

**E2E reliability.** Both full-suite flake sites from the 8D.1/8D.2 gates had a narrow, evidenced fix available. (1) `update-related-record-workflows.spec.ts:226` asserted a post-submit confirmation toast (`/1 workflow succeeded/`) with the plain 5s default timeout — the exact `expectAfterMutation` use case already used elsewhere in the suite for this precise flake signature; migrated that one call site only. (2) `process-runs.spec.ts`'s `afterAll` iterated its ~13 independent, disjoint-data fixture "runs" through `cleanupE2eRun` one at a time — the dominant cost under full-suite contention. Since different runs never share data, there's no correctness reason to serialize them: changed to `Promise.all`, aggregating failures the same way `cleanupEntitiesById` already does internally rather than losing visibility into later runs when an earlier one throws; also replaced its hand-rolled second-member-deletion loop with the existing shared `deleteE2eUsers` helper. Global hook/test timeouts were not touched.

**Shared E2E cleanup helper audit.** `notifications`/`workspace_events` (0064) were never cleaned up by `cleanupEntitiesById` — by design they carry no foreign keys back to `entity_types`/`process_runs`/`process_step_runs` (an audit-log table must never block deletion of the business data it describes), so nothing cascaded them away, and both `notification-commit.test.ts` and `tests/e2e/notifications.spec.ts` rely solely on this shared helper for cleanup. Without an explicit step, every E2E/unit run touching notifications was leaking rows into the shared dev project indefinitely. Fixed: two new steps, scoped by `entity_type_id` (which every notification/event this suite generates carries, since assignment/due-soon/overdue all derive it from the triggering run's origin record). No ordering constraint versus the rest of the function, since nothing FK-depends on these two tables.

**Unit-test infrastructure.** `npm run test:unit` is otherwise deterministic — grepped every `lib/domain/*.test.ts` file for `new Date()`/`Date.now()` used in an assertion; the fixed recurrence test was the only site. The same clock-skew condition diagnosed above surfaced independently, twice in this session, in two unrelated files (`recurrence-commit.test.ts` and `record-import.commit.test.ts`), both as a `JWT issued at future` error from `createEntity` — GoTrue rejecting a token whose `iat` looks later than the server's own clock — and both clean on an immediate rerun with no code change. Two occurrences in different files during a single pass is a repeated pattern, not a one-off, so `JWT issued at future` was added to `supabase-test-data.ts`'s `isRetryableInfraError` message allowlist. This is a narrow, deliberate exception to that policy's general "never retry auth errors" rule: the exclusion exists to avoid masking a genuine authorization bug, and this specific error is not one — it carries no capability/permission semantics, it is purely environmental skew, the same class as the transport failures already in the allowlist. Looked for small, obviously-shared-worthy duplication across the various per-spec "create a disposable workspace member" helpers (`process-runs.spec.ts`'s `createSecondWorkspaceMember`, `notifications.spec.ts`'s `createMemberWithPassword`) — not extracted: they differ in real ways (fixed deterministic identity reused for one canonical user vs. a fresh random throwaway per scenario), not just naming.

**Migration/documentation hygiene.** Re-checked `docs/PROJECT_CONTEXT.md`'s application notes for migrations `0059`–`0064` against `supabase/migrations/`: all six are already correctly marked applied, with prior stale "not yet applied" notes already corrected in an earlier pass. No further correction needed beyond the recurrence-clock note above. No new migration was required for anything in this pass.

**Files changed:** `lib/domain/recurrence-commit.test.ts`, `tests/e2e/helpers/supabase-test-data.ts`, `tests/e2e/process-runs.spec.ts`, `tests/e2e/update-related-record-workflows.spec.ts`, `docs/PROJECT_CONTEXT.md`.

**Verification:** `npm run test:unit` (97/97, including two consecutive clean runs of the fixed recurrence file), `npm run lint` (clean, 3 accepted warnings), `npx tsc --noEmit` (clean). Full E2E suite was not re-run in this pass — the cleanup-helper change is additive (two new scoped delete steps, same shape as every existing step) and the two spec changes are each independently narrow; focused/isolated verification of the changed files was judged sufficient, consistent with this project's standing policy against re-running the full suite without new evidence requiring it.

### Phase 8D.3 — Activity

**Scope.** A record-context Activity timeline answering "what meaningful things have happened around this business object," built entirely on the existing `workspace_events` foundation (0064) rather than a second table. Four event types are visible: `process_started`, `process_completed`, `step_assigned` (reused from 8D.2 unchanged), `approval_decided` (new). `step_due_soon`/`step_overdue`/`recurrence_started_process` remain in `workspace_events` for their existing purposes (notification traceability, future bookkeeping) but are excluded from the Activity projection entirely — the read RPC's own `event_type in (...)` filter, not a schema distinction. No field-level record-change audit, no record-created event (no durable actor exists for ordinary creation today), no import Activity, no historical backfill: an old record's Activity timeline is honestly empty until a new durable event occurs.

**Canonical event-emission points and actor semantics.** All three new events are emitted from the single canonical transition each already funnels through, confirmed by inspection: `process_started` from `start_process_run_authorized_member` (the unwrapped implementation every trigger path — manual, workflow, recurrence — already shares), `process_completed` from `private.try_complete_process_run` (0033, unchanged since, the sole place `process_runs.status` ever becomes `'completed'`), `approval_decided` from `decide_process_approval_authorized_member` (0035, unchanged since). Actor semantics needed a real design decision, not just `auth.uid()`: workflow-triggered starts run under the *triggering editor's own session* (same interactive RPC the manual "Start Process" button used to share), so naive `auth.uid()` would misattribute an automated workflow action to "whoever's unrelated edit happened to fire it" — exactly the "last editor" misattribution the product spec explicitly forbade. Fixed by making the canonical function's actor parameter required with no default, and giving workflow-triggered starts their own interactive door (`start_process_run_via_workflow_authorized`, identical `processes.operate` capability check, hard-coded `null` actor) rather than overloading the existing public `start_process_run_authorized`, whose signature stays exactly as every existing caller already uses it. `process_completed` and `step_assigned` always get a `null` actor (both are derived/system transitions — no reassignment feature exists today, and no human directly "completes a process," they complete a step or decide an approval). `approval_decided` always gets `auth.uid()` (the function already requires a non-null authenticated user).

**Label/history snapshot strategy.** No event carries copy-duplicating metadata — everything needed already lives as a durable snapshot on `process_runs`/`process_step_runs` from prior migrations (`process_template_name`, step `name`, `assignee_label`, `approval_outcome_label`, `decided_by_label`, all snapshotted at start/decision time), so `list_record_activity_authorized` joins to them directly rather than duplicating labels into `workspace_events.metadata`. The one un-snapshotted piece — a manual `process_started`'s human actor's display name — resolves via a live join to `auth.users.email`, matching the identical precedent already used inside `decide_process_approval_authorized_member` itself; a renamed/changed email would very rarely happen and was accepted as a minor, deliberate v1 gap rather than adding a new snapshot column.

**Query and authorization.** `list_record_activity_authorized`: workspace-membership bar (identical to an ordinary record read — Kinema has no finer-grained record visibility yet), scoped to one `(entity_type_id, entity_record_id)`, `order by created_at desc limit 20`, no service_role bypass (it's built only for an interactive session, unlike the scheduler-facing system RPCs). `workspace_events` itself stays exactly as closed as 0064 left it — no SELECT policy, no grant to `authenticated` — there is no general events explorer. New index: `(workspace_id, entity_record_id, created_at desc) where entity_record_id is not null`.

**Ordering, a real bug caught during dogfood.** Events created within one canonical transaction (a run starting and its first step assigning, in particular) share the exact same `created_at` — Postgres `now()` is frozen per-transaction, not per-statement — so `order by created_at desc` alone left same-instant rows in an undefined order. An initial fix ranked all four event types on a single global "lifecycle stage" (`process_started < step_assigned < approval_decided < process_completed`), which was wrong for the approval → next-step-assignment case: live dogfooding showed "Manager review approved by..." rendering *above* "...was assigned Publish report" even though the assignment was the decision's own direct, causally-later consequence. Fixed with a two-tier model instead (`compareNewestFirstWithStableTies` in `activity-repository.ts`, applied client-side after the RPC call, no schema/RPC change): `process_started`/`approval_decided` are always "triggers," `step_assigned`/`process_completed` are always their "consequence" within the same transaction, and a trigger never shares a transaction with another trigger (nor a consequence with another consequence) — so a flat two-tier ranking is sufficient and correct for every real case. Caught and fixed before this was reported as done, not left for a later pass.

**UI.** `RecordActivity` (`app/components/record-activity.tsx`), a compact newest-first list appended after Processes on the record detail page (Overview → Related → Processes → Activity), always rendered (never hidden like Processes when empty) with a plain "No activity yet." empty state. Operational-language copy only, formatted by the pure `formatActivityEvent` (`activity-copy.ts`): "{name} started" / "{name} started automatically" (the sole actor-presence-driven distinction — no separate trigger-source field needed), "{name} completed", "{assignee} was assigned {step}", "{step} {outcome} by {actor}". No pagination in v1 (fixed 20-item cap).

**A real availability risk found and fixed while the migration was still pending.** The record-detail page's data loader treats any fetch failure as `notFound()`. Before 0065 was applied, an unguarded `listRecordActivity` call would have 404'd *every* record page, not just left Activity empty — confirmed concretely via a live E2E run against the pre-migration database. Fixed by catching that one fetch independently (`.catch(() => [])`) so Activity — additive, secondary context — can never take down the core page it lives on.

**Focused verification.** `npx vitest run lib/domain/activity-commit.test.ts`: 7/7 — manual start actor = the acting human, workflow-triggered start actor = null, recurrence-triggered (`start_process_run_system`) actor = null, approval decision actor + durable outcome label, `step_due_soon`/`step_overdue`/`recurrence_started_process` excluded from the projection, cross-workspace access rejected with the correct "Workspace access denied" message (not just "any error"), and marking a notification read leaving its paired Activity event byte-identical. `npx playwright test tests/e2e/process-activity.spec.ts`: 2/2 — manual start + assignment newest-first with click-through to the run, and a system-triggered start reading as "started automatically" with no actor implied. Both E2E fixtures needed an explicit assignee to actually exercise `step_assigned` (an unassigned human_task only produces `process_started`) and a non-cyclic approval-reject route (a reject edge routed back to an earlier node broke the "exactly one start step" template-validation invariant) — both fixture-authoring corrections, not product defects. `notification-commit.test.ts`'s pre-existing "creates exactly one notification when a step activates with an assignee" test needed one query-scope fix: an unfiltered `workspace_events` count for a `process_run_id` now legitimately returns 2 (`process_started` + `step_assigned`, correct new behavior), not the 1 it asserted before 8D.3 existed — filtered to `event_type = 'step_assigned'`, matching what the test actually verifies (no duplicate assignment event). Full `npm run test:unit`: 118/118, with the two pre-existing, unrelated `record-import.commit.test.ts`/`record-import.relations.test.ts` `afterAll` hooks hitting the same established under-full-suite-contention timeout documented in the post-8D.2 cleanup pass above, both reconfirmed clean (12/12) on an immediate isolated rerun — not an 8D.3 regression, not investigated further per the same standing policy.

**Manual dogfood** (`dogfood.worker@ops-project.test`/`dogfood.builder@ops-project.test`, both preserved, only disposable fixtures created/torn down) walked a full three-step process — worker completes a human task, builder decides an approval, worker completes the final task, run completes — plus a separately-fixtured system/recurrence-simulated start, through the real UI/API as both personas. The resulting six-line timeline ("Monthly Client Report completed" → "worker was assigned Publish report" → "Manager review approved by builder" → "builder was assigned Manager review" → "worker was assigned Prepare summary draft" → "Monthly Client Report started") read as a genuinely coherent, complete narrative with zero technical vocabulary. Judged against the specific questions asked: the timeline tells a useful story (confirmed above); `step_assigned` does not feel redundant next to Processes — Processes shows only current state ("Current: {step} · {assignee}"), Activity shows the full history, complementary rather than duplicated; system vs. human actions are unambiguous ("started" vs. "started automatically", with no actor named in either case, matching the approved copy examples exactly); "started automatically" reads naturally; a 20-item cap is reasonable for v1 given a run produces ~5 events total; the empty state read as honest, not broken. The one real defect dogfooding surfaced (the ordering-tiebreak bug above) was fixed immediately and re-verified live in the browser before continuing — no broader event-model or actor-semantics issue emerged, so this did not need to stop and re-scope.

The Phase 8D.3 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; all four were clean. The E2E suite posted **212 passed, 0 failed, 0 skipped, 0 retries, in 23.1 minutes** — a fully clean run with no flakes to classify or isolate-rerun, only the second zero-failure full-suite baseline recorded in this document (after Phase 8C.2's 205/0). `process-activity.spec.ts`'s 2 tests passed within the full run as well as in isolation.

### Phase 8D.4 — Search

**Scope.** Global Search's filtering, matching, ranking, and per-entity-type cap all moved from Node (fetch every active workspace record, match/rank/slice in JavaScript) into one Postgres RPC. No new table, column, or extension. `searchWorkspaceRecords` (`record-repository.ts`) now calls `search_workspace_records_authorized` (migration `0066`) and only resolves the matched ids into full records/labels for display.

**Architecture decision.** Case-insensitive substring matching (`lower(...) like '%query%'`) is preserved exactly, not replaced with full-text search — `tsvector`/`to_tsquery` match whole/stemmed lexemes, not arbitrary mid-word substrings ("cme" inside "Acme"), so FTS would have silently changed matching behavior the existing E2E suite already encoded. `pg_trgm`/a maintained search-text projection was deliberately not added — the plain, unindexed `LIKE` pushdown was benchmarked first (see below) and found clearly fast enough, so the smaller architecture was kept rather than adding write-path triggers and an index on spec. `LIKE`'s own wildcard characters (`%`, `_`) and its escape character (`\`) are explicitly escaped in the query before use, preserving literal-substring semantics exactly (a naive unescaped query for "50%" would otherwise be silently reinterpreted as a wildcard) — verified directly for all three characters.

**Searchable-field semantics — unchanged from the pre-8D.4 behavior, now enforced in SQL:** only `field_definitions.type = 'text'`, non-archived, on non-archived entity types, on active records. Numbers/dates/booleans/relation UUIDs are never matched, confirmed directly (a number field whose string value equals the search marker, and a relation field pointing at a matching-name target, neither surfaces).

**Ranking behavior** mirrors the prior JS ranking for its three meaningful signals — identity/display-field match first, prefix match beats a weaker mid-string substring match, then field position — now computed via one `LATERAL` join plus `row_number() OVER (PARTITION BY entity_type_id ...)` for the per-type cap, all in a single round trip. One disclosed simplification: the final tiebreak (reachable only once those three signals are fully tied) orders by the identity field's raw value rather than replicating `getRecordLabel`'s complete empty-value/shortened-id fallback chain in SQL.

**Authorization.** `search_workspace_records_authorized` follows this codebase's established `SECURITY DEFINER` + fixed `search_path` + explicit `private.is_workspace_member` pattern (no `SECURITY INVOKER`-relying-on-RLS shortcut, even though that would have been technically sufficient here — consistency with every other authorized RPC was judged more valuable than the small simplification). No new capability; same read bar as an ordinary record fetch. Like `list_record_activity_authorized` (8D.3), it has no `service_role` bypass — it's built only for an interactive session, so DB tests and the scale-benchmark script both call it through a real authenticated member client rather than the service-role admin client used everywhere else for convenience.

**UX.** A new object-type filter (`?type=<entityTypeId>`, a plain `<select>` on the search page) restricts matching to one entity type; the per-type cap stays 20 either way, matching the approved v1 simplicity. No cursor pagination. Grouped-by-type results, per-group and total counts, and the empty/no-results states are otherwise unchanged.

**Scale benchmark** (`scripts/manual-search-scale-check.ts`, disposable fixture, not part of `test:unit`/`test:e2e`) — every planted record in a tier contains the same marker, so the search query matches the tier's *entire* row count, the real stress case for both query time and the cap:

| Tier | Fixture insert | Search (all rows match) | Search (no match) | Rows returned |
|---|---|---|---|---|
| 100 | 153–166ms | 117–149ms | 180–185ms | 20 (capped) |
| 1,000 | 516–705ms | 85–105ms | 160–190ms | 20 (capped) |
| 10,000 | ~5.0s | 182–209ms | 211–241ms | 20 (capped) |

Query time stayed roughly flat (well under 2x) across a 100x growth in candidate rows, and payload size stayed small (4.75–14.24 KB cumulative across tiers) regardless of underlying table size — confirming the architectural goal directly: search cost tracks matching/result work, not workspace size. Per your explicit decision criterion, this result was clearly acceptable, so `pg_trgm`/indexing was not added. The benchmark script itself surfaced and fixed a real test-infrastructure gap in passing: a single unbatched `DELETE` across 10,000+ fixture rows hit a Postgres statement timeout (confirmed directly, requiring a manual chunked cleanup of stranded data the first time) — the script now deletes its own fixtures in bounded chunks rather than relying on the shared `cleanupE2eRun` helper, which no other fixture in this codebase creates enough rows to need.

**Focused verification.** `npx vitest run lib/domain/search-commit.test.ts`: 12/12 — display-field match ranks first, prefix beats weaker substring, text-only/active-only field matching, archived record/entity-type/field exclusion (including a field archived *after* its value was written, proving fields are resolved live on every call), the SQL-enforced 20-per-type cap, cross-workspace rejection, similar labels across two object types grouped correctly, the object-type filter, and literal `%`/`_`/`\` handling. `npx playwright test tests/e2e/workspace-search.spec.ts`: 4/4 (3 pre-existing plus the new type-filter test). Full `npm run test:unit`: 128/128, with the same pre-existing, unrelated `record-import.*` `afterAll` timeout as prior phases, reconfirmed clean in isolation.

**Manual dogfood** covered a disposable fixture (a client entity with prefix/substring/literal-wildcard records, a >20-record bulk set, and a same-name deal record for cross-type matching) through the real UI: broad substring matching, prefix-beats-substring ranking, literal `%` and `_` (each correctly matching only their literal-character record, not a wildcard-expanded set), the object-type filter, the 20-of-22 cap, the no-results state, and click-through to the record detail page — all correct. One investigation, not a product defect: an early type-filter test appeared to silently drop the `type` param on submission: root-caused to a leftover diagnostic `submit` listener (with `preventDefault`) from this same dogfood session's own troubleshooting, still attached to an un-reloaded page — a fresh navigation and a clean click confirmed the feature works correctly; documented here since it cost real investigation time, not because it reflects a product issue.

**Files changed:** `supabase/migrations/0066_workspace_search_rpc.sql`, `lib/domain/record-repository.ts`, `app/(app)/search/page.tsx`, `lib/domain/search-commit.test.ts`, `tests/e2e/workspace-search.spec.ts`, `scripts/manual-search-scale-check.ts`.

The Phase 8D.4 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. The E2E suite posted **210 passed, 1 failed, 2 did not run (serial-mode siblings skipped), in 22.8 minutes**. The failure (`update-record-workflows.spec.ts`, a default 5-second Playwright assertion timeout on a post-submit confirmation toast) matched this project's long-documented under-full-serial-load signature exactly, in a file entirely untouched by this phase. Reconfirmed clean via one immediate isolated rerun of the whole file (9/9, including the 2 skipped serial siblings) rather than re-running the full suite again. `workspace-search.spec.ts`'s 4 tests, including the new type-filter test, all passed within the full run. Zero deterministic defects.

### Phase 8E.1 — Member/Invitation Lifecycle

**Scope.** The first slice of Admin & Governance (Phase 8E): shareable-link workspace invitations (no email delivery) and soft member deactivation/reactivation, replacing the prior "manual SQL only" path for adding/removing workspace members (see `supabase/README.md`).

**Deactivation is the sole enforcement point, not a new check scattered everywhere.** `workspace_memberships.deactivated_at` (nullable, migration `0067`) is never a hard delete — it mirrors `entity_types.archived_at`/`entity_records.archived_at`/`process_templates.archived_at` and preserves every historical FK reference (assignments, approval decisions, notifications, `workspace_events`) exactly as before. Deactivation is enforced by updating exactly two functions — `private.is_workspace_member` and `private.has_workspace_capability`, the two primitives every RLS policy and `_authorized` wrapper in this codebase is built on — to exclude deactivated rows. No other policy or wrapper needed to change. One deliberate exception: the pre-existing `workspace_memberships_select_own` RLS policy (`user_id = auth.uid()`, migration `0021`) still lets a deactivated user see their own (deactivated) membership row directly — that was never the access boundary and was left untouched; the real cutoff is `is_workspace_member`/`has_workspace_capability`, confirmed directly against `list_workspace_member_identities_authorized` in testing, not against a raw table read.

**Last-admin protection extends to deactivation**, reusing the existing `private.assert_workspace_administrator` guard (0049) rather than inventing a new one: a deactivated member no longer counts toward "the workspace still has someone who can manage members and roles," and deactivating the workspace's last qualifying admin is rejected and rolled back. Self-deactivation is blocked outright at the DB level — unlike self-role-downgrade (UI-discouraged, DB-permitted), deactivating yourself would strip your own session's access mid-transaction, an unrecoverable self-lockout the last-admin guard cannot catch after the fact.

**Invitations** (`workspace_invitations`, migration `0067`) are a shareable link an admin sends themselves — no email provider exists, so there is no automated delivery. `token` (a real `uuid`) is the sole bearer credential; resend rotates it so a stale/forwarded link stops working. Status stays `pending` until explicitly accepted/cancelled — expiry (14 days) is a plain time comparison at read/accept time, not a separate status a background job flips. At most one pending invitation per email per workspace (partial unique index), so resend/cancel manage an existing invitation rather than accumulating duplicates. `get_invitation_by_token` is the one deliberate exception to "no raw table access, only `_authorized` RPCs": granted to `anon`, since a visitor following a link is by definition not yet a member — it returns only what the accept page needs (workspace/role names, invited email, status, expiry, whether that email already has a Kinema account), never the token itself or the inviter's identity.

**Accept flow and the narrow service-role exception.** `accept_workspace_invitation_authorized` requires a real authenticated session whose email matches the invitation (case-insensitive); the token identifies *which* invitation, the session's verified email is what actually authorizes accepting it, so a forwarded link can't be redeemed under a different identity. `ON CONFLICT (workspace_id, user_id) DO UPDATE` makes re-inviting a previously-deactivated member a clean reactivation-with-possibly-new-role rather than a primary-key collision. Idempotent: accepting an already-accepted invitation simply returns the workspace id rather than erroring. For a genuinely new person (no existing Kinema account), plain `supabase.auth.signUp()` was tested directly and confirmed to trigger Supabase's own confirmation-email flow (hit "email rate limit exceeded" against a realistic domain) — since this app has no email provider, that path cannot deliver a usable account. The approved fix is a narrow, explicitly scoped service-role exception: `lib/supabase/admin.ts`'s `createAdminSupabaseClient()`, used by exactly one call site (`acceptInvitationCreateAccountAction`) to call `admin.auth.admin.createUser({ email, password, email_confirm: true })` for the invited email only, immediately followed by an ordinary `signInWithPassword()` using the password the person just chose. This is account provisioning, not session minting or impersonation — `accept_workspace_invitation_authorized` still independently re-validates token/email/status/expiry regardless of how the session was established, so account creation alone grants zero workspace access. Documented at both the call site and in `supabase/README.md` with an explicit "do not extend this exception" boundary.

**UX.** Workspace Settings' existing Members/Roles UI gained inline deactivate/reactivate controls per member row, an invite-member form (email + role), and a pending-invitations list with resend/cancel and the generated link surfaced for the admin to copy/share. A new `/accept-invitation?token=...` page branches across invalid/expired/cancelled, already-signed-in-matching-email (direct accept), already-signed-in-different-email (blocked with a clear message), and not-signed-in-with/without-an-existing-account (sign-in-then-accept vs. create-account-then-accept). `proxy.ts`'s route-protection middleware required a fix in passing: it only special-cased `/sign-in` as reachable while signed out, so an anonymous invitee following a real link was bounced straight to `/sign-in` before the invitation page could render — fixed by adding `/accept-invitation` as reachable both signed out (a brand-new invitee has no session yet) and signed in (accepting a second invitation, or an already-registered invitee), unlike `/sign-in` which still redirects an authenticated visitor away.

**Focused verification.** `npx vitest run lib/domain/member-invitation-commit.test.ts`: 13/13 — invitation create/duplicate-reject/resend-token-rotation/cancel, accept (wrong-email rejection, expired rejection, idempotent second accept, re-invite-reactivates-a-deactivated-member-with-the-new-role), self-deactivation block, last-admin-guard rejection (with the target membership confirmed untouched by the rolled-back transaction), deactivate/reactivate with access-loss confirmed via `list_workspace_member_identities_authorized` (not the raw table, per the RLS note above), double-toggle rejection, and cross-workspace isolation. Two of the author's own test bugs were caught and fixed during this pass, not product bugs: an invitee's Supabase Auth account was being created under one random email while the invitation and sign-in attempt used a different one (fixed with a `createUserWithEmail` helper); and two deactivation assertions checked raw `workspace_memberships` row visibility, which the pre-existing self-select RLS policy deliberately preserves regardless of deactivation (fixed by asserting against the actual enforcement boundary instead). `npx playwright test tests/e2e/member-invitation-lifecycle.spec.ts`: 3/3 — a brand-new person invited, creating an account and joining through the real UI; an existing Kinema user invited, signing in to accept through the real UI; and deactivate-via-UI blocking access immediately (`/no-workspace`) with reactivate-via-UI restoring it. The migration itself required one fix before it would apply: Postgres rejected `create or replace function list_workspace_members_with_roles_authorized` outright (`cannot change return type of existing function`) because the new `deactivated_at` output column changes its row type, which `create or replace` cannot do in place — fixed with an explicit `drop function if exists ... ;` immediately before the redefinition.

The Phase 8E.1 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. Full `npm run test:unit`: 155/155 (13 new plus 142 existing), with the same pre-existing, unrelated `record-import.*` `afterAll` timeout under full-suite concurrency as prior phases, reconfirmed clean in isolation. Full `npm run test:e2e`: 209 passed, 1 failed, 6 did not run (serial-mode siblings skipped), in 22.7 minutes — the failure (`record-updated-transition-conditions.spec.ts`, a post-submit heading-visibility timeout) matched the established under-full-serial-load signature in a file untouched by this phase, reconfirmed clean via isolated rerun (9/9). All 3 `member-invitation-lifecycle.spec.ts` tests passed within the full run. Zero deterministic defects.

**Files changed:** `supabase/migrations/0067_member_invitation_lifecycle.sql`, `lib/supabase/admin.ts`, `lib/auth/workspace.ts`, `lib/domain/workspace-role-repository.ts`, `lib/domain/workspace-invitation-repository.ts`, `app/workspace-role-actions.ts`, `app/workspace-invitation-actions.ts`, `app/(app)/settings/page.tsx`, `app/components/workspace-role-management.tsx`, `app/components/accept-invitation-forms.tsx`, `app/accept-invitation/page.tsx`, `proxy.ts`, `supabase/README.md`, `lib/domain/member-invitation-commit.test.ts`, `tests/e2e/member-invitation-lifecycle.spec.ts`.

### Phase 8E.2 — Impersonation ("Log in as")

**Scope.** Same-workspace admin impersonation for `records.operate` and `processes.operate`: an admin with the new `workspace.impersonate_users` capability can act as another active member, with worker actions (record CRUD, process step completion, approval decisions) genuinely executing and being attributed as that member — not merely a UI relabel. Governance/builder capabilities (`workspace.manage_*`, `schema.manage`, `automation.manage`) are deliberately out of scope: they remain bound to the real actor throughout, and Configure is unconditionally hidden (both from nav and direct URL) while impersonating.

**Architecture: DB-derived effective identity, not session-swapping and not a client-supplied identity.** `auth.uid()` comes straight from the request's JWT and cannot be changed by a cookie, header, or RPC parameter — so this design doesn't try to make Postgres believe the caller is someone else. Instead, `private.current_effective_user(workspace_id)` (new, `SECURITY DEFINER`) resolves `auth.uid()` normally, or the target of a valid, DB-verified `impersonation_sessions` row when one exists for the real, literal caller — falling back to `auth.uid()` the instant the target is no longer an active member, even before anything has explicitly ended the session. A small, explicitly curated set of capability checks and RPCs opt into this; nothing else in the schema can be affected, because nothing else calls it. Two new sibling functions, `private.is_workspace_member_as`/`private.has_workspace_capability_as`, take an explicit user id — deliberately *not* an in-place extension of `private.is_workspace_member`/`private.has_workspace_capability` (those are depended on by dozens of RLS policies and RPC bodies; Postgres can't widen a function's parameters via `create or replace` without either a silently-coexisting overload or a `DROP` that fails outright with so many dependents in place). Every pre-existing call site of the original two functions is untouched, byte for byte.

**What actually changed, concretely bounded.** Three RLS policies (`entity_records_operate_write`, `relation_values_operate_write`, `entity_views_operate_write` — the only capability-gated *raw* write policies in the schema; record archive/restore and saved-view CRUD go through these directly, not a wrapper RPC) now check the effective user. Roughly a dozen thin `_authorized` wrapper RPCs (record create/update/delete, bulk CSV import, and the five `processes.operate` wrappers) had their one capability-check line swapped for the effective-identity variant — no signature change. Exactly two functions have real per-row identity-equality logic touched: `complete_process_step_run_authorized_member`'s assignee check and `decide_process_approval_authorized_member`'s assignee check/`decided_by`/actor attribution, both now resolving the effective user instead of `auth.uid()` directly. `start_process_run_authorized_member`'s signature was already an explicit `p_actor_user_id` parameter (from 8D.3) — only what the wrapper passes in changed.

**Audit semantics.** `workspace_events.actor_user_id` keeps meaning the effective/operational actor everywhere, unchanged for every existing and future event. New nullable `real_actor_user_id` is populated only by `process_started` and `approval_decided` when genuinely impersonating — no broader CRUD audit coverage is claimed than existed before (most mutations still emit no durable event, impersonated or not). Session start/end are themselves durably audited via new `impersonation_started`/`impersonation_ended` event types.

**Session lifecycle.** `impersonation_sessions` (workspace-scoped, one open session per admin — starting a new one ends the prior) is RLS-closed to everyone except `real_actor_user_id = auth.uid()` reading their own rows. The app calls one RPC, `get_active_impersonation_authorized()`, on every request; it self-heals — if the target has been deactivated, it ends the session and returns empty in the same call, so `resolveImpersonationContext()` (`lib/auth/impersonation.ts`) can treat empty as definitive and clear/resync the cookie without a second round trip. The banner cannot keep showing a session that has stopped conferring authority. Switching workspace and signing out both proactively end any open session.

**Capability default.** `workspace.impersonate_users` is backfilled onto the built-in "Workspace administrator" role specifically because that role's own seed comment documents it as *"Compatibility role with full workspace access"* — a stated invariant this migration preserves. Custom roles never receive it automatically.

**Two real bugs found and fixed during verification, not hedged past.** (1) `get_active_impersonation_authorized`'s self-heal branch had an ambiguous `workspace_id` reference — its own `returns table` column shadows the table column, the identical bug class already fixed once for `bulk_create_entity_records_authorized` in migration `0062`. Confirmed directly (every call failed with Postgres 42702); fixed in `0069`. (2) `bulk_create_entity_records_authorized`'s capability-check swap was written by reconstructing the function from a partial read rather than copying it in full, and the reconstruction silently dropped `entity_records.import_batch_id` and `entity_record_relation_values.source_entity_type_id`/`target_entity_type_id`, and invented a `record_import_batches.completed_at` column that has never existed — every relation-import and completed-batch path broke. Caught immediately by `record-import.commit.test.ts` (5/6 of its tests failed); fixed in `0070` by restoring the exact original body from `0062` verbatim, with only the intended line changed. Both are logged here as a reminder that "reproduce a function's current body exactly" means a full read, every time — a partial one is a guess wearing a citation.

**Disclosed V1 gaps, not silently expanded past approved scope.** UI capability-gating (which buttons render) still reflects the real actor's own capabilities, not the effective user's, outside of Configure's hard hide — attempting a `records.operate`/`processes.operate` action the effective user lacks is always correctly refused server-side regardless, so this is a UX-completeness gap, not a security one. Notifications are not yet effective-user-aware (they're a raw RLS-gated table read, not an RPC, so making them so is a larger, deliberately deferred change); My Work is (`lib/domain/process-repository.ts`'s `listMyWorkItems` now accepts an explicit `effectiveUserId`, still always server-resolved, never client-supplied).

**Focused verification.** `npx vitest run lib/domain/impersonation-commit.test.ts`: 15/15 — session lifecycle (capability required, self/non-member/deactivated/cross-workspace target rejection, one-open-session-per-admin, self-heal on mid-session deactivation), `records.operate` enforcement at both the RLS and RPC layer evaluating the effective user, the `schema.manage` real-actor-bound scope boundary demonstrated directly (the real admin's own capability still governs during impersonation), process step completion/approval gated by the effective user's assignment, `real_actor_user_id`/`actor_user_id` attribution present only when actually impersonating, and role-editor acceptance of the new capability. A first version signed in fresh per test and reliably hit Supabase's own auth rate limiter (15+ sign-ins per run); fixed by caching one shared authenticated admin client for the whole file, the same pattern `search-commit.test.ts` already established for the identical reason. `npx playwright test tests/e2e/impersonation.spec.ts`: 1/1 — Log in as → banner → Configure hidden from nav and blocked on direct `/settings` navigation → exit → everything restored.

The Phase 8E.2 milestone gate ran `lint` → `tsc --noEmit` → `build -- --webpack` → the full E2E suite, in that order; the first three were clean. Full `npm run test:unit`: 158/158 once Supabase's auth rate limiter (heavily exercised by this session's own back-to-back test runs) had cooled down — the same established `record-import.*` `afterAll` timeout under full-suite concurrency reconfirmed clean in isolation, plus several rate-limit failures across unrelated files (`notification-commit.test.ts`, `member-invitation-commit.test.ts`) reconfirmed clean once retried in isolation rather than competing with a concurrently-running E2E suite for the same auth quota. Full `npm run test:e2e`: 198 passed, 3 failed, 16 did not run (serial-mode siblings skipped), in 20.1 minutes — all 3 failures (`analytics.spec.ts` rate-limited sign-in, plus the same established `record-updated-transition-conditions.spec.ts`/`update-record-workflows.spec.ts` post-submit-timeout signature as prior phases) reconfirmed clean via one isolated rerun (26/26, 5.1 minutes). All impersonation and 8E.1 invitation-lifecycle E2E tests passed within the full run. Zero deterministic defects.

**Files changed:** `supabase/migrations/0068_impersonation.sql`, `0069_impersonation_ambiguous_column_fix.sql`, `0070_bulk_import_impersonation_fix.sql`, `lib/auth/capabilities.ts`, `lib/auth/impersonation.ts` (new), `app/impersonation-actions.ts` (new), `app/components/impersonation-banner.tsx` (new), `app/components/workspace-role-management.tsx`, `app/(app)/layout.tsx`, `app/(app)/settings/page.tsx`, `app/(app)/my-work/page.tsx`, `app/auth-actions.ts`, `lib/domain/process-repository.ts`, `lib/domain/impersonation-commit.test.ts`, `tests/e2e/impersonation.spec.ts`.

### Phase 8E.3 — Workspace Health

**Scope.** A single admin-facing page (`/settings/health`, gated on `workspace.manage_settings`) surfacing five deterministic structural findings, computed live from one read-only RPC — no persisted findings table, no health score, no AI assessment, no acknowledgement/snooze workflow, no background scanner, no auto-fix. Every finding is explainable, reproducible, and links directly to where it can be fixed.

**The five checks**, refined from their initial sketch by reading the actual schema and existing code rather than assuming:
1. **`no_active_fields`** (`needs_attention`) — an active entity type with zero active fields; nothing can be recorded against it.
2. **`missing_display_field`** (`worth_reviewing`) — an active entity type with at least one active field but no active text field. Traced `getRecordLabel`'s real fallback chain (`record-repository.ts`): a configured-but-broken display field is silently absorbed by the fallback-to-first-active-text-field logic, so "configured display field itself invalid" was dropped as a separately-checked condition — it never independently changes the outcome. Deliberately mutually exclusive with check 1 (this one requires *at least one* active field; zero active fields is check 1's territory alone).
3. **`recurrence_unreachable`** (`needs_attention`) — an active recurrence rule that can structurally never fire again: its process template is archived, its origin record is archived, or (added after inspection, approved) its `end_date` has already passed. The first two conditions are the literal inverse of the existing scheduler's own discovery join (`discover_and_start_recurrence_occurrences_system`, `0063`) — a rule outside that join was already silently dead, this just surfaces it.
4. **`stuck_process_run`** (`needs_attention`) — an active process run with no active or pending step; every step is completed/skipped yet the run never transitioned to `completed`. Purely structural, no age/staleness inference.
5. **`deactivated_assignee`** (`needs_attention`) — an active `human_task`/`approval` step whose snapshotted assignee (8D.2's deliberately un-FK'd historical column) is now a deactivated member (8E.1). Scoped to `status = 'active'` only for V1; a deactivated assignee on a not-yet-reached `pending` step is a real but lower-urgency latent issue, deferred.

**Result shape and check-type vocabulary.** `list_workspace_health_findings_authorized(p_workspace_id)` is one `plpgsql` function whose body is a fixed five-branch `UNION ALL`, each branch hardcoding its own `check_type` literal — the bounded vocabulary is enforced by this fixed shape plus a matching TypeScript literal union (`lib/domain/workspace-health-repository.ts`), not a DB `check` constraint, since findings are never persisted to a table there is nothing to constrain. Returns raw ids/labels only (`entity_type_id`/`record_id`/`process_run_id`/`process_step_run_id`/`member_email` plus human `title`/`detail` text) — turning a finding into a destination URL is a presentation concern, handled entirely by one `fixHrefFor` switch in the repository, not the RPC.

**Authorization.** `private.require_workspace_capability(p_workspace_id, 'workspace.manage_settings')` — real-actor-bound like every other governance capability (8E.2), not effective-user-aware. The page itself hard-blocks direct URL access exactly like `/settings` already does during impersonation, reusing that same infrastructure rather than inventing new impersonation-awareness.

**Focused verification.** `npx vitest run lib/domain/workspace-health-commit.test.ts`: 18/18 — one true-positive and one true-negative per check (including all three `recurrence_unreachable` branches independently), the archived-entity-type and inactive-rule exclusion guards, capability rejection, cross-workspace rejection, and a from-scratch workspace with only healthy fixtures returning zero findings. `npx playwright test tests/e2e/workspace-health.spec.ts`: 3/3 — a builder sees grouped findings with a working Fix link; a worker is denied via both nav-absence and direct URL, and the same denial holds for an impersonating admin (Configure already hidden, this page hard-blocked too); a healthy workspace shows "Everything looks healthy." Two real test-fixture bugs were caught and fixed during this pass, not product bugs: `field_definitions.key` turned out to be unique per *workspace*, not per entity type (unchanged since migration `0001`) — reusing a literal key like `"name"` across multiple test entity types in one workspace fixture violated that constraint, fixed with a deterministic per-entity-type key suffix; and the auth E2E test originally reused one browser page for two different personas' sign-ins, which hung indefinitely once the second `signIn()` call hit `/sign-in` while already authenticated (the existing middleware redirects an authenticated visitor away from it) — fixed by using separate browser contexts per persona, the same pattern every other multi-persona spec in this codebase already uses.

**Files changed:** `supabase/migrations/0071_workspace_health.sql`, `lib/domain/workspace-health-repository.ts` (new), `app/(app)/settings/health/page.tsx` (new), `app/components/app-header.tsx`, `app/(app)/layout.tsx`, `lib/domain/workspace-health-commit.test.ts`, `tests/e2e/workspace-health.spec.ts`.

### Phase 8F.1 — CSV Export

**Scope.** One business object at a time, entered from the same collection experience as import (a new "Export CSV" link on `entity-views-panel.tsx`, immediately after "Import CSV"). Active records and active fields only, in the same canonical field position order the table/form already use — deliberately symmetric with import's own scope, not a broader export feature. Membership-level read authorization only (no new capability, `records.operate` not required — export is a read of data already visible in the table, and this app's model already treats reads as membership-wide with capability gates reserved for writes). No migration, no pagination/streaming/background job, no column-selection UI, no archived-record toggle.

**Architecture.** `app/(app)/entities/[entityTypeId]/export/route.ts` is a Route Handler, not a Server Action — mirrors import's own directory-segment convention (`.../[entityTypeId]/import/`) and lets a plain `<a href>` trigger a native browser download via `Content-Type`/`Content-Disposition` headers, no client JS needed. `lib/domain/record-export.ts` deliberately reuses import's own read paths rather than inventing new ones: `getEntityContext` and `listEntityRecords` (already merges relation values into `record.values[fieldKey]` as raw target-record UUIDs) from `metadata-repository.ts`/`record-repository.ts`. A relation cell's exported value is the target's normal resolved label via `getRecordLabel` — the same function every other read surface uses — never a raw id and never decorated with the UI-only "(Archived)" suffix `getRelationOptionLabel` adds elsewhere. `csv-stringify/sync` (the Adaltas sibling of the already-used `csv-parse/sync`) does RFC 4180 serialization, with two options required beyond its defaults: `record_delimiter: "\r\n"` (the library defaults to a bare `\n`), and `quoted_match: /[\r\n]/` (once a non-default delimiter is set, the library's own quoting heuristic only recognizes the literal `\r\n` sequence as a reason to quote a cell — a cell containing a bare `\n` would otherwise be written unquoted, corrupting row structure). Both were caught by direct unit tests against the real output, not assumed. Field formatting mirrors import's own parsing exactly for round-trip fidelity: booleans as literal `"true"`/`"false"` (the canonical form within import's accepted vocabulary), dates as the stored `YYYY-MM-DD` string unchanged, null/undefined as an empty cell (matching import's own `"" → null` symmetry). Filename: `${entityType.slug}-${YYYY-MM-DD}.csv`.

**Round-trip guarantees**, scoped to only what import already supports, not asserted more broadly: primitive fields (text/number/boolean/date) round-trip exactly. An active relation target with a unique matching label round-trips. An archived relation target's label is exported undecorated (identical to what it would be if active) and IS found by label on re-import — `resolveRelationValues` matches against active and archived targets alike — but is then explicitly rejected as an `{status: "archived"}` import error, not silently dropped and not "not found"; this is a real, documented import/round-trip limitation, not a defect. An ambiguous (non-unique) relation label remains an import error, unchanged from import's existing behavior.

**Focused verification.** `npx vitest run lib/domain/record-export.test.ts`: 10/10 — header row uses field names in position order; text/number/date pass through unchanged; booleans export as literal `true`/`false`; null/undefined export as empty string for every field type; an already-resolved relation label passes through as-is; three RFC 4180 escaping cases (comma, embedded quote doubled, embedded newline) exercised through the same `stringifyExportTable` function production code calls, not a duplicated options object. `npx playwright test tests/e2e/record-export.spec.ts`: 2/2 — a worker without `records.operate` can still export CSV, with archived records and archived fields both excluded from the output (neither the archived field's header nor either archived value's content appears anywhere in the CSV), plus cross-workspace isolation (a real entity type in a different workspace 404s, not distinguishable from a nonexistent id); and a full export → re-import round trip through the real browser UI (primitive fields plus an active relation via unique label match), confirming every value `csv-stringify` produced is independently valid input to the unmodified, existing import parser and relation resolver.

One test-assertion bug was found and fixed during this pass, not a product bug: the round-trip test initially asserted `page.getByText(/error/i)` had zero matches after upload, but the preflight summary line unconditionally renders "N rows total — N ready — 0 with errors" regardless of whether any row actually failed (`record-import-flow.tsx:379`), so the assertion false-positived on its own success state. Fixed by asserting the Import button is enabled instead — the same signal `record-import.spec.ts`'s own mixed-validity test already relies on, and consistent with the happy-path import test's pattern of proving a clean import via the eventual "N rows imported" message rather than a generic error-absence check.

Full unit suite (`npm run test:unit`) showed 10 pre-existing, unrelated failures (`member-invitation-commit.test.ts`, `notification-commit.test.ts`, `search-commit.test.ts`, `activity-commit.test.ts`, `recurrence-commit.test.ts`) all failing on `Request rate limit reached` from local Supabase Auth's `signInWithPassword` — reproduced identically running `member-invitation-commit.test.ts` in isolation, confirming this is cumulative local-auth rate-limiting from the volume of sign-ins across this session's testing, not a regression from this change. `record-export.test.ts` and every other file were unaffected (176/186 passing). `npx playwright test tests/e2e/record-import.spec.ts` (4/4) was rerun to confirm the shared import path has no regression from export's changes. `lint`, `tsc --noEmit`, and `next build` were all clean, with the new `/entities/[entityTypeId]/export` route registering correctly in the build's route table.

No scale/performance concern was observed or expected: export runs one bounded read per object (plus one bounded read per distinct related entity type for relation label resolution) and returns a single response, no different in shape from any other page-level `listEntityRecords` call already in this codebase.

**Files changed:** `lib/domain/record-export.ts` (new), `lib/domain/record-export.test.ts` (new), `app/(app)/entities/[entityTypeId]/export/route.ts` (new), `app/components/entity-views-panel.tsx`, `tests/e2e/record-export.spec.ts` (new), `package.json`/`package-lock.json` (added `csv-stringify`).

### Phase 8F.2 — Outbound Webhooks

**Scope.** Deliver a bounded subset of the existing `workspace_events` vocabulary (`process_started`, `process_completed`, `approval_decided`, `step_assigned` — deliberately excluding `recurrence_started_process` and the notification-only `step_due_soon`/`step_overdue`) to admin-configured external HTTPS endpoints, reliably and inspectably. No integration marketplace, no OAuth apps, no payload mapping builder, no custom headers beyond signing, no delivery dashboard/charts — a plain create/edit/disable UI and a plain recent-delivery log under Configure → Webhooks (`/settings/webhooks`).

**A dedicated capability, not reused.** `workspace.manage_integrations` (migration `0073`), backfilled onto every workspace's built-in "Workspace administrator" role only (the same mechanism `0068` used for `workspace.impersonate_users`, preserving that role's documented full-access invariant) — never onto custom roles automatically. Chosen deliberately over reusing `workspace.manage_settings`: webhook management transmits workspace data externally and controls external secrets/endpoints, a materially different trust boundary than ordinary workspace configuration, and one 8F.3's API keys (and later connectivity surfaces) are expected to reuse rather than each minting their own capability. Real-actor-bound and blocked while impersonating, matching every other `workspace.manage_*` capability.

**Schema.** `webhook_subscriptions` (id, workspace_id, name, url `check (url ~ '^https://')`, `event_types text[]`, active, `signing_secret` plaintext, `secret_preview` last-4-chars, created_by, created_at) and `webhook_deliveries` (id, workspace_id, subscription_id, event_id, status, attempts, next_attempt_at, last_attempted_at, last_response_status, last_failure_summary, created_at, **`unique(subscription_id, event_id)`**). Both fully closed at RLS (no policies at all, same posture as `workspace_events`) — every access goes through `_authorized`/`_system` RPCs.

**Fan-out is a trigger, not a call-site change.** `private.enqueue_webhook_deliveries()`, an `AFTER INSERT` trigger on `workspace_events`, mirrors `entity_records_process_condition_wait_wakeup` (`0038`) exactly: zero changes to any of the ~7 existing event-emission call sites, transactionally atomic with the event insert, `on conflict (subscription_id, event_id) do nothing` for idempotency. Only `active` subscriptions whose `event_types` contains the new event's type get a row — a subscription inactive at event time never gets a delivery for it even if later reactivated (no historical backfill, consistent with Activity/notifications).

**Dispatcher.** New, separate route `app/api/internal/webhook-deliveries/route.ts` (not folded into `process-waits` — a different failure/timeout profile), sharing only its bearer-secret check via a new `lib/scheduler-auth.ts` helper (`process-waits` refactored onto the same helper; each route keeps its own env var, `PROCESS_WAIT_SCHEDULER_SECRET` and `WEBHOOK_DISPATCH_SCHEDULER_SECRET`, so they rotate independently). `claim_due_webhook_deliveries_system` mirrors `resume_due_process_waits_system`'s `FOR UPDATE SKIP LOCKED` idiom but adapted to *return* rows to the caller rather than do the work in SQL, since an outbound HTTP call can't happen inside Postgres: each claimed row is leased by pushing `next_attempt_at` forward 2 minutes as part of the claim, so a crashed dispatcher self-heals once the lease expires rather than leaving a stuck row — no separate "claimed" status exists. The route delivers a claimed batch (limit 25) concurrently via `Promise.allSettled` (bounding wall time to ~10s rather than 25×10s sequential) and reports each outcome through `record_webhook_delivery_attempt_system`, which either marks `succeeded`, marks `failed` (terminal — a deterministic 4xx or `attempts >= 6`), or advances `next_attempt_at` by a fixed backoff (1m/5m/30m/2h/6h/24h). Retry policy lives in app code (`lib/domain/webhook-dispatch.ts`, pure and unit-tested): retry transport errors, timeouts, 429, and 5xx; terminal on any other non-2xx, including a 3xx (redirects are never followed — `redirect: "manual"`, empirically confirmed that Node's fetch returns the real status code rather than an opaque response for a same-process server-side call).

**Signing.** Secrets are 32 random bytes from Node's CSPRNG (not `pgcrypto`, which this project has never depended on), stored plaintext with RLS fully closed — `pgcrypto` envelope encryption is recorded as accepted production-hardening debt in the migration's own header, not implemented in V1. Shown once on create/regenerate, never re-selectable afterward; only a 4-character preview persists. Stripe-style HMAC-SHA256 over `${timestamp}.${body}`, sent as `X-Kinema-Signature`/`X-Kinema-Timestamp`/`X-Kinema-Delivery-Id`/`X-Kinema-Event-Type`. A delivery always signs with whatever secret is live on its subscription **at claim time**, never one snapshotted at enqueue time, so a rotation takes effect on a retry's very next attempt, not just on new deliveries.

**SSRF protection** (`lib/domain/webhook-url-safety.ts`) uses `ipaddr.js` rather than hand-rolled range parsing. `https://` only; blocks `localhost`/`.local`/`.internal`/`.localhost` hostnames outright; resolves the hostname and inspects **every** returned address via `ipaddr.process(address).range() === "unicast"` — a fail-closed allowlist (private/loopback/link-local/unique-local IPv6/reserved/multicast/carrier-grade-NAT/etc. all rejected), with IPv4-mapped IPv6 correctly normalized before classification. Re-checked both at subscription create/edit time and immediately before every delivery attempt (fresh DNS each time, not cached, so a destination that stops being safe between retries stops being delivered to).

**Deletion/archive.** No delete action exists in the UI at all — only create, edit, and an active/inactive toggle, sidestepping the delivery-history-loss question rather than picking a lossy default. Deactivating a subscription also pauses its already-queued retries (the claim query requires `active = true`), not just future enqueueing, so "disable" reads as "stop hammering this endpoint" while the delivery log stays intact either way. `webhook_deliveries`' cascade-on-subscription-delete FK only ever fires as part of a whole-workspace deletion.

**Focused verification.** 50 pure unit tests (`webhook-url-safety.test.ts` 17, `webhook-signing.test.ts` 7, `webhook-payload.test.ts` 5, `webhook-dispatch.test.ts` 11, `record-export.test.ts` 10 reconfirmed unaffected) covering every important SSRF address class (IPv4 private/loopback/link-local/CGNAT/reserved/multicast, IPv6 loopback/unique-local/link-local, IPv4-mapped IPv6, a mixed public+private multi-address response), signature determinism, payload ref-omission, and the full retry/terminal classification matrix — all pass without touching the database. `lib/domain/webhook-delivery-commit.test.ts` (12 DB/RPC tests: capability backfill onto the built-in role, subscription CRUD with capability and cross-workspace rejection, invalid-event-type rejection, fan-out event-type filtering and inactive-subscription exclusion, the `unique(subscription_id, event_id)` constraint, claim+lease with no immediate reclaim, inactive-subscription exclusion from claiming, and success/retryable/terminal/max-attempts-exhaustion outcomes) — 12/12 passing against the applied migration, reconfirmed in isolation after a full-suite run reproduced this session's already-documented local Supabase Auth rate-limiting (`Request rate limit reached` on `signInWithPassword`, now touching more files than earlier in the session purely from cumulative sign-in volume across everything already run) — not a regression, matching the same pre-existing environmental constraint noted under 8F.1.

**Dogfood.** End-to-end against a real public HTTPS receiver (`httpbin.org/anything`, chosen since a real outbound webhook cannot target `localhost` by design): signed in as `dogfood.builder`, created a subscription through the actual `/settings/webhooks` UI, confirmed the SSRF check live-rejects a plain `http://` URL with its friendly message before ever reaching the database, confirmed the secret-shown-once reveal and the 4-character preview match, drove a synthetic `workspace_events` insert through the real trigger → claim → `deliverClaimedWebhookDelivery` → record path, and confirmed the delivery log rendered "Succeeded, 1 attempt, 200" after a reload. Independently recomputed the HMAC signature from the receiver's own echoed headers and body outside the application entirely and confirmed it matched exactly — proving the signing scheme is receiver-verifiable, not just self-consistent. All dogfood fixtures (subscription, synthetic events) were torn down afterward.

**Files changed:** `supabase/migrations/0073_webhook_deliveries.sql` (new), `lib/domain/webhook-url-safety.ts`/`.test.ts` (new), `lib/domain/webhook-signing.ts`/`.test.ts` (new), `lib/domain/webhook-payload.ts`/`.test.ts` (new), `lib/domain/webhook-dispatch.ts`/`.test.ts` (new), `lib/domain/webhook-events.ts` (new), `lib/domain/webhook-repository.ts` (new), `lib/domain/webhook-delivery-commit.test.ts` (new), `lib/scheduler-auth.ts` (new, and `app/api/internal/process-waits/route.ts` refactored onto it), `app/api/internal/webhook-deliveries/route.ts` (new), `app/webhook-actions.ts` (new), `app/components/webhook-subscriptions-panel.tsx` (new), `app/(app)/settings/webhooks/page.tsx` (new — renamed to `settings/integrations` in 8F.3 below), `lib/auth/capabilities.ts`, `app/components/workspace-role-management.tsx`, `app/components/app-header.tsx`, `app/(app)/layout.tsx`, `package.json`/`package-lock.json` (added `ipaddr.js`).

### Phase 8F.3 — Read-only API Foundations

**Scope.** A small, versioned, bearer-key-authenticated read surface — `GET /api/v1/objects`, `GET /api/v1/objects/:entityTypeId`, `GET /api/v1/objects/:entityTypeId/records`, `GET /api/v1/objects/:entityTypeId/records/:recordId` — plus workspace-scoped API key issuance/revocation under the renamed `Configure → Integrations` page (previously "Webhooks," now hosting both webhook subscriptions and API keys, sharing `workspace.manage_integrations`). No writes, no OAuth, no per-user tokens, no marketplace, no GraphQL, no usage dashboard.

**The core architectural invariant, driven by review before implementation began.** An API key is an external authorization principal, not trusted system authority — a bug in a route handler must not be able to turn service-role possession into a cross-workspace read. Every externally-reachable RPC takes `p_key_hash`, never a workspace id; the workspace is derived exclusively from the active, non-revoked key row that hash resolves to (`private.resolve_api_key_workspace`), and every requested object/record id is constrained against that derived workspace inside the function itself — never passed in and trusted. Route handlers issue no direct table reads at all; every read is exactly one RPC call, so there is no `.eq("workspace_id", ...)` anywhere in `app/api/v1/**` for a future change to get wrong.

**Schema.** `api_keys` (id, workspace_id, name, `key_hash` unique, `key_preview`, `scopes text[] default {records:read}`, created_by, created_at, last_used_at, revoked_at — `revoked_at` is the sole lifecycle flag, no separate boolean) and a deliberately separate `api_key_rate_limits` (api_key_id, window_started_at, request_count) — kept off the credential row so a high-frequency counter never contends with or bloats the security-sensitive table. The rate-limit row is created transactionally alongside the key. Every new function in both migrations uses `set search_path = ''` with fully-qualified references — a tighter posture than this codebase's established `public, pg_temp` convention, warranted because this is the one surface an external, non-interactive credential drives end to end.

**Hashing.** SHA-256 of a `kinema_live_<64 hex>` key (Node's CSPRNG) — not bcrypt/argon2, which exist to slow-brute-force a low-entropy human password; a 256-bit random secret has no such exposure, and a fast hash is what makes an indexed equality lookup practical. The raw key is generated and hashed entirely client-side in the app layer; only the hash ever reaches a SQL parameter.

**Scope model.** Every V1 key is issued with the fixed scope `records:read`, not user-selectable — deliberately not a granular picker, since there's nothing to choose between yet (interactive membership read is already ungated by capability everywhere else in this app). The `scopes text[]` column and the `'records:read' = any(scopes)` check inside every data RPC exist specifically as the seam a future write scope slots into without restructuring; an existing V1 key can never silently gain one.

**Rate limiting: a two-call design, not one.** A first draft raised an exception for "over limit" inside the same transaction as the counter update — caught before application, since that rolls the counter back along with everything else, defeating its own purpose. Fixed by splitting into `check_api_key_rate_limit_for_api_key` (called first, always awaited on its own; its `last_used_at`/counter mutation commits whether or not the caller is over limit, since it never raises for that case — only for a truly unknown/revoked key, where nothing has been written yet) and the four data RPCs, which independently re-derive their own workspace from the key rather than trusting the rate call's prior resolution. 60 requests/60 seconds per key, `429` with `Retry-After: 60`.

**Pagination.** One consistent contract for both list endpoints (`/objects`, `/objects/:id/records`): keyset pagination on `(created_at, id)` ascending, and — after review flagged that "a full page came back" is indistinguishable from a genuinely last page whose count is an exact multiple of the limit — every list RPC now selects `limit + 1` rows internally. A pure helper, `buildApiPage` (`lib/domain/api-cursor.ts`), trims the response to at most the requested limit and emits `nextCursor` only when that lookahead row actually exists.

**Response shape.** Field keys in `values` are the internal `key`s, not display names — a deliberate divergence from 8F.1's CSV export, since an API is code-facing. A relation field resolves to `{id, label}`: the raw id for a future write endpoint to accept back, the label so a consumer doesn't need a second round trip just to display something. `private.api_record_label` mirrors `getRecordLabel`/`getRecordIdentityField` (`lib/domain/record-repository.ts`) exactly — same display-field-then-fallback-field-then-shortened-id chain, same empty/whitespace-only-value handling — reproduced in SQL rather than called from SQL, since duplicating this one well-scoped, stable piece of logic was the smaller cost next to either breaking the tenant-isolation invariant or building a second RPC hop just to avoid it.

**Two real bugs found by testing against the live database, both fixed before this phase closed:**
1. Relation values were read from `entity_records.values`, which never contains them — Kinema's canonical persistence keeps primitive values there and relation values exclusively in `entity_record_relation_values`; the ordinary interactive repository (`listEntityRecords`) merges the two only on read, in Node. Caught by a DB-level test whose fixture, on inspection, had made the identical wrong assumption the RPC did. Both record-serving RPCs now overlay resolved relation values (via `entity_record_relation_values`) onto the primitive `values` jsonb rather than assuming a relation's raw target id was ever stored inside it.
2. A record's `values` passed the raw stored jsonb through with only relation values overlaid — an archived field's value, still sitting in that jsonb from before it was archived, leaked into the response even though `get_object_for_api_key`'s own field list correctly excluded it. Caught by the same commit test, live, after 0074 had already been applied — fixed in a follow-up migration (0075, since editing an applied migration in place isn't safe) that rebuilds `record_values` by projecting onto the entity type's currently-active field set for every record, rather than passing stored data through unfiltered. Every active field's key is now always present (primitive or relation), `null` when unset, matching a complete, predictable contract rather than "whatever happened to be in the stored jsonb."

**A third bug, unrelated to the API/webhook code itself, found by the first real (non-Playwright-fixture) HTTP call to any bearer-authenticated route in this project.** `proxy.ts`'s site-wide auth middleware redirected every unauthenticated request to `/sign-in` — including `/api/v1/*` and `/api/internal/*`, neither of which has or needs a session cookie by design. Both bearer-token route families (webhook/scheduler dispatch from 8F.2, and this phase's API) were silently non-functional over real HTTP the entire time; every prior test of them either called their underlying functions directly in-process or (for this phase's own first E2E draft) unintentionally rode along on a session cookie inherited from Playwright's global `storageState`, which also applies to the `request` fixture and made that first draft pass without actually exercising a cookie-less caller. Fixed by excluding `api/v1` and `api/internal` from the middleware's route matcher; the E2E spec was corrected to use a genuinely cookie-less `APIRequestContext` (`playwright.request.newContext()`, no inherited storageState) for its actual API assertions, specifically so this class of bug can't hide behind the fixture again.

**Focused verification.** 16 pure unit tests (`api-key-signing.test.ts` 6, `api-cursor.test.ts` 10 including 5 for `buildApiPage`'s over-fetch/trim logic) — no database. `lib/domain/api-key-commit.test.ts`, 9 DB/RPC tests (capability backfill reuse confirmed, key creation with hash-only storage verified directly against the row, capability and cross-workspace rejection, valid/unknown/revoked verification, the rate-limiter's counter-commits-on-rejection behavior asserted directly, both list RPCs' over-fetch-by-one confirmed at the raw-RPC level, scope enforcement via a key with emptied scopes, bounded/cursor-paginated reads with archived-record and archived-field exclusion and no cross-page overlap, relation resolution as `{id, label}` via a correctly-normalized fixture) — 9/9 passing against the live database, both post-0074 and again post-0075. New `tests/e2e/api-keys.spec.ts` (2/2): create key through the real UI with one-time secret reveal, all four endpoints exercised over genuinely cookie-less HTTP with truthful terminal pagination, relation shape, archived exclusion, foreign-workspace 404, `Cache-Control: private, no-store` on success and error responses, revoke-then-401; and a dedicated rate-limit test confirming the 429 and its durability across a follow-up call. A real product bug surfaced by that spec's revoke step: `ApiKeyRow` only rendered `RevokeButton` while `!isRevoked`, so the instant a revoke succeeded and the page revalidated with fresh server data, the component holding the "API key revoked." confirmation unmounted in the same render — the message was never actually visible. Fixed by keeping `RevokeButton` mounted regardless of revoked state and moving the conditional inside it, so its `useActionState` confirmation survives the revalidation.

**Dogfood.** Signed in as `dogfood.builder`, created a key through the real `/settings/integrations` UI, confirmed the one-time secret reveal. Confirmed the pre-fix middleware bug directly with a plain `curl` (307 to `/sign-in`) before fixing it, then confirmed the fix with the same `curl` (correct 401 JSON, correct `Cache-Control`). Exercised all four endpoints against a disposable fixture (a relation, an archived field, an archived record) via a real external script making plain `fetch` calls with the bearer key — object detail, records list with `{id, label}` relation resolution, record detail, revoke-then-401 — then confirmed the UI-created key independently over a second real `curl` call, revoked it through the UI (confirmed both the "Revoked" label and the now-persistent confirmation message), and confirmed the revoked key immediately 401s. All fixtures (disposable entities, both API keys) torn down afterward.

**Files changed:** `supabase/migrations/0074_api_keys.sql` (new), `supabase/migrations/0075_api_records_active_fields_only.sql` (new), `lib/domain/api-key-signing.ts`/`.test.ts` (new), `lib/domain/api-cursor.ts`/`.test.ts` (new), `lib/domain/api-key-auth.ts` (new), `lib/domain/api-key-repository.ts` (new), `lib/domain/api-read-repository.ts` (new), `lib/domain/api-key-commit.test.ts` (new), `app/api/v1/objects/route.ts`, `app/api/v1/objects/[entityTypeId]/route.ts`, `app/api/v1/objects/[entityTypeId]/records/route.ts`, `app/api/v1/objects/[entityTypeId]/records/[recordId]/route.ts` (all new), `app/api-key-actions.ts` (new), `app/components/api-keys-panel.tsx` (new, includes the `RevokeButton` mount-lifetime fix), `app/(app)/settings/webhooks/page.tsx` → `app/(app)/settings/integrations/page.tsx` (renamed, now hosts both panels), `app/components/app-header.tsx` (nav label/link), `app/webhook-actions.ts` (`revalidatePath` targets updated), `proxy.ts` (middleware matcher excludes `api/v1`/`api/internal`), `tests/e2e/api-keys.spec.ts` (new).

### Phase 8F.4 — Outbound Email / Provider Infrastructure

**Scope.** A narrow, deterministic outbound-email foundation for workspace invitations only. V1 uses Resend as the single provider, queues email only when deployment-level `RESEND_API_KEY`, `EMAIL_FROM`, and `KINEMA_PUBLIC_APP_URL` are configured, and keeps the existing manual invitation-link fallback in every create/resend UI response. No notification emails, comments/mentions, user preferences, marketplace/OAuth/provider framework, or generic webhook/email delivery abstraction were added.

**Security and persistence model.** Migration `0076_outbound_email_deliveries.sql` adds `workspace_invitations.email_generation_id` (backfilled with fresh UUIDs for existing rows, deliberately without historical delivery rows) and a closed `outbound_email_deliveries` ledger. Delivery rows store only workspace/invitation/generation references, recipient, status, attempt timing, response status, failure summary, provider, provider message id, and creator; they never store invitation bearer tokens, invitation URLs, rendered subjects, text bodies, or HTML bodies. Invitation create/resend RPCs now accept a server-derived `p_enqueue_email` boolean and atomically create/rotate the authoritative invitation token/generation plus the corresponding delivery row when email is configured. Resend idempotency is derived from the durable DB delivery id (`kinema-email/<delivery_id>`), never from a token or recipient-supplied value.

**Dispatcher.** `app/api/internal/email-deliveries/route.ts` is a service-only, cookie-less internal route guarded by `EMAIL_DISPATCH_SCHEDULER_SECRET`. It claims a bounded batch of pending rows via `claim_due_outbound_email_deliveries_system` (`FOR UPDATE SKIP LOCKED`, 2-minute lease), calls `prepare_workspace_invitation_email_delivery_system(delivery_id)` as the canonical final pre-send gate, renders the invitation email in memory from the authoritative current invitation, POSTs to Resend, and records the result with `record_outbound_email_delivery_attempt_system`. Status language intentionally stops at provider acceptance: a 2xx Resend response marks the row `accepted`, meaning "accepted by provider," not recipient delivery. Retry policy is email-specific: retry transport errors/timeouts, 429 (respecting `Retry-After` when practical), 5xx, and Resend's concurrent-idempotency 409; terminal on deterministic non-retryable failures or after 6 attempts.

**Concurrency boundary.** Resend/retry idempotency is durable at Kinema's delivery-row level, and `prepare_workspace_invitation_email_delivery_system(delivery_id)` refuses non-pending rows, stale generations, cancelled/non-pending invitations, and expired invitations. Resend creates a new generation/delivery; previous pending generations are marked superseded. V1 honestly accepts the tiny race after the final DB check and before/during the external provider call: an already-in-flight provider request cannot be cancelled after a concurrent resend. Since resend rotates the authoritative token, an old token is invalid after rotation; this is a UX race, not an authorization leak.

**Focused verification.** `lib/domain/email-dispatch.test.ts` and `lib/domain/email-delivery-route.test.ts` cover delivery-id idempotency keys, render-time URL construction, Resend response classification, Retry-After parsing, scheduler bearer auth, configured dispatch, stale prepare refusal, and repeated dispatcher execution after an accepted delivery. `lib/domain/email-delivery-commit.test.ts` covers the live DB/RPC layer: atomic enqueue, manual/no-delivery mode, resend generation rotation, pending-generation superseding, current-generation uniqueness, closed raw table access, `workspace.manage_members`/cross-workspace boundaries, prepare refusal for stale/expired/cancelled/superseded/otherwise invalid deliveries, bounded claim/lease behavior with concurrent claims, and accepted/retryable/Retry-After/terminal/max-attempt recording. Focused Playwright coverage (`tests/e2e/outbound-email-invitations.spec.ts`) verifies provider-disabled manual mode creates no delivery, and a dummy-configured deployment queues email, shows truthful "email queued" copy, keeps the manual fallback link visible, resends into a new generation, and prevents the old pending generation from preparing.

**Dogfood.** Signed in as the persistent `dogfood.builder` persona, created a disposable workspace so the controlled accessible Gmail recipient (`djplana@gmail.com`) was not already a member, invited that recipient through the real `/settings` UI, and confirmed the UI showed both the manual fallback link and "Invitation created and email queued. You can also share this link manually." Invoked the real internal dispatcher route over cookie-less HTTP; Resend accepted the first delivery (`status = accepted`, one attempt, provider message id present). Invoked the dispatcher again before resend and confirmed zero claimed/attempted rows and no duplicate accepted send for that delivery. Resent through the real UI, confirmed a new invitation token/generation and a second pending delivery, confirmed the old generation's `prepare_workspace_invitation_email_delivery_system` result was empty, invoked the dispatcher again, and confirmed the second delivery was accepted by provider. Gmail search against the exact recipient and subject showed exactly the two Kinema invitation emails for the dogfood workspace; the new link opened the existing-account invitation page for `djplana@gmail.com`, `get_invitation_by_token` resolved it to the disposable workspace, and the old token returned no invitation rows/rendered "Invitation not found." A first attempted plus-address dogfood recipient hit Resend's test-mode 403 recipient restriction; that failed invitation/delivery and the disposable dogfood workspace were deleted afterward. Persistent dogfood personas were preserved.

**Files changed:** `supabase/migrations/0076_outbound_email_deliveries.sql` (new), `lib/domain/email-dispatch.ts`/`.test.ts` (new), `lib/domain/email-delivery-repository.ts` (new), `lib/domain/email-delivery-commit.test.ts` (new), `lib/domain/email-delivery-route.test.ts` (new), `app/api/internal/email-deliveries/route.ts` (new), `tests/e2e/outbound-email-invitations.spec.ts` (new), plus invitation enqueue wiring/copy in `lib/domain/workspace-invitation-repository.ts`, `app/workspace-invitation-actions.ts`, and `app/components/workspace-role-management.tsx`. `lib/supabase/admin.ts`'s comment was updated to reflect the already-existing service-role scheduler usage from 8F.2 plus the new email dispatcher.

## Intentional Limitations

- Email/password Auth only. Workspace membership is now also obtainable via a shareable invitation link (Phase 8E.1) alongside direct manual-SQL provisioning; there is still no self-serve signup outside an invitation, no user-profile management, and no workspace switching administration UI. Deployment-configured invitation email exists via Resend (Phase 8F.4), but the manual invitation link remains the fallback and V1 has no other system email use cases.
- No workspace switching UI is shown unless a user has multiple memberships. There is no auth/RLS coverage for other clients or integrations beyond the app and targeted E2E harness.
- Admin impersonation (Phase 8E.2) covers `records.operate`/`processes.operate` only — governance and builder capabilities (`workspace.manage_*`, `schema.manage`, `automation.manage`) always evaluate the real actor, never the effective user, and Configure is hidden while impersonating rather than adapted. UI capability-gating outside of Configure still reflects the real actor, not the effective user (server-side enforcement is always correct regardless). Notifications are not yet effective-user-aware; My Work is. No production support mode (reason capture, richer audit history, read-only support access) — this is the internal/admin-only V1 ROADMAP describes, not the "Later" tier.
- Workspace Health (Phase 8E.3) is exactly five deterministic structural checks, computed live — no score, no AI assessment, no persistence, no acknowledgement/snooze, no scheduled scans, no email/notification alerts, no auto-fix, no duplicate-record detection, no field-level data-quality scoring, no list-page hygiene surface, no general audit browser. `deactivated_assignee` covers `status = 'active'` steps only, not `pending`.
- No ORM.
- No field type changes.
- No relation target changes after field creation.
- No field/entity delete flows that rewrite dependent data.
- No many-to-many relationships.
- No reverse relation editing; reverse relationships are derived lists with a narrow action to create one new source record prelinked to the current record.
- No relation traversal in workflows/templates/conditions or saved views.
- `update_related_record` is limited to one direct relation and one related target record; no arbitrary lookup, reverse traversal, multi-hop traversal, or multi-record update exists.
- No custom record layouts/page builder.
- No comments, attachments, activity feed, or audit UI.
- No Kanban/calendar/gallery saved-view modes.
- No fuzzy/vector search, pagination, advanced filters, or search for workflows, views, or settings.
- No workflow recursion/chaining (still true even with multiple actions per workflow: workflow-generated writes never re-trigger workflow evaluation).
- No branching, parallel actions, loops, or delays/scheduling within a workflow's action sequence — actions execute strictly in configured order.
- No "unset" sentinel for changed_from/changed_to/changed_from_to transition operands; they require concrete typed values.
- No schedules, queues, background workers, integrations, or webhooks.
- No AI configuration UI yet.
- No configurable delete/archive policies.
- Workflow execution is synchronous app-side execution.
- Workflow logs are basic execution records, not a durable audit ledger.
- No deep DB-level JSON-schema validation of individual `workflows.actions[]` elements; the DB only enforces a non-empty array, and per-action shape (action type, target, mappings) is validated entirely at the application layer. Noted as a future hardening candidate.
- Entity/field create/update RPCs remain SECURITY INVOKER, so a technical member can still use retained same-workspace raw create/update grants to bypass app-layer validation. Safe entity/field deletion and record/relation mutation bypasses are closed through authorized wrappers.
- Process Templates support `human_task`, single-person `approval`, timer `wait`, `condition_wait`, and structured parallel system nodes in a forward-only DAG. There is no loop/back-edge, generalized gateway, automated node, subprocess, comments, voting, delegation, or escalation.
- Process-step assignment is fixed (none, or exactly one current workspace member — a real authenticated Kinema user, not a user-defined Team Member entity) with no dynamic/expression/role/round-robin/group assignment, no assignment from an origin-record field, and no reassignment of an already-instantiated running step. Due dates and derived overdue attention exist; reminders, email, and notifications do not.
- Workflow process starts are limited to one configured active template on the original triggering record. There is no related/arbitrary origin, dynamic template selection, parameter mapping, scheduled/delayed start, workflow-driven step completion/cancellation/restart, or process-triggered workflow recursion.
- No process run skip/reopen/cancel/rework, and no deletion of process runs/step runs — history is permanent and unedited in v1.
- The Graph view (Process Graph 6A/6B) now supports direct add (insert-on-edge), delete (with an explicit reconnect choice where unambiguous), and reorder (button-based, guarded) — but it is still not a freeform canvas. There is no drag-to-move node positioning, no drag-to-connect/rewire edges, no layout persistence, and no moving a node across branches or into/out of a parallel region (only via delete-and-recreate).
- The Process Run Graph (6C) is read-only visualization of the run's authoritative snapshot — List remains the default view. There is no editing a template from a run, no changing a route or approval outcome after the fact, no manual runtime-state manipulation, no freeform canvas positioning, and no per-branch lane correlation for join obligations (parallel-join progress is shown as an aggregate "N of M branches" count plus each branch's own node status, not a token-to-lane mapping).
- Automated Action Nodes support one action per node (create record, update origin record, update related record, start process) — no multi-action ordering, no HTTP/webhook/email/Slack/script/AI actions, no delays or custom retry policies, and no compensation/rollback. A retry recovers a genuinely transient failure (a value that changes) but can never recover a structurally misconfigured action, since the run's step config was already snapshotted at start time and template edits never affect an existing run. Idempotency for `create_record`/`start_process` closes the specific crash window between a successful side effect and its completion record; it is not a general exactly-once guarantee for arbitrary future action types. The Workflow editor's own action-configuration UI was not retrofitted onto the new shared component — it remains its own implementation.
- Operational Analytics is scoped exclusively to the caller's current direct-report/active-led-team management scope — there is no workspace-wide analytics surface, no admin/executive rollup, and no separate capability for one. Historical attribution follows a person's *current* manager/team, never a point-in-time org snapshot, so a manager who recently inherited (or lost) reports sees that person's full history move with them; this is disclosed on the page, not hidden. There is no employee scoring, ranking, or performance/efficiency/productivity metric of any kind, by design — every figure is a raw, traceable count or median. Time periods are fixed 7/30/90-day UTC rolling windows with no custom range and no timezone alignment. There is no SLA/business-calendar concept, no saved analytics views, no export, and no AI-generated narrative summary.
- Phase 8A is navigation/IA/copy only, deliberately not a visual redesign: no configurable record-page layouts, no per-entity icons, no wholesale component restyle. The `schema.manage` gate covers both the entity page's top-level "Manage" toggle and the schema-editing content itself (`EntitySettingsForm`, `FieldCreateForm`, `FieldManagementList`): a caller without `schema.manage` who manually requests `?manage=true` is redirected to the plain records view rather than seeing any admin form render.
- CSV import (Phase 8C.2) is one existing object at a time, 5&nbsp;MB app-level file-size cap, synchronous request/response with no background job system. No schema-from-CSV (unmapped columns cannot create new fields), no CSV export, no fuzzy/partial relation matching or auto-creating missing relation targets, and no multi-object import in one file.
- Recurrence (Phase 8D.1) requires a real origin record — no template-only recurrence, no "run for every matching record," no saved-view/filtered-object or dynamic batch recurrence. Frequency is daily/weekly/monthly only (no cron syntax, no RRULE, no business calendars/holidays, no "last weekday"). Timezone is workspace-wide only, no per-user override. A missed scheduler interval catches up only the single latest due occurrence, never a backlog.
- Notifications (Phase 8D.2) are in-app only — no email, SMS, push, or Slack/Teams; no notification preferences or channel toggles (assignment/due-soon/overdue are unconditionally on); no digests; no configurable due-soon threshold (fixed at 24 hours); no repeated overdue nagging (one notification per step-run lifetime, ever); no manager/team alerts (recipient is always the individual assignee); no recurrence-started notification (event-only, by deliberate design — see Phase 8D.2 under Testing above). No comments/mentions. No retention/cleanup jobs; both `workspace_events` and `notifications` retain indefinitely.
- Activity (Phase 8D.3) is record-context only — no general workspace-events explorer, no team/manager/admin activity feed. No field-level record-change or relation-change history, no record-created event (no durable actor exists for ordinary creation today), no import-batch Activity, no workflow-execution-history Activity, no process-failure Activity (process failure/attention alerts remain deferred from 8D.2). No historical backfill — an existing record's Activity begins from 8D.3 onward, never fabricated from current state. Fixed 20-item cap, no pagination/cursor. `step_due_soon`/`step_overdue` stay notification-only, never shown in Activity. No impersonation/effective-user actor support — a future admin "act as" feature will need real actor-vs-effective-user semantics this milestone does not build.
- Search (Phase 8D.4) matches only `type = 'text'` fields, active records, active entity types — no fuzzy/typo-tolerant matching beyond plain substring, no advanced query syntax, no search for workflows/views/settings, no archived-record search. Fixed 20-per-entity-type cap, no cursor pagination. Plain `LIKE` substring matching, deliberately not full-text search or a trigram-indexed/maintained search projection — benchmarked to 10,000 records per entity type and found clearly fast enough (see Phase 8D.4 under Testing above); revisit only if a real workspace's scale or query patterns prove otherwise.
- The Business dropdown's quick-jump list is a deterministic alphabetical slice, not favorites/pins/"recently viewed" — no per-user navigation preferences exist yet, so nothing claims to track real usage or recency. No object grouping/categories, no configurable per-object icons. Configure's visual treatment kept the sidebar-era strong Graphite ground on the header rather than the alternative light-header direction considered during planning; that alternative was deliberately not implemented, not merely deferred by omission. Responsive behavior below the `lg` breakpoint is a single flattened "Menu" disclosure, not a mobile-optimized IA.

## Development Principles

- Plan -> review -> implement -> test -> commit.
- Prefer small vertical slices over broad infrastructure.
- Favor structural database integrity where it meaningfully protects data.
- Keep workspace scoping explicit in repository calls.
- Keep server-side validation authoritative.
- Do not silently rewrite/delete/null dependent data.
- Preserve backwards compatibility where reasonable.
- Use immutable IDs/keys for stored references; allow user-facing names/slugs to evolve.
- Build generic foundations with specific, understandable UI language.
- Avoid premature libraries, frameworks, ORMs, and deployment infrastructure.
- Use Server Components and Server Actions where they fit; Client Components are for interactive form state.
- Use accessible selectors in tests.
- Run lint, typecheck, webpack build, and E2E tests sequentially before declaring milestones complete.

## Agent Working Agreement

- The repository and checked-in migrations are the source of truth.
- Read `PROJECT_CONTEXT.md` before starting substantial work.
- Inspect existing code before proposing new architecture.
- Plan first when a milestone involves product semantics or architectural choices.
- Keep milestones small and coherent.
- Do not silently change established behavior outside the requested milestone.
- Reuse existing domain/repository abstractions where practical.
- Favor structural database integrity where appropriate.
- Do not silently rewrite or delete dependent data.
- Preserve backwards compatibility where reasonable.
- During implementation, prefer targeted verification: lint, typecheck, and directly relevant E2E spec(s).
- At milestone completion, run the full gate sequentially:

```bash
npm run lint
npx tsc --noEmit
npm run build -- --webpack
npm run test:e2e
```

- Do not run typecheck and build concurrently because of the known `.next` generated-types race.
- Update `PROJECT_CONTEXT.md` only when a milestone is complete or when durable architecture/process decisions change.
- Do not begin the next milestone without approval.
- If another coding agent has worked on the repo since your last session, re-read `PROJECT_CONTEXT.md` and inspect the current diff/history before proceeding.

## Multi-Agent Development

This project may be worked on by Codex, Claude Code, or other coding agents.

- Only one agent should actively implement a milestone at a time.
- Agents should not assume their prior chat history is current.
- Each new session should orient from `PROJECT_CONTEXT.md` and the repository.
- Completed milestones should be tested, documented, and committed before another agent begins the next milestone where practical.
- A second agent may review another agent's work, but should not simultaneously rewrite the same feature unless explicitly requested.

## Current State And Next Frontier

Complete major capabilities:

- Configurable entity types and primitive/relation fields.
- Metadata-driven record creation, editing, table display, archiving/restoring, and safe deletion.
- Entity lifecycle management: rename, archive/restore, safe deletion.
- Field lifecycle management: add, edit safe properties, archive/restore, safe deletion.
- Relation fields with dedicated relational persistence and generic labels.
- Configurable entity display fields.
- Saved table views with filters, sorts, visible columns, and default view selection.
- Workspace home and shared navigation with active entity cards and capped saved-view shortcuts.
- First-run workspace onboarding with small editable Clients, Projects, Tasks, and Opportunities starter structures, atomically created through the authorized metadata setup wrapper; established Home and navigation emphasize operational record work while configuration is progressively disclosed.
- Operational UX Polish: shared page primitives, a table-first entity workspace, inline Add Record disclosure, explicit Manage views disclosure, secondary row/detail lifecycle actions, clearer record-form hierarchy, and refined Home/search/navigation presentation. It adds no domain or persistence capability.
- Workspace record search across active entities and records, with deterministic per-entity result caps and detail-page links.
- Record detail pages with outgoing links and derived reverse relationship visibility.
- Create related records from reverse relationship groups using the standard record-create form and validated origin-detail return navigation.
- Workflow management: create/edit/enable/disable/delete.
- Workflow triggers for record created and record updated.
- Workflow actions for create record, update triggering record, update one record reached through a direct triggering-record relation, and start one configured compatible Process Template against the original triggering record — composable as an ordered sequence of multiple actions per workflow (add/remove/reorder in the editor), executed sequentially with first-failure-stops semantics, no cross-action rollback, and structured per-action execution logging (`action_results`). Unknown/crafted action types are rejected by server form parsing rather than coerced to another action.
- Conditions, watched fields, constants, source-field mappings, text templates, relation mappings, deterministic execution, isolated failures, no recursion, and execution logs. Conditions/watched fields are workflow-level, evaluated once against the original triggering event regardless of how many actions the workflow has.
- Transition-aware record_updated conditions (`changed`, `changed_from`, `changed_to`, `changed_from_to`) evaluated against the previous/current values from the original user-edit event, with a save-time-and-execution-time-enforced invariant that a transition condition's field must also be watched.
- A consistent light UI theme: the app shell is pinned to its light palette regardless of OS/browser dark-mode preference, matching the hardcoded light-card design used throughout, so text never renders on a mismatched background.
- Entity hard deletion is blocked when a workflow's `create_record` action still targets that entity, alongside the existing record-count and relation-field checks — deletion is blocked, never cascaded, and the dependent workflow is left untouched.
- Faster Record Work: inline click-to-edit editing of text/number/date/boolean fields directly from the entity table, with explicit commit/cancel semantics, no autosave-on-blur, `router.refresh()`-based UI sync, a no-op short-circuit, and full reuse of the canonical record-update validation/repository/workflow path — inline edits fire `record_updated` workflows (including transition-aware conditions) exactly as the full edit form does. Relation and identity/display fields remain read-only inline; no migration or repository change was required.
- Process Templates + Process Runs (v1 foundation): reusable linear human-task templates (`ProcessTemplate`/`ProcessNode`/`ProcessEdge`, graph-capable persistence), started manually per compatible record or by a configured workflow action against its triggering record, into a `ProcessRun`/`ProcessStepRun` with full template/step snapshotting, sequential step completion with atomic activation cascade, at-most-one-active-run-per-template-per-record enforcement, and safe-delete integration blocking entity/record/template deletion while applicable/referenced. Five new select-only-for-authenticated tables, membership-checked SECURITY DEFINER write RPCs, no raw mutation path.
- Fixed Process-Step Assignment + My Work: each `human_task` node may be unassigned or fixed to one current workspace member, structurally guaranteed to belong to that workspace (composite FK to `workspace_memberships`) and structurally blocking membership removal while assigned — including on archived templates. Assignment/label are snapshotted onto `ProcessStepRun` at run start so later reassignment or membership loss never rewrites history. Completion is assignee-only when a step has an assignee, open to any workspace member otherwise, enforced inside `complete_process_step_run_authorized` via `auth.uid()`. A new primary-nav "My Work" page shows the authenticated user's own assigned active/pending steps (Ready now / Upcoming) from active runs only, deriving identity server-side, never from client input. Identity display is a single narrow RPC (`list_workspace_member_identities_authorized`) exposing only `{user_id, email}` — no profile subsystem, no Team Member entity linkage.
- Process Due Dates + Attention: optional bounded `hours`/`days` due rules live on ProcessNode config and snapshot into each StepRun; `due_at` is persisted only upon activation, with days defined as 24 elapsed hours. Overdue is derived from an active due StepRun, never a lifecycle state. My Work prioritizes Overdue, then other Ready now work, then Upcoming pending work. Due timestamps render as browser-local absolute dates, and no scheduler/reminder delivery exists yet.
- Process Graph 5A — Conditional Branching: template position is only editor/topological order; execution uses immutable per-run route snapshots. A source has one unconditional/default successor or ordered conditional routes plus exactly one default; first matching AND-condition wins, otherwise default. One successor activates, unreachable pending StepRuns become `skipped`, and reconvergent reachable steps remain pending. Branch checks use current live origin values; missing/archived condition fields fail completion atomically and may be retried after restoration. My Work stops its deterministic upcoming projection at unresolved splits and never shows mutually exclusive work early. Safe field deletion blocks both live template references and unresolved active-run route snapshots. `routing_result` retains selected route/target/outcome/time/concise condition context.
- Process Graph 5B — Parallel Paths + Joins: explicit split/join system nodes form validated, non-nested/non-overlapping parallel regions. A split activates all branch tokens; each branch may use ordinary conditional routing, an approval, or a wait; a join advances only after every expected token arrives, then its downstream work activates exactly once. Template node/route configuration and group/token topology are snapshotted per run, branch assignments/due dates remain local, and My Work may show multiple active assigned human tasks or approvals while suppressing waits/join/system work. Direct system-step completion is forbidden, join obligations are select-only runtime state, and malformed graph edits reject atomically. Parallel regions do not add loops, general parallel joins, automated nodes, or a visual graph builder.
- Process Graph 5C.1 — Approval Nodes: an approval is one explicitly assigned or unassigned human decision with two or more named, stable UUID-backed outcomes. Outcome IDs survive rename, reorder, and target changes; labels are trim/case-fold unique; multiple outcomes may share a target. Run start snapshots every outcome ID/label/target, so live template edits never alter active-run choices. `decide_process_approval_authorized` records the selected outcome and actor/time separately on the completed StepRun, then appends an `approval_outcome` routing result and advances exactly one snapshotted route. It uses the same assignment, due-date, My Work, advisory-lock, conditional, and parallel branch-token/join-obligation semantics as human tasks, but generic completion never accepts it. No comments, voting, delegation, escalation, timers, automation nodes, or reminders are included.
- Process Graph 5C.2 — Wait States: a wait is a timer-only system node with a structured, self-contained rule and a deterministic UTC `resume_at` calculated once at activation. It supports elapsed hours, timezone-aware calendar days/weekdays, and bounded calendar targets; it is scheduler-resumed through a narrow service-only RPC, has no assignee/due rule/manual completion/My Work entry, and preserves run snapshot, conditional-route, branch-token, and join semantics. External scheduler provider selection, holidays/business calendars, manual skip/cancel/resume, reminders/notifications, recurrence, automated nodes, and visual builder work remain deferred.
- Process Graph 5C.3 — Condition/Event Waits: a condition wait snapshots one direct origin/related target and typed AND conditions, then resumes only through the service-only durable wakeup dispatcher when current authoritative values satisfy them. Dependencies and wakeups are distinct, related targets can rebind, and direct or workflow-originated writes use the same trigger/outbox path. No polling condition, arbitrary/reverse/multi-hop traversal, condition-history UI, manual resume/skip/cancel, notifications, automated nodes, or visual builder work was added.
- Process Graph 6A — Visual Process Builder Foundation: the template editor gained a List/Graph toggle over one shared client-side step/route state, with no new persistence or migration. Graph view renders every node type distinctly (human task, approval, wait, condition wait, parallel split/join) and every route as a labeled edge, including conditional routing and approval outcomes as edge/route behavior rather than a separate node type. Selecting a node opens a side panel built from the same shared per-node editing components the List view uses, so the two views can never present different controls. Layout is computed fresh on every render from existing node position/edge data — never stored — using plain rank ordering plus a bounded lane walk for parallel regions; no graph layout library was needed. Reordering remains List-only. Fixed alongside verification: a pre-existing bug where reopening a template with a `condition_wait` step for editing lost its saved condition configuration.
- Process Graph 6B — Direct Graph Editing: the Graph view became a practical primary editor without a second graph semantics layer — insert-on-edge (add), an explicit delete choice (reconnect vs. not) for unambiguous nodes, and a guarded button-based reorder (List and Graph both refuse an adjacent swap that would break an existing route's forward validity, with a visible reason) are all sugar over the same `steps`/`routes` array model 6A established. Non-adjacent edges now route through a deterministic side gutter instead of drawing through occupied node cards, fixing the known occlusion issue. Fixed alongside verification: the wait-amount validator's regex was double-escaped inside a regex literal and rejected every wait amount unconditionally. Found and flagged, then fixed in a follow-up pass (Wait UI Bug Fixes, below): a client-side crash on any interaction with the Wait Amount input, and a separate bug where hours-duration waits always failed to save due to an always-resnapshotted timezone the RPC rejects.
- Wait UI Bug Fixes: fixed the Wait Amount input crash (every `WaitConfigFields` handler read the SyntheticEvent's `currentTarget` inside a Strict-Mode-doubled, asynchronously-invoked state updater — fixed by capturing each value synchronously in `onChange` instead) and the hours-duration timezone leak (`getWaitRule` now gates timezone inclusion on `unit === "calendar_days"` rather than truthiness). Both verified by reverting each independently and confirming the regression test genuinely fails, then passes restored.
- Process Graph 6C — Visual Process Run / Execution View: Process Run detail gained the same List/Graph toggle (List remains default) over one runtime-snapshot-driven graph — no second runtime semantics model and no new persistence. `computeProcessGraphLayout` was type-narrowed to a minimal structural interface so the same layout algorithm and gutter-routing serve both template editing and run visualization; a dedicated adapter maps a run's already-immutable snapshotted steps/routes into that shape. Nodes render completed/active/pending/skipped status as both color and explicit text; routes render a three-way taken/untaken/undetermined state driven by each step's own `routingResult`, using color/opacity/width rather than dashing alone so it can't be confused with the template graph's dash semantics. Approval and conditional decision history, parallel branch/join progress ("N of M branches joined"), and wait/resume context all reuse the same prose the List view already had, via a shared summaries module. The existing `CompleteStepButton`/`ApprovalDecisionButtons` — no new action components or server actions — surface in the Graph side panel per selected step-run, gated by the same active/human-actionable/authorized rule List already enforced.
- Automated Action Nodes: a process step (`action` node) performs create record, update origin record, update related record, or start process automatically on activation, reusing the exact `WorkflowAction` shape and `executeSingleAction` executor workflows already use. One canonical system action-step executor runs identically whether the step was activated by human completion, an approval, a timer wait, a condition wait, another action, or parallel/system advancement — the wait/condition-wait scheduler drains any action node it activates through that same executor under its own admin client, never a separate or broader execution path. Failure represents entirely through `action_result.status = 'failed'` on an otherwise-still-`active` step (no new runtime status), visible in List and Graph with a Retry control open to any workspace member. `create_record`/`start_process` are idempotent against a retry after a crashed-but-successful attempt via a durable `originating_process_step_run_id` identity, closing the specific duplicate-creation window that risk implied. My Work, template/entity/field safe-delete, and process-template deletion all treat action nodes consistently with every other config-bearing node type.
- Automated Playwright E2E harness covering representative entity, relation, archived-relation edit preservation, display-field, saved-view, record-detail, related-record creation, workspace navigation/search, workflow, record-updated, record-updated transition conditions, update-record, update-related-record, multiple-ordered-actions, dark-mode-contrast, inline record-editing, process template/run behavior, and process-step assignment/My Work behavior.
- Supabase Auth email/password sign-in, protected workspace access, explicit memberships, active-workspace selection, RLS tenant isolation, immutable workspace ownership, and authorized record mutation wrappers.
- Phase 7A — Roles and Permissions Foundation: one role per membership, custom roles, membership-wide workspace reads, capability-gated interactive mutation/configuration, atomic last-administrator-safe role transitions, and a narrow Members and Roles management UI.
- Phase 7B — Teams & Organizational Structure: flat workspace teams, multiple memberships/leads, and optional primary-manager reporting relationships, all kept separate from roles/capabilities and workspace-wide record visibility. Workspace Settings exposes functional Teams and Reporting controls to organization managers; no nested teams, team visibility rules, ownership, or profile/invitation system is included.
- Phase 7C — Manager / Portfolio Experience: Team Work is a scoped operational view over existing assigned human tasks and approvals for direct reports and active led-team members. It adds no new record/process visibility boundary, employee profile, recursive hierarchy, workload scoring, historical analytics, notifications, team-owned records, or reassignment/balancing behavior.
- Phase 7D — Operational Analytics & Performance Trends: Operational Analytics (`/analytics`) turns existing process history into a workload snapshot, throughput/timeliness summary and trend, a dwell-time bottleneck table, and person/team workload breakdowns — scoped identically to Team Work, sharing its exact management-scope derivation (`private.managed_user_ids`), and drilling into Team Work's/the process template editor's existing pages rather than new ones. It measures work, not people: raw counts and medians only, no scores or rankings, no workspace-wide surface, and no historical org snapshot (current scope is used for both access and attribution, disclosed on the page).
- Phase 8A — Worker Experience & Information Architecture (COMPLETE): navigation now genuinely reflects capability (a "Configure" section, renamed from "Workspace setup," with every link individually gated, closing a prior gap where configuration links were visible to everyone regardless of capability), Home leads with a personal attention strip and a manager-only team-attention line rather than entity cards alone, "Workflow" is relabeled "Automation" everywhere product-facing (internal names/routes unchanged), "Entity" is avoided in ordinary copy in favor of "business object"/"Data model", inactive sidebar text uses a new higher-contrast token, and a follow-up hardening pass closed the `?manage=true` schema-content-gating gap (redirects unauthorized callers to the plain records view instead of hiding form controls in place). No new tables, RPCs, domain types, or authorization semantics — purely navigation, copy, and one CSS token. Final gate: lint/typecheck/build clean; E2E verified sound via 8 full-suite attempts plus targeted isolated confirmation of every failure (zero deterministic defects, all scattered flakes matching this project's established under-load timeout/network-blip taxonomy).
- Application Shell & Scalable Navigation (post-8A, COMPLETE): the per-page sidebar is gone, replaced by a persistent global header (`app/components/app-header.tsx`) mounted once in a new `app/(app)/layout.tsx` shell, plus a contextual saved-view rail (`app/components/object-context-nav.tsx`) shown only on business-object pages. Business objects no longer enumerate as an unbounded sidebar list — a capped alphabetical quick-jump dropdown plus a new All Objects index page (`app/(app)/entities/page.tsx`) replace it, scaling to any object count. Configure holds Automations/Processes/Data model/Workspace settings, each gated on its own capability (`schema.manage` included, after a review pass corrected an interim version that had dropped it). Business → All objects and Configure → Data model both reach `/entities`, but present as genuinely distinct worker-browsing vs. builder-configuration modes via `?manage=true` (a follow-up fix after the two initially rendered identically, undermining the intended IA) rather than a second object-management system. A pre-existing latent bug in `listCurrentUserMemberships` (workspace name always resolving to `""`) was found and fixed in passing. No authorization semantics changed — purely a shell/IA relocation. Final gate: lint/typecheck/build clean; full E2E suite 191/196 passing on the first attempt, 2 failures both classified and confirmed as this project's established environmental/under-load flake signatures via isolated rerun (14/14 clean), 0 product defects.
- E2E Reliability Hardening (COMPLETE, test-infrastructure only): a shared `expectAfterMutation` helper (documented 15s timeout) for the specific post-Server-Action-submit assertion shape that recurred across the whole flake history, applied to four evidenced sites; a narrow, classified retry policy (network/5xx only, max 3 attempts, logged) for Supabase Auth Admin API calls and this suite's shared fixture create/cleanup helpers, deliberately excluding a non-idempotent record-creation RPC and the ~29 files' own ad-hoc fixture calls; cleanup helpers now attempt every step and aggregate failures instead of aborting on the first one. No product code changed; no global Playwright config changed. Full E2E suite improved to 192/196 on one run (18.2 min) — the cleanest full-suite baseline recorded in this document — with the one remaining failure a newly-evidenced instance of the same known pattern, hardened in response and reverified in isolation, not by re-running the full suite again.
- Phase 8B — Business Object Experience (record detail restructure): the record detail page's flat field list plus separate "Related Records" section became three sections — Overview (every non-identity field, primitive or forward relation, in one canonically-position-ordered populated-first capped summary with a full-canonical-order "All fields" expand; primitives inline-editable via the same `EditableTableCell`/`updateRecordField` wiring the records table already used, relations read-only chip/link), Related (reverse-derived relationship collections only, capped preview with expand), and Processes (`ProcessSection`, unchanged). A dogfood-informed closing pass added worker-facing terminology cleanup ("More actions", plain "Search", truthful "← {entity}" collection links), builder field reordering (Move Up/Move Down, no migration — `position` already existed), and a relation-label-resolution performance fix (`listEntityRecords` gained an optional `ids` filter, used by `listIncomingRelationsForRecord` and an opt-in detail-page mode of `getRelationLookups` to stop fetching an entire related entity type's table just to resolve a handful of labels). No new tables, migrations, or authorization semantics anywhere in this phase. See Phase 8B under Testing above for full detail.
- Phase 8C.1 — Empty-Workspace Authority Correction: a genuinely empty workspace's Home page now shows the onboarding wizard only to a `schema.manage` caller; anyone else sees a read-only "Nothing has been set up yet" state. Closed the underlying authority gap too, not just its UI: `create_entity_types_with_fields_authorized` had been membership-only-checked since before the roles/capabilities system existed and was never updated once it did — migration `0060` brings it in line with every other schema-write path. `/entities/new` gained a matching page-level gate for UX (its own RPC was already RLS-protected, so this closes a confusing dead-end, not a bypass). See Phase 8C.1 under Testing above for full detail.
- Phase 8C.2 — CSV Import Pipeline: import CSV rows into one existing business object, entered from the object's own collection experience (empty-state CTA and a persistent "Import CSV" link), gated on `records.operate` alone. Server-authoritative parsing, an editable column-mapping step with exact-match suggestions, strict per-type parsing rules, and relation import by exact UUID or unique label match. Preflight validates every row into Ready/Error before commit is even clickable; commit is one all-or-nothing transaction. New durable idempotency ledger `record_import_batches` (migration `0061`, fixed for an ambiguous-column bug by `0062` before the retry/cross-workspace/cross-entity-type paths worked) makes a repeated client-generated import id a safe no-op retry for the *same* workspace and entity type, and an explicit rejection — never another workspace's or object's result — for any other. First Vitest unit-test infrastructure in this project (`lib/domain/*.test.ts`). Verified to 5,000 rows synchronously (~1.5s direct, ~6s through the full browser/Server-Action/RPC path) with no background-job system needed. Dogfood surfaced and fixed one real client-side double-submit bug (rapid repeated Import clicks each generating an independent import id before React re-rendered could disable the button); the backend idempotency boundary itself needed no change. No schema-from-CSV, no CSV export, no multi-object import. See Phase 8C.2 under Testing above for full detail.
- Phase 8D.1 — Recurrence: origin-record-anchored process scheduling ("run this process for THIS business object on a schedule"), gated on `automation.manage`. Daily/weekly/monthly frequency with an interval, optional end date, and workspace-timezone-interpreted local time of day; day-of-month clamps to a shorter month's last valid day rather than skipping. New durable idempotency ledger `process_recurrence_rules`/`process_recurrence_occurrences` (migration `0063`) makes a scheduler invocation safe to repeat or overlap — a missed interval catches up only the single latest due occurrence, never a backlog. `start_process_run_system`/`discover_and_start_recurrence_occurrences_system` are `service_role`-only, structurally unreachable by any interactive caller, delegating to the same canonical, unmodified process-start implementation every manual/workflow-triggered run already uses. New `workspaces.timezone` column (default UTC) is the first real use of the `workspace.manage_settings` capability. No template-only or dynamic/filtered-batch recurrence, no notifications/reminders, no activity trail yet. See Phase 8D.1 under Testing above for full detail.
- Phase 8D.2 — In-App Notifications: assignment/due-soon/overdue notifications for human_task/approval steps, generated from the one canonical step-activation function (`private.activate_process_step_run`) every activation path already shares, plus a new due-soon/overdue scheduler pass added to the existing internal route. New thin shared log `workspace_events` (bounded 4-type vocabulary, no FKs on its reference columns, closed read access — infrastructure for 8D.3's Activity, not yet a read surface) and recipient-scoped `notifications` (migration `0064`), both using the same claim-before-side-effect idempotency shape as 8C.2/8D.1. My Work and Notifications are strictly decoupled — verified by a dedicated test that a step-run row is unchanged by marking its notification read. Recurrence-started runs get no dedicated notification (a deliberate decision: the useful case is already covered by the ordinary assignment notification on the run's first step). In-app only; no email/SMS/push, no preferences, no digests, no manager alerts, no repeated overdue nagging. See Phase 8D.2 under Testing above for full detail.
- Phase 8D.3 — Activity: a record-context timeline (Overview → Related → Processes → Activity) answering "what meaningful things have happened here," built entirely on 8D.2's `workspace_events` table — no new table. Four visible event types (`process_started`, `process_completed`, `step_assigned`, `approval_decided`, migration `0065`), each emitted from the one canonical transition it already funnels through; `step_due_soon`/`step_overdue`/`recurrence_started_process` stay recorded but excluded from the projection. The canonical `start_process_run_authorized_member` gained a required (no-default) actor parameter so a human "Start Process" click, a workflow-triggered start, and a recurrence-triggered start can never be conflated — a workflow-triggered start deliberately records no actor even though it runs under the triggering editor's session, avoiding a "last editor" misattribution. No metadata duplication: rendering joins the durable snapshots (`process_template_name`, step `name`, `assignee_label`, `approval_outcome_label`, `decided_by_label`) already living on `process_runs`/`process_step_runs` from earlier migrations. `list_record_activity_authorized` is the sole read path, same membership bar as an ordinary record read, capped at 20, newest-first. No field-level record audit, no record-created event, no import Activity, no historical backfill, no team/admin activity feed. See Phase 8D.3 under Testing above for full detail.
- Phase 8D.4 — Search: global Search's filtering, ranking, and per-entity-type cap moved from Node (fetch every active workspace record, match in JavaScript) into one Postgres RPC (`search_workspace_records_authorized`, migration `0066`) — no new table, column, or extension. Plain case-insensitive `LIKE` substring matching is preserved exactly (deliberately not full-text search, which matches whole/stemmed words, not arbitrary mid-word substrings), with the query's own `%`/`_`/`\` characters escaped so they can never be reinterpreted as wildcards. Only `type = 'text'` fields on active records/entity types are ever searchable; ranking (identity-field match, then prefix, then field position) and the existing 20-per-entity-type cap are both now enforced in SQL. New object-type filter. `pg_trgm`/a maintained search-text projection was deliberately not added — benchmarked to 100/1,000/10,000 records per entity type first (query time stayed under ~250ms even when every row in a 10,000-row tier matched, payload stayed a few KB regardless of table size) and found clearly fast enough, so the smaller architecture was kept. Follows this codebase's established `SECURITY DEFINER`+explicit-membership-check RPC pattern rather than relying on RLS alone, for consistency. See Phase 8D.4 under Testing above for full detail.
- Phase 8E.1 — Member/Invitation Lifecycle: shareable-link workspace invitations (create/resend/cancel/accept, no email delivery) and soft member deactivation/reactivation (`workspace_memberships.deactivated_at`, migration `0067`), replacing the prior manual-SQL-only path for workspace membership changes. Deactivation is enforced at the two foundational authorization primitives (`private.is_workspace_member`/`private.has_workspace_capability`) rather than scattered checks, extends the existing last-admin guard, and is a hard block on self-deactivation. A brand-new invitee's account is provisioned through a narrow, explicitly-scoped service-role exception (`lib/supabase/admin.ts`) — account creation only, never session minting or impersonation. See Phase 8E.1 under Testing above for full detail.
- Phase 8E.2 — Admin Impersonation ("Log in as"): same-workspace impersonation for `records.operate`/`processes.operate`, genuinely executing and attributing worker actions as the effective user via a DB-derived identity helper (`private.current_effective_user`, migrations `0068`–`0070`) rather than session-swapping or a client-supplied identity — `auth.uid()` itself is never altered; a small, explicitly curated set of RLS policies and RPCs opt into resolving the effective user instead. Governance and builder capabilities stay bound to the real actor by design; Configure is hidden while impersonating. New `workspace.impersonate_users` capability, backfilled onto the built-in Administrator role only. Session lifecycle is self-healing (deactivating the target ends the session automatically) and durably audited (`impersonation_started`/`impersonation_ended`, plus `workspace_events.real_actor_user_id` on the two worker-action events that support it). See Phase 8E.2 under Testing above for full detail, including two real bugs found and fixed post-migration.
- Phase 8E.3 — Workspace Health: `/settings/health`, gated on `workspace.manage_settings`, surfaces five deterministic structural findings computed live by one RPC (`list_workspace_health_findings_authorized`, migration `0071`) — no persisted findings table, score, AI assessment, acknowledgement workflow, or background scanner. Checks: business objects with no active fields or no usable display field (mutually exclusive by construction), active recurrence rules that can structurally never fire again (archived template, archived origin, or an already-passed `end_date`), active process runs stuck with no active/pending steps, and active human/approval steps assigned to a now-deactivated member. Findings are grouped by check type with a direct "Fix" link to where each is resolved; hidden from and unreachable by workers and by an impersonating admin, same as the rest of Configure. See Phase 8E.3 under Testing above for full detail.
- Phase 8F.1 — CSV Export: symmetric complement to 8C.2 import, one business object at a time, active records/fields only, membership-level read authorization (no new capability, `records.operate` not required). Reuses import's own read paths (`getEntityContext`/`listEntityRecords`/`getRecordLabel`) rather than inventing new ones; a relation cell exports the target's normal resolved label, undecorated, even when archived (archived targets are found by label on re-import but explicitly rejected as a documented round-trip limitation, matching `resolveRelationValues`'s existing behavior). RFC 4180 serialization via `csv-stringify/sync`, the sibling of the already-used `csv-parse/sync`. No migration. See Phase 8F.1 under Testing above for full detail.
- Phase 8F.2 — Outbound Webhooks: deliver `process_started`/`process_completed`/`approval_decided`/`step_assigned` events to admin-configured HTTPS endpoints. New dedicated `workspace.manage_integrations` capability (migration `0073`, backfilled onto the built-in Administrator role only) rather than reusing `workspace.manage_settings`, intended for 8F.3 and later connectivity surfaces to share. `webhook_subscriptions`/`webhook_deliveries` (unique on `(subscription_id, event_id)`), fanned out by an `AFTER INSERT` trigger on `workspace_events` (same pattern as `entity_records_process_condition_wait_wakeup`, 0038) — no changes to any event-emission call site. A new, separate `/api/internal/webhook-deliveries` dispatcher route (sharing only a bearer-secret helper with `process-waits`) claims due deliveries via a lease-based `SKIP LOCKED` RPC, delivers concurrently, and retries transport/429/5xx failures with fixed backoff up to 6 attempts while treating other 4xx as terminal. SSRF protection (`ipaddr.js`-based, canonical parsing, DNS re-resolved before every attempt, every resolved address inspected, IPv4-mapped IPv6 handled) and Stripe-style HMAC signing (plaintext-at-rest secret, closed RLS, shown once) are both real, tested boundaries, not stubs. No delete action in the UI — only disable, so delivery history is never silently lost. See Phase 8F.2 under Testing above for full detail.
- Phase 8F.3 — Read-only API Foundations: a bounded four-route `/api/v1/objects...` surface authenticated by workspace-scoped, SHA-256-hashed bearer keys (`kinema_live_...`, shown once, `api_keys`/`api_key_rate_limits`, migrations `0074`/`0075`), reusing `workspace.manage_integrations` for key management. Every externally-reachable RPC takes only a key hash and derives its own workspace from it — never a passed-in workspace id — so a route-handler bug structurally cannot produce a cross-workspace read; route handlers issue no direct table reads at all. Fixed V1 scope (`records:read`, not user-selectable) with the enforcement check already in place for future write scopes. Keyset pagination on both list endpoints with a truthful `nextCursor` (`limit + 1` fetched internally, trimmed to `limit` externally). Relations resolve as `{id, label}`, sourced from `entity_record_relation_values` (never `entity_records.values`, which never contains them) via a SQL reproduction of the canonical `getRecordLabel` fallback chain; record values are projected onto each entity type's currently-active field set, so an archived field's stale stored value can never leak through. Rate limiting (60/min/key) uses a separate, always-committing RPC called before the data RPC, specifically to avoid a same-transaction rollback undoing a rejected request's own counter update. Found and fixed live: the relation-source assumption, the archived-field leak, and a site-wide `proxy.ts` middleware bug (unrelated to this phase's own code) that had silently blocked every bearer-authenticated route — both this phase's and 8F.2's — from ever reaching its handler over real HTTP. `Configure → Webhooks` renamed to `Configure → Integrations`, now hosting both webhook subscriptions and API keys. See Phase 8F.3 under Testing above for full detail.

Sensible next areas, without committing to architecture yet:

- Impersonation V2 candidates, not yet approved: effective-user-aware UI capability-gating beyond Configure, effective-user-aware notifications (would require converting their raw RLS-gated read into an RPC), and a production support mode (reason capture, richer audit history, optional read-only access) per ROADMAP's "Later" tier.
- Workspace Health V2 candidates, not yet approved: `deactivated_assignee` coverage for `pending` (not just `active`) steps, additional deterministic checks (e.g. orphaned relationships) if they can be defined without fuzzy/heuristic judgment, and the broader Workspace Governance & Hygiene Policies area from ROADMAP (recurring cleanup rules, duplicate detection, configurable archive policies) — explicitly a later, larger product area, not an extension of this slice.
- Process reminders/notifications.
- Dynamic assignment (from an origin-record field, roles, expressions, round-robin, groups), workload counts/dashboards, and general reassignment of an in-flight step — all deliberately deferred from Fixed Process-Step Assignment + My Work.
- A Team Member entity concept that can optionally link to an authenticated workspace member, if/when richer people data is needed beyond email.
- Process loops, generalized gateways, automated/subprocess node types, and richer branch expressions.
- Freeform graph manipulation beyond 6B: drag-to-move node positioning, drag-to-connect/rewire edges, layout persistence, and moving a node across branches or into/out of a parallel region without delete-and-recreate.
- Process-*run* (instance) graph visualization, distinct from the template graph view Process Graph 6A/6B added.
- Future hardening of the remaining SECURITY-INVOKER entity/field create/update RPCs so their raw same-workspace mutation grants can be removed.
- Multi-role memberships, ownership, team-based record visibility, manager portfolio views, and row-scoped visibility are deliberately deferred to later Phase 7 work.
- Better workflow observability/history and possibly durable background execution.
- Richer record detail capabilities such as layouts, comments, attachments, and activity.
- Field/entity editing beyond currently safe properties.
- Richer saved views such as additional operators or alternate presentation modes.
- More workflow actions or conditions; an "unset" sentinel for transition-condition operands.
- Local/separate Supabase test environment before CI.
- Eventually, AI-assisted configuration of deterministic entity/field/workflow definitions.
